# SimHub Parameter Inventory

**CANONICAL CONTRACT**

Validated against: HEAD
Last reviewed: 2026-06-07
Last updated: 2026-06-08
Branch: work

- All exports are attached in `LalaLaunch.cs` during `Init()` via `AttachCore`/`AttachVerbose`. Core values are refreshed in `DataUpdate` (500 ms poll for fuel/pace/pit via `_poll500ms`; per-tick for launch/dash/messaging). Verbose rows require `SimhubPublish.VERBOSE`.【F:LalaLaunch.cs†L2644-L3120】【F:LalaLaunch.cs†L3411-L3775】
- Legacy spreadsheet removed; this file is canonical.
- “Defined in” lists the class/method that computes the value before `AttachCore/AttachVerbose` publishes it.
- SimHub displays plugin exports under the plugin namespace. The code registers PreRace and Friends properties as unqualified internal names (`PreRace.*`, `Friends.Count`) so dashboards consume the single-prefixed public names (`LalaLaunch.PreRace.*`, `LalaLaunch.Friends.Count`) instead of accidental `LalaLaunch.LalaLaunch.*` names.

## Monitor System
| Exported name | Type | Units / meaning | Update cadence | Defined in |
| --- | --- | --- | --- | --- |
| MonitorSystem.State | string | Monitor operating state: `OFF` when `Dash Control -> Global Dash Functions -> General -> Enable Monitor System` is disabled; `ON` when enabled. The persisted setting defaults to enabled. `AUTO` is no longer emitted by active runtime behavior. | On monitor output or enable-setting change. | `MonitorSystem.cs` state presentation + `LalaLaunch.cs` setting hook/`AttachCore`. |
| MonitorSystem.Text | string | Dash-ready monitor message. Current catalogue texts are `MONITOR OFF`, `MONITOR READY`, `FUEL HEALTH OK`, `CHECK FUEL DATA`, `FUEL DATA RECOVERED`, `FUEL DATA FAULT`, `REFUEL OFF`, `MFD FUEL LOW`, `EXIT FUEL SHORT`, `BASELINE SHORT`, `OPPONENT DATA UNRELIABLE`, `TRAFFIC DATA UNRELIABLE`, or `H2H DATA UNRELIABLE`. Phase 2B pit-stop warnings, the Phase 2C baseline warning, and Phase 3A Car/Opp/H2H impossible-state checks are edge-triggered from existing subsystem outputs. | On monitor output change. | `MonitorSystem.cs`; existing fuel-health outcome seams plus Phase 2B/2C pit-stop warning checks and Phase 3A Car/Opp/H2H checks in `LalaLaunch.cs`. |
| MonitorSystem.BackgroundColour / TextColour | string/string | Simple hex colours paired with the current monitor enum/text. WARNING/FAULT use red background (`#FF0000`) with yellow text (`#FFFF00`). Dashboards should consume these directly unless deliberately applying their own accessible theme. | On monitor output or enable-setting change. | `MonitorSystem.cs`. |
| MonitorSystem.Enum | int | Phase 1 severity contract: `0=OFF`, `1=OK`, `2=WATCH`, `3=CAUTION`, `4=WARNING`, `5=FAULT`, `6=RECOVERED`. | On monitor output change. | `MonitorSystem.cs`. |

Phase 1 reports the existing start/runtime fuel-health check only while `MonitorSystemEnabled` is true. Disabled state is `OFF` / `MONITOR OFF` / enum `0`; enabling resets the surface to `ON` / `MONITOR READY` / enum `1`. Phase 2B records pit-stop monitor trigger moments through edge-only logs and can publish `REFUEL OFF`, `MFD FUEL LOW`, or `EXIT FUEL SHORT`; Phase 2C uses the same edge framework for `BASELINE SHORT`. Phase 3A adds edge/low-frequency report-by-exception checks for impossible already-published CarSA slot identity, Opponents target, and H2H target states only; it does not add fallback target selection, self-healing, checkpoint validation, race-order validation, gap recalculation, or new exports. It adds no SimHub exports/properties, pit commands, or `Pit.Command.*` feedback replacement. The optional debug-only `EnableMonitorEventCsv` setting records changed non-normal MonitorSystem publications to `MonitorSystem_Events.csv` and is not a SimHub export. The message catalogue is `Docs/Internal/MonitorSystem_Messages.csv`. `MonitorSystem.*` belongs to the existing Property Snapshot `Fuel/Strategy` group.

## Brake
| Exported name | Type | Units / meaning | Update cadence | Defined in |
| --- | --- | --- | --- | --- |
| Brake.PreviousPeakPct | double | Peak brake input from the most recently completed braking event (normalized 0–1). Event begins when brake exceeds 5% while throttle is below 20%; while active, peak tracks `max(peak, brake)`. Event ends when brake falls to 2% or below, or throttle rises to 20% or above; on end, the event peak is latched to this export and capture enters a short re-arm lock that requires at least three consecutive release ticks where either `brake <= 0.02` or `throttle >= 0.20` before a new event can start. Manual/session recovery resets clear active + latched state/counters and set the export to `0.0` so stale prior-session peaks cannot leak forward. | Per tick. | `LalaLaunch.cs` — runtime `DataUpdate` brake event detector + `AttachCore`. |

## Fuel
> Export cleanup caution: do not remove/rename fuel exports until both audits are complete: (1) dashboard JSON usage audit, and (2) internal C# reference/consumer audit.

| Exported name | Type | Units / meaning | Update cadence | Defined in |
| --- | --- | --- | --- | --- |
| Fuel.LiveFuelPerLap | double | Rolling average burn per accepted lap (wet/dry windows, rejects pit/warmup/off-track/outliers). When accepted laps are unavailable, runtime uses profile condition burn before SimHub/DataCore fallback. | 500 ms poll (`UpdateLiveFuelCalcs`). | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` + `AttachCore`【F:LalaLaunch.cs†L1895-L2143】【F:LalaLaunch.cs†L2672-L2955】 |
| Fuel.LiveFuelPerLap_Stable / StableSource / StableConfidence | double/string/double | Smoothed burn chosen from live/profile (deadband hold; confidence aligned to source). | 500 ms poll. | `LalaLaunch.cs` — `UpdateStableFuelPerLap` + `AttachCore`【F:LalaLaunch.cs†L4180-L4254】【F:LalaLaunch.cs†L2672-L2955】 |
| Fuel.Burn.DisplayAnalysis | bool | Presentation-only Strategy Dash burn-popup/page state. `false` = normal burn display; `true` = analysis display. Toggled by plugin action `LalaLaunch.BurnDisplayToggle`; it is not a fuel-math input. | On action. | `LalaLaunch.cs` — action handler + `AttachCore`. |
| Fuel.Burn.Analysis.LastLap | double | Most recent fresh accepted fuel-lap burn across wet/dry. Seeded model values and rejected laps are excluded. Zero before the first accepted sample after Fuel Model lifecycle reset. | Per accepted fuel lap. | `LalaLaunch.cs` — accepted-fuel observer + `AttachCore`. |
| Fuel.Burn.Analysis.Avg3 / Avg5 / Avg10 | double/double/double | Average of the last 3 / 5 / 10 post-reset fresh accepted fuel laps across wet/dry, using the available count when fewer exist. All publish `0.0` with no post-reset samples and share only their dedicated rolling analysis list. | Per accepted fuel lap or `LalaLaunch.BurnAnalysisResetAverages`. | `LalaLaunch.cs` — accepted-fuel observer + reset action + `AttachCore`. |
| Fuel.Burn.Analysis.AvgSampleCount | int | Count of fresh accepted samples currently in the Avg3/Avg5/Avg10 rolling analysis list. Range `0` to `10`; use `>= 3` for Avg3 readiness, `>= 5` for Avg5 readiness, and `>= 10` for full Avg10 readiness. | Per accepted fuel lap or `LalaLaunch.BurnAnalysisResetAverages`; reset on lifecycle. | `LalaLaunch.cs` — accepted-fuel observer + reset action + `AttachCore`. |
| Fuel.Burn.Analysis.CurrentStint / StintSampleCount | double/int | Average and count of fresh accepted fuel samples since confirmed pit exit, normal Fuel Model lifecycle reset, or `LalaLaunch.BurnAnalysisResetCurrentStint`. They share the current-stint sum/count context and publish `0.0` / `0` safely when empty. | Per accepted fuel lap; reset on pit exit/action/lifecycle. | `LalaLaunch.cs` — accepted-fuel observer + pit-exit/reset seams + `AttachCore`. |
| Fuel.Burn.Analysis.SessionAvg / SessionSampleCount / SampleCount | double/int/int | Average and count of fresh accepted fuel samples since normal Fuel Model lifecycle reset or `LalaLaunch.BurnAnalysisResetSessionAverage`. `SampleCount` remains a compatibility alias of `SessionSampleCount`; all read the same session sum/count context and publish `0.0` / `0` safely when empty. | Per accepted fuel lap or session-average reset action/lifecycle. | `LalaLaunch.cs` — accepted-fuel observer + reset action + `AttachCore`. |
| Fuel.Burn.Analysis.MaxObserved | double | Highest fresh accepted fuel-lap burn since normal Fuel Model lifecycle reset or `LalaLaunch.BurnAnalysisResetMaxObserved`. Publishes `0.0` when empty/reset. | Per accepted fuel lap or max reset action/lifecycle. | `LalaLaunch.cs` — accepted-fuel observer + reset action + `AttachCore`. |
| Fuel.Burn.Analysis.MinObserved | double | Lowest fresh accepted fuel-lap burn since normal Fuel Model lifecycle reset or `LalaLaunch.BurnAnalysisResetMinObserved`. Seeded values and rejected laps are excluded. Publishes `0.0` when empty/reset and resets independently from `MaxObserved`. | Per accepted fuel lap or min reset action/lifecycle. | `LalaLaunch.cs` — accepted-fuel observer + reset action + `AttachCore`. |
| Fuel.Burn.Analysis.RemainingLapsMin / RemainingLapsMax | double/double | Conservative / optimistic analysis range from existing runtime current fuel litres divided by `MaxObserved` / `MinObserved`. Each publishes `0.0` when current fuel or its matching observed-burn input is invalid, non-finite, empty, or non-positive. | Runtime property read from synchronized analysis state and existing runtime fuel cache. | `LalaLaunch.cs` — burn-analysis synchronized getter + `AttachCore`. |
| Fuel.Burn.Target / TargetText / TargetValid | double/string/bool | Plugin-owned dashboard burn target selector. Phase selection uses `effectiveTargetStints = Fuel.Live.RemainingStints + 1.0` because `Fuel.Live.RemainingStints` excludes the current stint. Publishes `STINT` + `Fuel.StintBurnTarget` when `effectiveTargetStints > 2.0` (raw `Fuel.Live.RemainingStints > 1.0`); `SESSION` + `(min(current fuel + raw selected MFD fuel request, max tank) - active contingency litres + stationaryNoBurnCreditLitres) / Fuel.LiveLapsRemainingInRace_Stable` when `effectiveTargetStints > 1.0` (raw `Fuel.Live.RemainingStints > 0.0 && <= 1.0`); `END` + `Fuel.RequiredBurnToEnd` when `effectiveTargetStints <= 1.0` (raw `Fuel.Live.RemainingStints == 0.0`) and reserve/contingency does not still require another stop; otherwise `INVALID`, `0`, and `false`. SESSION stationary no-burn credit is fixed at 40.0 seconds of `Fuel.Refuel.SelectedBurnPerLap / selected runtime refuel projection lap seconds` per conservative `ceil(validated Fuel.Live.RemainingStints)` remaining stop, with no credit when any basis is invalid/non-positive. The credit accounts for a stationary car not burning fuel during pit-box service time and is not a timed-race distance/reduced-required-laps correction; timed and lap-limited fallback projections preserve the same `Fuel.LiveLapsRemainingInRace_Stable` remaining-lap denominator while still applying the box-time credit when inputs are valid. Only the SESSION branch requires raw MFD availability; STINT and END ignore MFD availability. SESSION consumes raw MFD `PitSvFuel` selected request gated by `dpFuelFill`; it does not consume `Fuel.Refuel.NextLitresCeil`, `Fuel.Pit.WillAdd`, plugin recommendations, planner fuel values, or tank-space-clamped add amounts. | 500 ms poll. | `LalaLaunch.cs` — `TryReadRawMfdSelectedFuelRequestLitres` + `UpdateFuelBurnTargetSelector` + `AttachCore`. |
| Surface.TrackWetness | int | Raw iRacing track wetness (0–3/4 depending on telemetry); informational only. | 500 ms poll. | `LalaLaunch.cs` — `ReadTrackWetness` + `AttachCore`【F:LalaLaunch.cs†L1402-L1426】【F:LalaLaunch.cs†L6095-L6134】【F:LalaLaunch.cs†L2996-L3003】 |
| Surface.TrackWetnessLabel | string | Human label for track wetness (“Dry/Damp/Light Wet/Mod Wet/Very Wet/Unknown”). | 500 ms poll. | `LalaLaunch.cs` — `MapWetnessLabel` + `AttachCore`【F:LalaLaunch.cs†L1402-L1426】【F:LalaLaunch.cs†L6115-L6134】【F:LalaLaunch.cs†L2996-L3003】 |
| Fuel.FuelReadyConfidenceThreshold | double | Confidence threshold gating fuel readiness and pit window. | 500 ms poll. | `LalaLaunch.cs` — `GetFuelReadyConfidenceThreshold` + `AttachCore`【F:LalaLaunch.cs†L2672-L2955】 |
| Fuel.LiveLapsRemainingInRace / _S | double | Projected laps remaining using stable burn & lap time with EMA-smoothed string variant. | 500 ms poll. | `LalaLaunch.cs` — `ComputeProjectedLapsRemaining`, `UpdateSmoothedFuelOutputs`, `AttachCore`【F:LalaLaunch.cs†L1895-L2143】【F:LalaLaunch.cs†L4243-L4306】【F:LalaLaunch.cs†L2676-L2690】 |
| Fuel.LiveLapsRemainingInRace_Stable / _Stable_S | double | Explicit stable mirror of the projection (for dash/debug). | 500 ms poll. | `LalaLaunch.cs` — `UpdateSmoothedFuelOutputs` + `AttachCore`【F:LalaLaunch.cs†L4243-L4306】【F:LalaLaunch.cs†L2676-L2690】 |
| Fuel.DeltaLaps | double | Lap surplus/deficit vs. projected distance at stable burn. | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` + `AttachCore`【F:LalaLaunch.cs†L1895-L2143】【F:LalaLaunch.cs†L2676-L2690】 |
| Fuel.TargetFuelPerLap | double | Required burn to finish when short (floored to 10% saving). | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` + `AttachCore`【F:LalaLaunch.cs†L1995-L2054】【F:LalaLaunch.cs†L2681-L2684】 |
| Fuel.LapsRemainingInTank | double | Tank fuel ÷ stable (or live) burn. | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` + `AttachCore`【F:LalaLaunch.cs†L1895-L1967】【F:LalaLaunch.cs†L2687-L2690】 |
| Fuel.Confidence | int | Fuel-model confidence from accepted window size/quality. | 500 ms poll. | `LalaLaunch.cs` — `ComputeFuelModelConfidence` + `AttachCore`【F:LalaLaunch.cs†L1830-L1890】【F:LalaLaunch.cs†L2688-L2691】 |
| Fuel.PushFuelPerLap / Fuel.FuelSavePerLap | double | Push = max session burn or +2%; Save = min window burn or 97% fallback. | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` + `AttachCore`【F:LalaLaunch.cs†L2005-L2143】【F:LalaLaunch.cs†L2690-L2694】 |
| Fuel.StintBurnTarget / Fuel.StintBurnTargetBand | double/string | Current-tank per-lap burn target plus band label (SAVE/PUSH/HOLD/OKAY) based on pit-in reserve percentage. | 500 ms poll. | `LalaLaunch.cs` — stint target block + `AttachCore`【F:LalaLaunch.cs†L2371-L2449】【F:LalaLaunch.cs†L3017-L3021】 |
| Fuel.FuelBurnPredictor / Fuel.FuelBurnPredictorSource | double/string | Predictor burn: stable burn until 5 valid laps, then rolling 3-lap average. Source labels: SIMHUB/PLUGIN/STINT/AVG3. | 500 ms poll. | `LalaLaunch.cs` — predictor outputs + `AttachCore`【F:LalaLaunch.cs†L5364-L5427】【F:LalaLaunch.cs†L3030-L3033】 |
| Fuel.DeltaLapsIfPush / Fuel.CanAffordToPush | double/bool | Surplus/deficit if driving at push burn and affordability flag. | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` + `AttachCore`【F:LalaLaunch.cs†L2005-L2143】【F:LalaLaunch.cs†L2690-L2694】 |
| Fuel.RequiredBurnToEnd / Valid / State / StateText / Source | double/bool/int/string/string | Driver tactical burn-to-end guidance. `RequiredBurnToEnd` is the clamped display value (`clamp(raw, FuelSavePerLap, PushFuelPerLap)`), while `State/StateText` use raw unbounded burn-to-end (`0=CRITICAL`, `1=SAVE`, `2=HOLD`, `3=PUSH`). `Valid` is numeric-basis only (`projected laps > 0`, `stable/save/push burns > 0`, usable current fuel). `Source` reflects burn/projection authority (`live`/`profile`/`fallback`/`invalid`). | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` BurnToEnd block + `AttachCore`. |
| Fuel.Contingency.Litres / Fuel.Contingency.Laps / Fuel.Contingency.Source | double/double/string | Active contingency reserve resolved by Option-A authority: planner first (`FuelCalcs.ContingencyValue/IsContingencyInLaps`), profile track fallback (`TrackStats.FuelContingencyValue/IsContingencyInLaps`), then default `1.5` laps. `Source` values: `planner`, `profile`, `default`, `none`, `fallback`. Zero contingency is valid. | 500 ms poll. | `LalaLaunch.cs` — contingency resolver + `AttachCore`; `FuelCalcs.cs` / `TrackStats` as upstream data sources. |
| Fuel.Refuel.NextLitres | double | Runtime next-stop actionable refuel guidance (litres). Uses Pit Fuel Control DATA/SOURCE authority as read-only selectors and computes with selected burn + DATA-governed projected remaining laps + current fuel + active contingency under final-stop vs multi-stop rule: `BaseToFinish=(laps*burn)-currentFuel`; `FinalStopNeed=BaseToFinish+contingency`; decision capacity uses runtime effective restricted tank-cap seam (`ResolveRuntimeLiveMaxTankCapacity`, live cap first with planner/profile fallback only when live cap is unavailable). If `FinalStopNeed <= decisionCap`, publish `max(0, FinalStopNeed)` (final stop). Projection basis is LIVE-first only when selected lap source is LIVE with valid live projection; otherwise remaining laps are reprojected from the selected lap-seconds basis so `Fuel.Refuel.LapSource` matches `NextLitres` math. When contingency is configured in laps, contingency litres are converted on the selected Fuel.Refuel burn basis used for `NextLitres` (NORM/PUSH/SAVE or STBY advisory NORM). If `FinalStopNeed > decisionCap`, publish conservative multi-stop max-fill add guidance from current runtime add-cap seam (`Fuel.Pit.TankSpaceAvailable`) instead of full tank size as add amount. STBY computes advisory NORM basis and remains command-neutral. | 500 ms poll. | `LalaLaunch.cs` — `ResolveRuntimeRefuelBasis` + `EvaluateRuntimeRefuelNeed` + `ComputeRuntimeRefuelOutputs` + `AttachCore`. |
| Fuel.Refuel.NextLitresCeil | double | Integer-safe pit-MFD guidance from `Fuel.Refuel.NextLitres`: `ceil(max(0, NextLitres))`. Publishes `0` when invalid. | 500 ms poll. | `LalaLaunch.cs` — `ComputeRuntimeRefuelOutputs` + `AttachCore`. |
| Fuel.Refuel.NextText | string | Short runtime refuel text: `CHECK FUEL` when invalid; `NO REFUEL` when valid and ceil is `0`; `REFUEL {N}L` when valid and ceil `>0`. STBY does not force `N/A`; normal text rules apply. | 500 ms poll. | `LalaLaunch.cs` — `ComputeRuntimeRefuelOutputs` + `AttachCore`. |
| Fuel.Refuel.Valid | bool | Runtime refuel math/context validity gate. False when projection/burn/current fuel/contingency/capacity context or DATA/SOURCE context is unusable. Capacity validity requires a resolvable effective decision-cap seam; zero-capacity with positive requirement is invalid (does not silently publish valid `NO REFUEL`). SOURCE `STBY` alone does not invalidate outputs. | 500 ms poll. | `LalaLaunch.cs` — `ResolveRuntimeRefuelBasis` + `EvaluateRuntimeRefuelNeed` + `ComputeRuntimeRefuelOutputs` + `AttachCore`. |
| Fuel.Refuel.BurnSource / Fuel.Refuel.LapSource | string/string | Provenance tokens for selected burn and projection basis. Burn emits one of `LIVE/PLAN/PROFILE/SIM/DEFAULT`; Lap emits one of `LIVE/PLAN/PROFILE/SIM/DEFAULT`. `SIM` for burn is emitted only when selected fallback burn is genuinely sourced from `DataCorePlugin.Computed.Fuel_LitersPerLap`; synthetic/plugin default fallback burn is emitted as `DEFAULT`. `Fuel.Refuel.DataMode` carries selected DATA context (`LIVE`/`SAVED`) separately. DATA SAVED paths do not select `LIVE/SIM`. When PUSH/SAVE fallback is derived from selected NORM basis, BurnSource mirrors true derivation (`PLAN`/`PROFILE`/`LIVE`) rather than forcing `DEFAULT`; `DEFAULT` is reserved for generic fallback derivation. | 500 ms poll. | `LalaLaunch.cs` — `ResolveRuntimeRefuelBasis` + `ResolveDataGovernedBurnAndPaceBasis` + `AttachCore`. |

| Fuel.Refuel.SelectedBurnPerLap | double | Selected runtime burn basis used by `Fuel.Refuel.*` math after DATA/SOURCE resolution (`NORM/PUSH/SAVE/STBY advisory NORM`). Dash/debug seam only. | 500 ms poll. | `LalaLaunch.cs` — `ResolveRuntimeRefuelBasis` + `ComputeRuntimeRefuelOutputs` + `AttachCore`. |
| Fuel.Live.RemainingStints | double | Runtime remaining fuel requirement as stints/tanks remaining on selected runtime refuel burn/projection basis, normalized by runtime effective max tank authority; safe `0` when invalid. | 500 ms poll. | `LalaLaunch.cs` — `ComputeRuntimeRefuelOutputs` + `ResolveRuntimeLiveMaxTankCapacity` + `AttachCore`. |
| Fuel.MaxTank | double | Runtime effective max tank authority seam used by runtime fuel-cap decisions. Live authority precedence: early DriverInfo restricted-cap (`DriverCarFuelMaxLtr * DriverCarMaxFuelPct`, pct normalized for `0..1` or `0..100`) -> GameData restricted-cap (`MaxFuel`/`CarSettings_MaxFUEL` with pct) -> fresh last-valid live fallback -> planner/profile fallback when live cap unavailable. | 500 ms poll. | `LalaLaunch.cs` — `ComputeLiveMaxFuelFromSimhub` + `ResolveRuntimeLiveMaxTankCapacity` + `UpdateLiveFuelCalcs` + `AttachCore`. |
| Fuel.Refuel.DataMode / Fuel.Refuel.BurnMode | string/string | DATA mode mirror (`LIVE`/`SAVED`) and SOURCE intent mirror (`NORM`/`PUSH`/`SAVE`/`STBY`) for runtime guidance context. BurnMode `STBY` computes advisory NORM basis with no command-send implication. | 500 ms poll. | `LalaLaunch.cs` — Pit Fuel Control state read + `ComputeRuntimeRefuelOutputs` + `AttachCore`. |
| Fuel.Delta.LitresCurrent / Plan / WillAdd | double | Tactical liter delta to finish with active contingency reserve protected on the **required-to-finish side** only. `Current` uses runtime stable/live normal basis. `Plan` remains after planned add (`current+requested add`) and now follows Pit Fuel Control DATA for its burn/lap basis (`LIVE`=live/stable normal, `SAVED`=planner/profile normal, no DATA SAVED use of LIVE/SIM authority). `WillAdd` remains current+clamped `Fuel.Pit.WillAdd` on runtime stable/live basis. When contingency is configured in laps, reserve litres are resolved on the matching burn basis for each path. | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` delta block + `AttachCore`. |
| Fuel.Delta.LitresCurrentPush / PlanPush / WillAddPush | double | Same contingency-protected tactical deltas assuming push burn. Reserve is added only to required-to-finish, never to request/clamp amounts. When contingency is configured in laps, reserve litres are resolved on `Fuel.PushFuelPerLap` for push deltas (not stable-burn litres). | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` delta block + `AttachCore`. |
| Fuel.Delta.LitresCurrentSave / PlanSave / WillAddSave | double | Same contingency-protected tactical deltas assuming fuel-save burn. Reserve is added only to required-to-finish, never to request/clamp amounts. When contingency is configured in laps, reserve litres are resolved on `Fuel.FuelSavePerLap` for save deltas (not stable-burn litres). | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` delta block + `AttachCore`. |
| Fuel.Delta.AfterStop.Selected | double | Selected after-stop fuel delta for pit-plan dash display. Selection uses Pit Fuel Control SOURCE (`PUSH=>LitresPlanPush`, `SAVE=>LitresPlanSave`, `NORM/STBY=>LitresPlan`) while underlying `LitresPlan*` basis follows Pit Fuel Control DATA (`LIVE`=live/stable basis, `SAVED`=planner/profile basis). STBY is advisory NORM. | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` delta block + `AttachCore`. |
| Fuel.Setup.FuelLevel / Fuel.Setup.FuelLevelValid / Fuel.Setup.FuelLevelSource | double/bool/string | Read-only setup-session fallback fuel export for pre-grid/pre-race use when live telemetry fuel level is unavailable/zero. Value resolves first usable source in priority order: `CarSetup.BrakesDriveUnit.Fuel.FuelLevel`, `CarSetup.Chassis.Front.FuelLevel`, `CarSetup.Chassis.Rear.FuelLevel`, `CarSetup.Suspension.Rear.FuelLevel`. Accepts numeric raw values as litres and accepts string values only when explicitly litre-labelled (`77.0 L`, `77 L`, `77,0 L`, `litre/litres/liter/liters`). Rejects known non-litre units (`gal`/`gallon`/`gallons`), unknown unit strings, bare numeric strings, null/blank/NaN/infinity/`<=0`. `FuelLevelSource` tokens: `brakesdriveunit`, `chassis.front`, `chassis.rear`, `suspension.rear`, `none`. | Per tick. | `LalaLaunch.cs` — `TryResolveSetupFuelLevelLitres` + `AttachCore`. |
| Fuel.Pit.TotalNeededToEnd / _S | double | Liters required to finish at stable burn (smoothed `_S` via EMA). During SessionState `2/3` timed-race lookahead uses `CurrentSessionInfo._SessionTime` authority; SessionState `4` uses normal live running projection. | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs`, `UpdateSmoothedFuelOutputs`, `AttachCore`【F:LalaLaunch.cs†L2379-L3511】【F:LalaLaunch.cs†L13812-L13867】【F:LalaLaunch.cs†L4783-L4784】 |
| Fuel.Pit.NeedToAdd | double | Shortfall vs. required liters (non-clamped runtime requirement seam; same SessionState `2/3` vs `4` projection authority split as above). | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` + `AttachCore`【F:LalaLaunch.cs†L3317-L3325】【F:LalaLaunch.cs†L4785-L4785】 |
| Fuel.Pit.TankSpaceAvailable | double | Remaining live pit capacity. Uses live session tank-cap authority seam first (`EffectiveLiveMaxTank` raw/fresh fallback path), then falls back to planner/profile cap only when live cap is unavailable. | 500 ms poll. | `LalaLaunch.cs` — `ResolveRuntimeLiveMaxTankCapacity`, `UpdateLiveFuelCalcs`, `AttachCore`【F:LalaLaunch.cs†L1895-L2143】【F:LalaLaunch.cs†L2705-L2708】 |
| Fuel.Pit.WillAdd | double | Requested add clamped to tank space. This can reduce near end-of-stop as `currentFuel` rises and tank-space clamp tightens (`min(requestedAdd, maxTank-currentFuel)`). Runtime math semantics remain unchanged. | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` + `AttachCore`【F:LalaLaunch.cs†L1895-L2143】【F:LalaLaunch.cs†L2706-L2708】 |
| Fuel.Pit.Box.EntryFuel | double | Latched fuel-at-entry for boxed refuel phase. While `Pit.Box.Active` and refuel remains selected, it latches once on first detected refuel-flow signal (`fuel rise` or existing refuel-learning `_isRefuelling` seam), then stays fixed for the rest of that boxed phase. Clears to `0` immediately if refuel is deselected in-box, and resets to `0` when boxed phase ends. | Per tick. | `LalaLaunch.cs` — `UpdatePitRefuelGaugeValues` + `AttachCore`. |
| Fuel.Pit.Box.WillAddLatched | double | Stable boxed UI target add amount. While `Pit.Box.Active`, it latches immediately from current `Fuel.Pit.WillAdd` when refuel is selected (before flow latch), then stays fixed for that boxed selected-refuel phase. Clears to `0` immediately if refuel is deselected in-box, and resets to `0` when boxed phase ends. | Per tick. | `LalaLaunch.cs` — `UpdatePitRefuelGaugeValues` + `AttachCore`. |
| Fuel.Pit.AddedSoFar | double | In-stop liters already added (`max(0, Fuel - Fuel.Pit.Box.EntryFuel)`) once boxed refuel phase is latched; otherwise `0`. | Per tick. | `LalaLaunch.cs` — `UpdatePitRefuelGaugeValues` + `AttachCore`. |
| Fuel.Pit.WillAddRemaining | double | UI-facing boxed countdown: `max(0, Fuel.Pit.Box.WillAddLatched - Fuel.Pit.AddedSoFar)`. Before flow latch, this equals the full early-latched target (with `AddedSoFar=0`). Clears to `0` immediately when refuel is deselected in-box and resets to `0` when boxed phase ends. | Per tick. | `LalaLaunch.cs` — `UpdatePitRefuelGaugeValues` + `AttachCore`. |
| Fuel.Pit.FuelOnExit | double | Current fuel + clamped add. | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` + `AttachCore`【F:LalaLaunch.cs†L1895-L2143】【F:LalaLaunch.cs†L2712-L2716】 |
| Fuel.Pit.DeltaAfterStop / _S | double | Lap surplus after the planned stop at stable burn (`_S` smoothed). | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs`, `UpdateSmoothedFuelOutputs`, `AttachCore`【F:LalaLaunch.cs†L1895-L2143】【F:LalaLaunch.cs†L2711-L2714】【F:LalaLaunch.cs†L4243-L4306】 |
| Fuel.Pit.FuelSaveDeltaAfterStop / _S | double | Surplus after stop using fuel-save burn (`_S` smoothed). | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs`, `UpdateSmoothedFuelOutputs`, `AttachCore`【F:LalaLaunch.cs†L1895-L2143】【F:LalaLaunch.cs†L2710-L2712】【F:LalaLaunch.cs†L4243-L4306】 |
| Fuel.Pit.PushDeltaAfterStop / _S | double | Surplus after stop assuming push burn (`_S` smoothed). | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs`, `UpdateSmoothedFuelOutputs`, `AttachCore`【F:LalaLaunch.cs†L1895-L2143】【F:LalaLaunch.cs†L2711-L2713】【F:LalaLaunch.cs†L4243-L4306】 |
| Fuel.PitStopsRequiredByFuel / Fuel.PitStopsRequiredByPlan / Fuel.Pit.StopsRequiredToEnd | int | Capacity-based stop count, strategy-required stops, and published stop requirement (plan-first). | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` + `AttachCore`【F:LalaLaunch.cs†L1895-L2143】【F:LalaLaunch.cs†L2714-L2718】 |
| Fuel.PitStopsRequiredByFuelExact | double | Non-ceiled exact companion to `Fuel.PitStopsRequiredByFuel` using the same litres-short / effective-capacity basis, published at 1dp and safe `0` when invalid. | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` + `AttachCore`. |
| Fuel.Live.RefuelRate_Lps | double | Effective runtime refuel rate used by strategy timing for the active profile. Locked profiles with a usable stored value keep publishing that stored value (learned overwrite candidates are blocked). Locked profiles with no usable stored rate allow a one-time first-fill from the first valid learned candidate. | 500 ms poll. | `LalaLaunch.cs` — refuel learning in `UpdateLiveFuelCalcs` + lock-gated save/runtime apply path + `AttachCore`. |
| Fuel.Live.TireChangeCount | int | Runtime raw selected tyre count (`0..4`) from DP tyre flags for observability/debug. During active boxed service, pit target modeling latches pre-service selected count separately; this export may decrease as per-wheel flags clear. When tyre flags are unavailable (including partial availability), conservative fail-open fallback publishes `4` (assume full tyre service). | 500 ms poll. | `LalaLaunch.cs` — `ResolveLiveSelectedTireChangeCount` + `AttachCore`. |
| Fuel.Live.TireChangeTime_S | double | Runtime selected-tyre service estimate used by live pit prediction. Full-4 base comes from active profile `TireChangeTime` (sanitized non-negative; independent of Strategy slider what-if value). Selected-count scaling: `count<=0 => 0`; `count>=4 => full4`; else `shared=1.0`, `variable=max(0, full4-shared)`, `estimate=shared + variable*(count/4)`. | 500 ms poll. | `LalaLaunch.cs` — `GetEffectiveTireChangeTimeSeconds` + `AttachCore`. |
| Fuel.Live.PitLaneLoss_S | double | Pit-lane-loss baseline from profile/learned value used by strategy. Canonical meaning is drive-through baseline lane travel (no box stop). | 500 ms poll. | `LalaLaunch.cs` — `_pit.LastTotalPitCycleTimeLoss` persistence + `AttachCore`【F:LalaLaunch.cs†L1408-L1462】【F:LalaLaunch.cs†L2717-L2721】 |
| TrackLearning.PitLoss.Display / ValueSec / SourceText / Locked | string / double / string / bool | Offline Testing Track Learning pit-loss review seams from active profile+track: formatted display (`"23.0s"` or `"-"`), stored seconds, source text (`DTL`/`DIRECT`/`MANUAL`/`-`), and lock state. | Per tick. | `LalaLaunch.cs` — active track stats resolver + `AttachCore`. |
| TrackLearning.Markers.EntryPct / ExitPct / Locked | double / double / bool | Offline Testing Track Learning marker review seams using stored marker values and current marker lock state. | Per tick. | `LalaLaunch.cs` — marker store seam + `AttachCore`. |
| TrackLearning.Condition.IsWet / ModeText / AvgLapDisplay / AvgLapSamples / FuelSave / FuelAvg / FuelMax / FuelSamples / Locked | bool / string / string / int / double / double / double / int / bool | Active-condition (dry/wet) Track Learning review seams. Condition lock maps to active condition (`DryConditionsLocked` or `WetConditionsLocked`) and is shared by avg-lap + fuel persistence ownership. | Per tick. | `LalaLaunch.cs` — `_isWetMode` routing + active `TrackStats` seam + `AttachCore`. |
| Fuel.Live.TotalStopLoss | double | Boxed-stop composite for strategy/pit-exit prediction seams: `pit lane loss baseline + boxed service + fixed transition allowance`. Boxed service uses modeled boxed target (`max(fuelTime, tireTime) + 1.0s` fixed boxed-service overhead), repair-aware via `max(modeledBoxTarget, repairRemainingSec)`, then adds fixed pit-box transition allowance `+2.00s` for slow-in/settle/launch-out overhead. | 500 ms poll. | `LalaLaunch.cs` — `CalculateTotalStopLossSeconds` + `AttachCore`【F:LalaLaunch.cs†L1911-L1950】【F:LalaLaunch.cs†L2719-L2722】 |
| Pit.Box.Active | bool | Driver box-service countdown active flag. `true` only while in valid in-box service state (`in pit lane && in pit stall && PitPhase.InBox`). | Per tick. | `LalaLaunch.cs` — `UpdatePitBoxCountdownValues` + `AttachCore`. |
| Pit.Box.ElapsedSec | double | In-box elapsed seconds for countdown. Reuses existing `PitEngine.PitStopElapsedSec` timer (no separate timer). | Per tick while active; else `0`. | `LalaLaunch.cs` — `UpdatePitBoxCountdownValues` + `AttachCore`. |
| Pit.Box.TargetSec | double | Box-service target seconds used by the countdown. During the first short settle period of a boxed stop (1.0s elapsed), live effective target is `max(modeledTargetSec, repairRemainingSec)` where `modeledTargetSec = (max(fuelTime, tireTime) + 1.0s fixed boxed-service overhead)` and `repairRemainingSec` is boxed native repair-left authority. The fixed `+1.0s` is stationary box-service allowance only (not pit-lane travel). Once settled, that effective target is latched/frozen for the stop and no longer follows late drift. | Per tick while active; else `0`. | `LalaLaunch.cs` — `CalculatePitBoxModeledTargetSeconds` + `CalculatePitBoxRepairRemainingSeconds` + `UpdatePitBoxCountdownValues` + `AttachCore`. |
| Pit.Box.RemainingSec | double | Countdown seconds remaining in box service, computed each tick as `max(max(0, Pit.Box.TargetSec - Pit.Box.ElapsedSec), repairRemainingSec)` where `repairRemainingSec` is the boxed native repair-left authority (`PitRepairLeft`, plus `PitOptRepairLeft` when optional repairs are enabled). This keeps remaining repair-aware while `Pit.Box.TargetSec` stays frozen once latched. | Per tick while active; else `0`. | `LalaLaunch.cs` — `CalculatePitBoxRepairRemainingSeconds` + `UpdatePitBoxCountdownValues` + `AttachCore`. |
| Pit.Box.LastDeltaSec | double | Post-stop delta published on box release as `Pit.Box.TargetSec(latched effective target) - final Pit.Box.ElapsedSec`; positive means quicker than target, negative means slower. Because the latched target now includes repair authority seen during settle, repair stops get meaningful post-stop delta. Value persists after stop for review, clears when the next boxed stop becomes active, and clears on session/reset paths. | Per tick (latched between boxed stops; otherwise `0`). | `LalaLaunch.cs` — `UpdatePitBoxCountdownValues` + `AttachCore`. |
| Fuel.Live.DriveTimeAfterZero | double | Extra drive seconds after timer zero (planner/live observed). | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` after-zero model + `AttachCore`【F:LalaLaunch.cs†L1911-L1993】【F:LalaLaunch.cs†L2721-L2724】 |
| Fuel.After0.PlannerSeconds / Fuel.After0.LiveEstimateSeconds / Fuel.After0.Source | double/double/string | Planner-configured after-zero allowance, live observed estimate, and source label (“planner”/“live”). | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` + `AttachCore`【F:LalaLaunch.cs†L1911-L1993】【F:LalaLaunch.cs†L2722-L2726】 |
| LalaLaunch.PreRace.Selected / LalaLaunch.PreRace.SelectedText | int/string | Driver-selected pre-race info mode (0 No Stop, 1 Single Stop, 2 Multi Stop, 3 Auto) and display label. Selection is persisted via a dedicated PreRace-only mode field (`SelectedPreRaceMode` / persisted `PreRaceMode`) and only affects the PreRace outputs (not planner/live fuel engines). | 500 ms poll. | `LalaLaunch.cs` — pre-race block in `UpdateLiveFuelCalcs` + unqualified `PreRace.*` `AttachCore` registration. |
| LalaLaunch.PreRace.Stints | double | Pre-race stints threshold signal computed as `total fuel needed / usable tank`, rounded 1dp. This is total stints needed (not current-fuel shortfall) and replaces separate stop outputs for dash use. | 500 ms poll. | `LalaLaunch.cs` — pre-race block in `UpdateLiveFuelCalcs` + unqualified `PreRace.*` `AttachCore` registration. |
| LalaLaunch.PreRace.TotalFuelNeeded / LalaLaunch.PreRace.FuelDelta | double/double | Unified pre-race fuel requirement and delta. `Auto` uses live race-definition authority first (`CurrentSessionInfo._SessionTime` timed, `CurrentSessionInfo._SessionLaps` lap-limited) plus the PreRace after-zero resolver (runtime after-zero when available, else planner after-zero, else `0`); planner race sliders are not Auto race-length authority. PreRace current-fuel basis is `live current fuel` when valid/positive. Setup fallback (`Fuel.Setup.FuelLevel` when `Fuel.Setup.FuelLevelValid=true`) is used only during pre-race phases (grid/formation, SessionState `<4`) when live current fuel is non-positive; during active race-running (SessionState `==4`), setup fallback is not used and live current fuel remains authoritative. `FuelDelta` is strategy-aware: one-stop path uses `(effective current fuel + requested add) - total fuel needed`, while no-stop/multi-stop paths use `effective current fuel - total fuel needed`. `TotalFuelNeeded` uses the matching Strategy planner total when planner total, car, canonical track key, race basis, race length, and manual wet/dry gates pass: `max(0, FuelCalculator.TotalFuelNeeded - LalaLaunch.PreRace.FormationFuelPlanned + LalaLaunch.PreRace.FormationFuelRemaining)`. PreRace authority uses the same resolved Live Detect race-definition seam as Strategy (`IsLimitedSessionLaps` / `IsLimitedTime`, compatibility fields, then `SessionsXX` fallback), requires a strict `0.001`-lap or `0.01`-minute tolerance, and rejects manual Dry/Wet authority when live condition is unknown or conflicting. If any authority gate fails, it retains the live/session fallback `base race fuel requirement + active contingency litres + LalaLaunch.PreRace.FormationFuelRemaining`. Formation is included before/during formation and drops to `0` once race-running starts; runtime fuel families remain formation-excluded. | 500 ms poll. | `LalaLaunch.cs` — pre-race block in `UpdateLiveFuelCalcs` + unqualified `PreRace.*` `AttachCore` registration. |
| LalaLaunch.PreRace.FormationFuelPlanned | double | Planner-entered/static formation lap fuel allowance from `FuelCalculator.FormationLapFuelLiters`, clamped to non-negative litres; invalid, negative, or unavailable planner values publish `0`. This is a PreRace/export contract only and does not alter Strategy planner storage or runtime pit/refuel math. | 500 ms poll. | `LalaLaunch.cs` — `ResolvePreRaceFormationFuelPlannedLitres`, `UpdatePreRaceOutputs`, and unqualified `PreRace.*` `AttachCore` registration. |
| LalaLaunch.PreRace.FormationFuelRemaining | double | Dynamic remaining formation allowance, clamped `0..FormationFuelPlanned`. Publishes planned fuel before formation (`SessionState 1/2`), burns down during formation (`SessionState 3`) from a live-fuel baseline captured on formation entry when valid live fuel exists, holds the last/planned value if live fuel is temporarily unavailable during formation, and publishes `0` when SessionState is invalid/unknown or race-running/post-race (`SessionState >=4`). `PreRace.TotalFuelNeeded` consumes this value; `Fuel.Pit.TotalNeededToEnd` does not. | 500 ms poll. | `LalaLaunch.cs` — `ResolvePreRaceFormationFuelRemainingLitres`, `UpdatePreRaceOutputs`, and unqualified `PreRace.*` `AttachCore` registration. |
| LalaLaunch.PreRace.FuelSource / LalaLaunch.PreRace.LapTimeSource | string/string | DATA-governed pre-race provenance labels. Accepted tokens are uppercase `LIVE`/`SAVED`/`PLAN`/`PROFILE`/`SIM`/`DEFAULT` (burn and lap may emit `SIM` in DATA LIVE fallback). DATA `LIVE` emits `LIVE` only when the selected stable basis is truly live runtime/plugin accepted; otherwise it falls through hierarchy (`BURN: LIVE -> PROFILE -> SIM -> DEFAULT`, `LAP: LIVE -> PROFILE -> SIM -> PLAN -> DEFAULT`). In DATA LIVE, burn `SIM` means genuine DataCore computed fallback only; synthetic/plugin-held fallback remains `DEFAULT`. DATA `SAVED` never emits `LIVE` or `SIM` (`PLAN -> PROFILE -> DEFAULT` only). | 500 ms poll. | `LalaLaunch.cs` — `ResolveDataGovernedBurnAndPaceBasis(...)` + pre-race export wiring + unqualified `PreRace.*` `AttachCore` registration. |
| LalaLaunch.PreRace.LiveFacingBasisText | string | Concise combined source readout for currently published pre-race authority: `DATA <LIVE|SAVED> | LAP <LIVE|PROFILE|SIM|PLAN|DEFAULT> / BURN <LIVE|PROFILE|SIM|DEFAULT>`. Mirrors `PreRace.LapTimeSource`/`PreRace.FuelSource` exactly. | 500 ms poll. | `LalaLaunch.cs` — pre-race output composition + unqualified `PreRace.*` `AttachCore` registration. |
| LalaLaunch.PreRace.StatusText / LalaLaunch.PreRace.StatusColour | string/string | Dash-facing pre-race decision output and colour (`green`, `orange`, `red`) evaluated scenario-first (`required strategy` from stints, then `selected strategy`). Status set includes: `STRATEGY MISMATCH`, `ADD START FUEL FOR SINGLE STINT`, `SINGLE STINT OKAY`, `SINGLE STINT POSSIBLE`, `SINGLE STINT NOT POSSIBLE`, `2 STINT PLAN NOT POSSIBLE`, `2 STINT PLAN REQUIRES MORE FUEL`, `OVERFUELLED`, `CHECK NEXT STINT FUEL`, `SINGLE STOP OKAY`, `SINGLE STOP POSSIBLE`, `MAX FUEL SET FOR MULTI STOP`, `MAX START FUEL REQUIRED`, `SET MAX FUEL NEXT STINT`, `MULTI STINTS REQUIRED`. `STRATEGY MISMATCH` remains non-Auto-only and only when planner/live inputs are comparable and mismatched (`HasComparableInputs && !IsMatch`). `2 STINT PLAN REQUIRES MORE FUEL` and `CHECK NEXT STINT FUEL` are phase-routed next-stint advisories: suppressed from main status in `PRE GRID`/`GRIDDING` (publish `SINGLE STOP POSSIBLE`), and allowed in `START READY`/`RACE`. `SET MAX FUEL NEXT STINT` is a phase-routed variant of max-start-fuel guidance in `START READY`/`RACE`. | 500 ms poll. | `LalaLaunch.cs` — pre-race status decision seam + unqualified `PreRace.*` `AttachCore` registration. |

| StrategyDash.Phase / StrategyDash.PhaseText | int/string | StrategyDash phase seam (`0=IDLE`, `1=PRE GRID`, `2=GRIDDING`, `3=START READY`, `5=RACE`). `PRE GRID` is used when not race-running and not in-car; `GRIDDING` is used when in-car and grid/formation authority is true; `START READY` is used when in-car pre-race but not grid/formation; `RACE` is race-running authority. | 500 ms poll. | `LalaLaunch.cs` — pre-race strategy-dash adapter + `AttachCore`. |
| StrategyDash.RequiredStopsPreGreen / StrategyDash.StrategyText | int/string | Required pre-green strategy class derived from existing pre-race stints classification (`0=no stop`, `1=one stop`, `2=multi stop`) and text (`NO STOP`/`ONE STOP`/`MULTI STOP`). | 500 ms poll. | `LalaLaunch.cs` — pre-race strategy-dash adapter + `AttachCore`. |
| StrategyDash.StartFuelRequiredLitres / StartFuelAdviceText / StartFuelStatus | double/string/int | Start-fuel advice seam for pre-green dashboards. `StartFuelRequiredLitres` uses the corrected `LalaLaunch.PreRace.TotalFuelNeeded` basis (matching Strategy planner total with dynamic formation replacement, else live/session fallback) and is capped for display by effective max tank when available. Advice/status are evaluated from a dedicated start-fuel comparison (`current fuel` when >0, else setup fallback when `Fuel.Setup.FuelLevelValid` **and pre-race/grid/formation fallback is allowed** (`SessionState < 4`), else unknown) against required using a `1.0 L` OK tolerance (`0=OK`, `1=CHECK`, `2=ACTION`); text is `START FUEL OK`/`START FUEL MAX`/`CHECK START FUEL`/`ADD START FUEL` and does not map from `LalaLaunch.PreRace.StatusText`. | 500 ms poll. | `LalaLaunch.cs` — pre-race strategy-dash adapter + `AttachCore`. |
| StrategyDash.NextRefuelTargetLitres / NextRefuelDeltaLitres / NextRefuelAdviceText / NextRefuelStatus | double/double/string/int | Pre-green next-stop advice seam. No-stop => target `0` + advice `N/A`; one-stop => second-stint target (`SET X L`); multi-stop => max-cap target (`REFUEL MAX`). `NextRefuelDeltaLitres` is `requested refuel - target` (negative means add more, positive means above target) and always uses the same burn/source/DATA basis as `NextRefuelTargetLitres`. `NORM` uses runtime/live burn; `PUSH`/`SAVE` use Pit Fuel Control DATA (`LIVE` live push/save, `SAVED` planner/profile memory push/save with fallback). Status is derived from `abs(delta)` (`<=0.5 OK`, `<=2.0 CHECK`, otherwise `ACTION`) and does not duplicate Pit Fuel Control MODE command ownership. | 500 ms poll. | `LalaLaunch.cs` — pre-race strategy-dash adapter + `AttachCore`. |
| StrategyDash.BurnPlanText | string | Pre-green display helper for main dash row, especially when `RequiredStopsPreGreen==0` and next-refuel advice is `N/A`. Publishes concise burn-plan text from effective StrategyDash burn basis (`BURN PLAN: NORM/SAVE/PUSH`, with optional `/ LIVE` or `/ MEMORY` suffix when basis source is clear; fallback basis omits suffix). | 500 ms poll. | `LalaLaunch.cs` — pre-race strategy-dash adapter + `AttachCore`. |
| StrategyDash.ContingencyText | string | Human-readable active contingency context (`CONT x.x LAPS` / `CONT x.x L`) from active contingency seam for pre-green display/debug. | 500 ms poll. | `LalaLaunch.cs` — pre-race strategy-dash adapter + `AttachCore`. |
| StrategyDash.AdvancedMode / ModeText | bool/string | Persisted Strategy Dash presentation mode state toggled by `StrategyDash.ModeToggle`; `AdvancedMode=true` means Advanced presentation, `false` means Simple presentation. `ModeText` publishes `ADVANCED`/`SIMPLE` for labels. Presentation state only; no strategy, fuel, or PreRace math changes. | Per tick. | `LaunchPluginSettings.StrategyDashAdvancedMode` + `LalaLaunch.cs` action handler/`AttachCore`. |
| Fuel.Live.ProjectedDriveSecondsRemaining | double | Projected wall-time remaining (includes after-zero). | 500 ms poll. | `LalaLaunch.cs` — `ComputeProjectedLapsRemaining` + `AttachCore`【F:LalaLaunch.cs†L4396-L4404】【F:LalaLaunch.cs†L2726-L2729】 |
| Fuel.ProjectionLapTime_Stable / _StableSource | double/string | Selected projection lap time and provenance. Raw `_StableSource` tokens currently used by runtime: `pace.stint`, `pace.last5`, `profile.avg`, `fuelcalc.estimated`, `telemetry.lastlap`, `fallback.none` (plus held carry-over of the previous token when deadband/invalid-candidate hold keeps existing stable value). | 500 ms poll. | `LalaLaunch.cs` — `GetProjectionLapSeconds` + `AttachCore`【F:LalaLaunch.cs†L17585-L17675】【F:LalaLaunch.cs†L6821-L6822】 |
| Fuel.ProjectionLapTime_StableSourceText | string | Dash-facing helper text for `Fuel.ProjectionLapTime_StableSource` (raw source remains canonical diagnostics). Mapping: `pace.stint→LIVE STINT`, `pace.last5→LIVE AVG5`, `profile.avg→PROFILE`, `fuelcalc.estimated→PLANNER EST`, `telemetry.lastlap→LAST LAP`, `fallback.none→NO DATA`; unknown values passthrough unchanged; blank/null defensively resolves to `NO DATA`. | 500 ms poll. | `LalaLaunch.cs` — `GetProjectionLapTimeStableSourceText` + `AttachCore`. |
| Fuel.Live.IsFuelReady | bool | True when stable confidence meets the readiness threshold. | 500 ms poll. | `LalaLaunch.cs` — `IsFuelReady` property + `AttachCore`【F:LalaLaunch.cs†L500-L506】【F:LalaLaunch.cs†L2727-L2729】 |
| Fuel.PitWindowState / Fuel.PitWindowLabel / Fuel.IsPitWindowOpen / Fuel.PitWindowOpeningLap / Fuel.PitWindowClosingLap | int/string/bool/int/int | Pit window state machine (open ECO/STD/PUSH, SET FUEL, NO DATA YET, N/A, TANK SPACE/ERROR). Open states are contingency-aware on required side (`needAdd = lapsRemaining*burn + contingencyForMode - currentFuel`), while runtime add/clamp seams (`Fuel.Pit.WillAdd`, `Fuel.Pit.FuelOnExit`) remain unchanged. Opening/closing lap markers mirror the state machine. | 500 ms poll. | `LalaLaunch.cs` — `UpdateLiveFuelCalcs` pit window block + `AttachCore`【F:LalaLaunch.cs†L2145-L2335】【F:LalaLaunch.cs†L2682-L2687】 |
| Fuel.LastPitLaneTravelTime | double | Last direct pit-lane travel time saved from pit cycle. | On save; surfaced each poll. | `LalaLaunch.cs` — `Pit_OnValidPitStopTimeLossCalculated` + `AttachCore`【F:LalaLaunch.cs†L2950-L3004】【F:LalaLaunch.cs†L2672-L2955】 |

Implementation note (no new export):
- Strategy live-cap validation now consumes the same runtime-authoritative live-cap seam used by fuel runtime (`raw -> live cached -> bounded last-valid fallback`) instead of maintaining a separate direct-only raw read path.

## Pace / Projection
| Exported name | Type | Units / meaning | Update cadence | Defined in |
| --- | --- | --- | --- | --- |
| Pace.StintAvgLapTimeSec | double | Rolling average of accepted clean laps. | Per lap (on crossing). | `LalaLaunch.cs` — `DetectLapCrossing` & pace acceptance + `AttachCore`【F:LalaLaunch.cs†L1080-L1405】【F:LalaLaunch.cs†L2732-L2737】 |
| Pace.Last5LapAvgSec | double | Average of last up-to-five clean laps. | Per lap. | `LalaLaunch.cs` — pace window update + `AttachCore`【F:LalaLaunch.cs†L1080-L1405】【F:LalaLaunch.cs†L2732-L2737】 |
| Pace.LeaderAvgLapTimeSec | double | Overall race-leading pace authority. Primary samples come from current overall P1 `CarIdxLastLapTime` using absolute plausibility only (`>20s`/`<900s`, finite/positive; no player-pace floor) and finish-timing overall-leader identity semantics (`CarIdxPosition==1 && CarIdxClassPosition==1`, then `CarIdxPosition==1`, in-world car required). The rolling window keeps the last 3 valid overall-P1 samples and is not tied to one `CarIdx`; if the overall-P1 identity/feed is temporarily unavailable, the previous rolling average can be held through the same completed-player-lap interval before failing closed. When the rolling window is empty, current overall P1 `CarIdxBestLapTime` can publish as a low-confidence fallback without being ingested into the window. Clears/fails closed when no valid overall P1 pace or bounded hold exists. | Per lap. | `LalaLaunch.cs` — overall leader lap processing + `AttachCore` |
| Pace.LeaderDeltaToPlayerSec | double | Player pace minus overall race-leading pace (`Pace.Last5LapAvgSec - Pace.LeaderAvgLapTimeSec`), or `0` when either side is unavailable. | Per lap. | `LalaLaunch.cs` — `UpdateLeaderDelta` + `AttachCore` |
| Pace.PaceConfidence | int | Confidence derived from clean pace window size/outlier rejection. | Per lap. | `LalaLaunch.cs` — `ComputePaceConfidence` + `AttachCore`【F:LalaLaunch.cs†L1080-L1405】【F:LalaLaunch.cs†L2735-L2737】 |
| Pace.OverallConfidence | int | Combined fuel/pace confidence (probabilistic product). | Per lap / poll. | `LalaLaunch.cs` — `OverallConfidence` getter + `AttachCore`【F:LalaLaunch.cs†L468-L491】【F:LalaLaunch.cs†L2736-L2737】 |
| Pace.PacePredictor / Pace.PacePredictorSource | double/string | Predictor lap time: projection lap time until 5 clean laps, then rolling 3-lap average. Source labels: SIMHUB/PLUGIN/STINT/AVG5/AVG3. | Per lap / poll. | `LalaLaunch.cs` — predictor outputs + `AttachCore`【F:LalaLaunch.cs†L5364-L5427】【F:LalaLaunch.cs†L3073-L3081】 |

## Opponents & Pit Exit
| Exported name | Type | Units / meaning | Update cadence | Defined in |
| --- | --- | --- | --- | --- |
| Opp.Ahead1..5.* / Opp.Behind1..5.* | mixed | Race-order same-class neighbors from the native RaceProgress-first ordering model (up to five each side). Backward compatibility is preserved for existing `Opp.Ahead1/2.*` and `Opp.Behind1/2.*`. | Per tick in eligible live sessions. | `Opponents.cs` + `AttachCore`. |
| Opp.AheadN/Opp.BehindN identity + validity + lap context | mixed | Per slot: `CarIdx`, `Name`, `AbbrevName`, `CarNumber`, `ClassName`, `ClassColor`, `ClassColorHex`, `IsValid`, `IsOnTrack`, `IsOnPitRoad`, `PositionInClass` (effective/live), `LastLap`, `LastLapTimeSec`, `BestLap`, `BestLapTimeSec`, `LapsSincePit` plus passive League Class metadata (`LeagueClassName`, `LeagueClassShortName`, `LeagueClassRank`, `LeagueClassColourHex`, `LeagueClassValid`, `LeagueClassSource`) for each `N=1..5`. | Per tick in eligible live sessions. | `Opponents.cs` + `LalaLaunch.cs` + `LeagueClassResolver.cs` + `AttachCore`. |
| Opp.AheadN/Opp.BehindN optional driver/tag metadata | mixed | Per slot optional metadata: `IRating`, `SafetyRating`, `Licence`, `LicLevel`, `UserID`, `TeamID`, `IsFriend`, `IsTeammate`, `IsBad` (friend/tag flags derive from existing Lala friend/tag sets via `UserID`). | Per tick in eligible live sessions. | `Opponents.cs` + `LalaLaunch.cs` + `AttachCore`. |
| Opp.AheadN/Opp.BehindN gap + fight metrics | mixed | `Gap.TrackSec` = progress/pace native track-gap estimate. `Gap.RelativeSec` = preferred relative gap (checkpoint seam for slot 1 per side when valid, else fallback to `Gap.TrackSec`). `GapToPlayerSec` remains the legacy-compatible mirror of that preferred relative value. Also publishes `BlendedPaceSec`, `PaceDeltaSecPerLap`, and `LapsToFight`. | Per tick in eligible live sessions. | `Opponents.cs`, `CarSAEngine.cs` seam + `AttachCore`. |
| Opp.Leader.BlendedPaceSec / Opp.P2.BlendedPaceSec | double | Blended pace for class position 1 and 2 from native same-class order resolution. | Per tick in eligible live sessions. | `Opponents.cs` + `AttachCore`. |
| Opponents_SummaryAhead / Opponents_SummaryBehind (+ per-slot variants) | string | Top-level summaries keep short readability (A1/A2 and B1/B2 shape), while per-slot summaries now include `Ahead1..5` and `Behind1..5`. | Per tick in eligible live sessions. | `Opponents.cs` + `AttachCore`. |
| PitExit.Valid | bool | True when race-scoped native pit-exit predictor has enough native identity/progress inputs. | Per tick on pit road/active pit trip; bounded time cadence off pit road when no active pit trip (currently ≥1.0s between refreshes). | `Opponents.cs` + `AttachCore`. |
| PitExit.PredictedPositionInClass / CarsAheadAfterPitCount | int/int | Progress-based post-stop class-position prediction using a phase split: pre-pit keeps existing full-loss behavior, while active pit-cycle mode uses a remaining pit-cycle countdown (latched total-loss minus elapsed cycle time) against the full race-context class field. With League Class enabled + resolved, race-context class authority is effective League class; disabled/unresolved falls back to native class behavior. | Per tick on pit road/active pit trip; bounded time cadence off pit road when no active pit trip (currently ≥1.0s between refreshes). | `Opponents.cs` + `AttachCore`. |
| PitExit.RemainingCountdownSec | double | Active-cycle remaining countdown (seconds) from the pit-exit predictor using the shared boxed-stop loss seam (`drive-through pit-lane baseline + boxed service + fixed 2.00s transition allowance`). Publishes `>0` while the active pit-cycle countdown is running, else `0` when inactive/unavailable. | Per tick on pit road/active pit trip; bounded time cadence off pit road when no active pit trip (currently ≥1.0s between refreshes). | `Opponents.cs` + `AttachCore`. |
| PitExit.TimeToExitSec | double | Plugin-owned blended pit-exit time for dash use: model-based `RemainingCountdownSec` dominates at low speed (only when strictly `>0`), then smoothly blends toward kinematic `PitExit.TimeS` near limiter speed. `RemainingCountdownSec <= 0` is treated as unavailable so inactive-cycle zeros do not collapse the blend. Publishes `0` outside pit lane and clamps invalid values to `0`. Limiter source chain: `GameData.PitLimiterSpeed` → parsed `WeekendInfo.TrackPitSpeedLimit`. | Per tick while pit logic updates run. | `LalaLaunch.cs` blend seam + `AttachCore`. |
| PitExit.ActivePitCycle | bool | `true` while the player is in the active pit-cycle prediction phase (`onPitRoad || pitTripActive`), otherwise `false`. | Per tick on pit road/active pit trip; bounded time cadence off pit road when no active pit trip (currently ≥1.0s between refreshes). | `Opponents.cs` + `AttachCore`. |
| PitExit.Summary | string | Human summary for pit-exit prediction (`P#`, nearest ahead/behind, pit-loss used). During active pit-cycle updates, `loss` reflects the remaining countdown estimate used by the predictor. Timing/gap/loss wording math is unchanged by League presentation mode. | Per tick on pit road/active pit trip; bounded time cadence off pit road when no active pit trip (currently ≥1.0s between refreshes). | `Opponents.cs` + `AttachCore`. |
| PitExit.Ahead.* / PitExit.Behind.* | mixed | Nearest same-class cars ahead/behind after predicted stop, using progress deltas and pace scaling from the shared Opponents pace-reference seam. In active pit-cycle mode, rivals behind who entered pit road after our cycle started are excluded as normal on-track pass-before-exit threats while they remain on pit road. Class-facing presentation fields now follow League Class authority when enabled+resolved (`ClassColor` remains `0xRRGGBB`), with native fallback when disabled/unresolved. | Per tick on pit road/active pit trip; bounded time cadence off pit road when no active pit trip (currently ≥1.0s between refreshes). | `Opponents.cs` + `LalaLaunch.cs` + `AttachCore`. |
| Pit.Debrief.Valid / AgeSec / StopIndex / SummaryText | bool/double/int/string | Latest pit-stop debrief for the alerts overlay. `SummaryText` now updates progressively during collection (`ENTRY ... | BOX PENDING | SVC PENDING | STRAT Δ PENDING`) and latches at finalization; it excludes PitExit verdict wording, uses signed one-decimal entry/box deltas, service text from actual fuel added plus tyre count, and `STRAT Δ` for final actual-vs-predicted stop delta. Entry headline wording is performance-oriented from `Pit.EntryLineTimeLoss_s` and is intentionally separate from the Pit Entry Assist safety/compliance token. Box delta is shown only when the existing countdown source is valid/plausible; impossible values (`abs(delta) > 10.0s`) are suppressed to `Δ PENDING`. `Valid` still becomes true only after finalization, `AgeSec` is still seconds since final emission, and dashboards must not recompute summary text. Public names are single-prefixed as `LalaLaunch.Pit.Debrief.*`. | Updated on pit entry/box/service/lane-exit/out-lap lifecycle edges; age is read-time derived and unchanged. | `PitDebriefEngine.cs` + `LalaLaunch.cs` `AttachCore`. |
| Pit.Debrief.StateText | string | Diagnostic lifecycle state: `EMPTY`, `COLLECTING`, `LANE_EXIT`, `AWAITING_OUT_LAP`, or `FINAL`. | Latched on debrief lifecycle edges. | `PitDebriefEngine.cs` + `LalaLaunch.cs` `AttachCore`. |
| Pit.Debrief.Entry.QualityText / LineTimeLossSec / DecelQualityText / LimiterQualityText | string/double/string/string | Latched pit-entry review from existing Pit Entry Assist outputs. `QualityText` is the Pit Debrief performance headline from `LineTimeLossSec` (`>0.5s` → `POOR`, `>0.1s` → `NORMAL`, otherwise `GOOD`), so a Pit Entry Assist `bad` safety/compliance token with `0.0s` time loss no longer becomes `ENTRY POOR`. `LimiterQualityText` keeps the original compliance token mapping (`safe`/`normal`/`bad` → `SAFE`/`NORMAL`/`POOR`) for debug/log review; unavailable publishes `UNKNOWN`/`0`. `DecelQualityText` intentionally remains `UNKNOWN` until an existing actual-decel source is available; no new decel calculation is introduced. | Latched during the pit cycle from existing PitEngine entry outputs. | `PitDebriefEngine.cs` + `PitEngine.cs` entry-assist outputs + `LalaLaunch.cs`. |
| Pit.Debrief.Box.QualityText / MissedReason / StationarySec | string/string/double | Latched box review. Valid boxed stop without a missed-box phase publishes `GOOD`/`none`; `MissedBoxLong` maps to `OVERSHOT`/`long`; short/left/right missed phases map to `MISSED` with the matching reason; unavailable publishes `UNKNOWN`/`unknown`/`0`. | Latched on first box entry and box exit. | `PitDebriefEngine.cs` + PitEngine phase/stop-duration seams. |
| Pit.Debrief.Service.FuelAddedLitres / FuelTargetLitres / RefuelDurationSec / RefuelRateLps / TyreChangeCount | double/double/double/double/int | Latched service review from existing Fuel.Pit/Fuel.Pit.Box and Fuel.Live seams. Fuel-added evidence is preserved from the existing gauge while the box countdown is still active so box-exit gauge resets cannot erase a completed refuel; `SummaryText` service wording uses actual fuel added, not target/exit/MFD request. `FuelTargetLitres` remains debug-only and is the existing MFD-request/tank-space-clamped `Fuel.Pit.WillAdd`/`Fuel.Pit.Box.WillAddLatched` source. `RefuelDurationSec` now latches the existing refuel-rate learner's completed-flow duration when that seam finalizes; no separate flow detector is added. | Latched on box entry/active service/box exit/refuel-learner finalization. | `PitDebriefEngine.cs` + existing fuel runtime exports/seams. |
| Pit.Debrief.Timing.PredictedTotalLossSec / ActualTotalLossSec / LossDeltaSec / LossSource | double/double/double/string | Latched timing review. Prediction is `Fuel.Live.TotalStopLoss` at pit-entry baseline. Actual is normalized to total-stop-equivalent: existing PitEngine/PitLite DTL or direct lane-equivalent final loss plus the latched stationary box duration when a boxed stop occurred. Drive-throughs add `0`. `LossDeltaSec = actual total - predicted total`; source remains `dtl`, `direct`, or `unavailable`. | Latched on pit entry and final out-lap/loss seam. | `PitDebriefEngine.cs` + PitEngine timing seams + `LalaLaunch.cs`. |
| Pit.Debrief.Exit.PredictedPositionInClass / ActualPositionInClass / PositionDelta / AccuracyText | int/int/int/string | Latched PitExit review. Predicted position comes from `PitExit.PredictedPositionInClass` at pit entry; actual position is the existing live race-position seam at pit-lane exit. Delta is actual − predicted; accuracy is `EXACT`, `CLOSE`, `MISS`, or `UNKNOWN`. | Latched on pit entry and pit-lane exit. | `PitDebriefEngine.cs` + Opponents/PitExit live race-position seams. |

## Head-to-Head (H2H)
| Exported name | Type | Units / meaning | Update cadence | Defined in |
| --- | --- | --- | --- | --- |
| H2HRace.ClassSessionBestLapSec / H2HTrack.ClassSessionBestLapSec | double | Family-level player-class session-best lap time in seconds used for dash/debug visibility and H2H last-lap coloring. Shared class-best seam: `HasMultipleClassOpponents=false` scans whole-field best lap; `HasMultipleClassOpponents=true` scans only cars matching player class (`GameData.CarClass` + per-car `DriverInfo` class label). When unresolved, value remains `0`. | Per tick. | `H2HEngine.cs` + `LalaLaunch.cs` selector/update wiring + `AttachCore`. |
| H2HRace.Player.* / H2HTrack.Player.* | mixed | Player lap-summary values for the race/track head-to-head families: `LastLapSec`, `BestLapSec`, `PositionInClass`, `LastLapDeltaToBestSec`, `LiveDeltaToBestSec`, `LastLapColor`, `ActiveSegment`, `LapRef`. `PositionInClass` uses the same effective/live race-order context seam used by H2H target rows when available (with local/native fallback when unavailable). `LastLapColor` is a direct dash-ready `#RRGGBB` string: white `#FFFFFF` for a normal last lap, lime `#00FF00` when the published last lap is also the participant's valid PB, and magenta `#FF00FF` when that valid PB also matches the same-class session-best; session-best overrides PB, and invalid faster raw last laps stay white. H2H still keeps lightweight player presentation state for live delta and active segment while published sector rows now come from CarSA's per-car cache. | Per tick. | `H2HEngine.cs` + `LalaLaunch.cs` selector/update wiring + `AttachCore`. |
| H2HRace.Ahead.* / H2HRace.Behind.* | mixed | Race-target H2H contract for class-ahead / class-behind identity selected from Opponents and then resolved back to live `CarIdx` for comparison. Fields: `Valid`, `CarIdx`, `IdentityKey`, `Name`, `CarNumber`, `ClassColor`, `ClassColorHex`, `PositionInClass`, `LastLapSec`, `BestLapSec`, `LastLapDeltaToBestSec`, `LiveDeltaToBestSec`, `LastLapColor`, `LastLapDeltaToPlayerSec`, `LiveGapSec`, `ActiveSegment`, `LapRef`, and `S1..S6DeltaSec` / `S1..S6State`. `PositionInClass` is published as effective/live race-order context when available. | Per tick. | `H2HEngine.cs` + `LalaLaunch.cs` selector/update wiring + `AttachCore`. |
| H2HTrack.Ahead.* / H2HTrack.Behind.* | mixed | Track-target H2H contract for local on-track ahead/behind selection from the nearest valid same-class CarSA slot. Fields match `H2HRace.*` exactly, but `LiveGapSec` is sourced from the selected CarSA slot directional `Gap.TrackSec` magnitude (positive display convention) instead of H2H recomputing an independent full race-progress gap; if that selected TrackSec is non-finite, live gap publishes the existing safe `0.0` value. Track selector ownership stays in CarSA; published `PositionInClass` uses effective/live race-position context when available without moving selector ownership. Class presentation fields (`ClassColor`, `ClassColorHex`) now follow the same League Class presentation gate as race-context outputs when enabled+resolved, with native fallback unchanged when disabled/unresolved. | Per tick. | `H2HEngine.cs` + `LalaLaunch.cs` selector/update wiring + `AttachCore`. |
| H2HRace.Ahead/Behind.S1..S6State / H2HTrack.Ahead/Behind.S1..S6State | int | Cache-based sector-state contract: `0 = empty`, `1 = pending`, `2 = valid`. `empty` means the target sector is missing/invalid in the CarSA cache. `pending` means the target sector is valid but the player sector is missing/invalid. `valid` means both target and player sectors are valid in cache. State is no longer driven by target-bind timing windows or row rebuild mechanics. | Per tick. | `H2HEngine.cs` + `AttachCore`. |
| H2HRace.Ahead/Behind.S1..S6DeltaSec / H2HTrack.Ahead/Behind.S1..S6DeltaSec | double | Cache-based per-sector delta versus the player for the fixed 6-sector model. Delta is `0` when the target sector is missing, `0` when the player sector is missing, and otherwise `target.DurationSec - player.DurationSec` using the CarSA snapshots for the selected target and player. On target swap, H2H publishes immediately from the new target's existing cache if present; if not, the row naturally shows `empty`/`0` without a bind-clear wait. | Per tick. | `H2HEngine.cs` + `AttachCore`. |

## Lap Reference (LapRef)
| Exported name | Type | Units / meaning | Update cadence | Defined in |
| --- | --- | --- | --- | --- |
| LapRef.Valid | bool | True when LapRef has usable runtime context (player car idx + combo identity). | Per tick. | `LapReferenceEngine.cs` + `LalaLaunch.cs` + `AttachCore`. |
| LapRef.Mode | string | Active condition mode (`Dry` / `Wet`) from runtime `_isWetMode` routing. | Per tick. | `LalaLaunch.cs` wet-mode routing + `LapReferenceEngine.cs` + `AttachCore`. |
| LapRef.PlayerCarIdx / LapRef.ActiveSegment | int/int | Player `CarIdx` and current active segment context (1..6; 0 when unknown). | Per tick. | `LalaLaunch.cs` + `LapReferenceEngine.cs` + `AttachCore`. |
| LapRef.DeltaToSessionBestSec / LapRef.DeltaToProfileBestSec | double/double | Cumulative player delta (seconds) to session-best/profile-best across completed **current-lap comparable sectors only**. Eligibility is re-armed on normal lap rollover, so prior-lap carried display boxes do not contribute; values publish `0` until at least one current-lap valid player/reference sector pair contributes. | Per tick. | `LapReferenceEngine.cs` + `LalaLaunch.cs` + `AttachCore`. |
| LapRef.DeltaToSessionBestValid / LapRef.DeltaToProfileBestValid | bool/bool | True only when at least one current-lap valid sector pair contributed to the corresponding cumulative delta; false at lap start/rollover until new-lap comparable sectors exist. | Per tick. | `LapReferenceEngine.cs` + `LalaLaunch.cs` + `AttachCore`. |
| LapRef.Player.* | mixed | Player live-comparison side outputs: `Valid`, `LapTimeSec`, `ActiveSegment`, and `S1..S6State` / `S1..S6Sec`. Player row sectors are published directly from live CarSA fixed-sector cache presence each tick (same H2H-style cache-consumption behavior; no LapRef-local display rollover model). `LapTimeSec` is published from the same trusted player last-lap seam used by H2H/core (CarIdx last-lap authority path via `playerLastLapTimeSec`). | Per tick. | `LapReferenceEngine.cs` + `LalaLaunch.cs` + `AttachCore`. |
| LapRef.SessionBest.* | mixed | Session-best reference side (`Valid`, `LapTimeSec`, `S1..S6State`, `S1..S6Sec`) with split authority: `LapTimeSec` is synchronized each tick from the same trusted player best-lap seam used by H2H/core (`BestLapTime`/CarIdx-best path in `LalaLaunch`), while LapRef keeps snapshot ownership for session-best sector payload (`S1..S6*`) from validated-lap capture. Lap time can remain valid even with missing sectors. | Per tick. | `LapReferenceEngine.cs` + `LalaLaunch.cs` + `AttachCore`. |
| LapRef.ProfileBest.* | mixed | Profile all-time static reference side (`Valid`, `LapTimeSec`, `S1..S6State`, `S1..S6Sec`) rematerialized from active profile/track/condition each tick using `BestLapMsDry/Wet` and optional `BestLapSector1..6Dry/WetMs`. | Per tick. | `CarProfiles.cs` + `LapReferenceEngine.cs` + `LalaLaunch.cs` + `AttachCore`. |
| LapRef.Compare.SessionBest.* | mixed | Per-sector comparison versus session best: `S1..S6State`, `S1..S6DeltaSec`. Comparison uses current-lap comparable eligibility (not carried display state), so prior-lap sectors are not treated as new-lap compare input after rollover. Delta publishes only when both sectors are real; otherwise state is non-valid and delta is `0`. | Per tick. | `LapReferenceEngine.cs` + `AttachCore`. |
| LapRef.Compare.ProfileBest.* | mixed | Per-sector comparison versus profile best: `S1..S6State`, `S1..S6DeltaSec`. Same current-lap eligibility and delta validity rules as session-best comparison. | Per tick. | `LapReferenceEngine.cs` + `AttachCore`. |

## CarSA (Car System)
| Exported name | Type | Units / meaning | Update cadence | Defined in |
| --- | --- | --- | --- | --- |
| Car.Valid | bool | True when CarSA has a valid player index and CarIdx lap pct truth for the tick. | Per tick. | `CarSAEngine.cs` + `AttachCore`【F:CarSAEngine.cs†L74-L233】【F:LalaLaunch.cs†L3414-L3418】 |
| Car.Source | string | Source label (`CarIdxTruth`). | Per tick. | `CarSAEngine.cs` + `AttachCore`【F:CarSAEngine.cs†L74-L87】【F:LalaLaunch.cs†L3414-L3418】 |
| Car.SlotsAhead / Car.SlotsBehind | int | Slot counts (5/5). | Per tick. | `CarSAEngine.cs` + `AttachCore`【F:CarSAEngine.cs†L7-L11】【F:LalaLaunch.cs†L3414-L3418】 |
| Car.iRatingSOF | double | Strength-of-field iRating average. League Class OFF (or unresolved player effective class): average across active CarSA slots. League Class ON + resolved player effective class: average across active CarSA slots whose resolved effective class matches the player's effective class. | Per tick. | `CarSAEngine.cs` + `AttachCore`【F:CarSAEngine.cs†L270-L304】【F:LalaLaunch.cs†L3551-L3556】 |
League Class canonical subsystem reference: `Docs/Subsystems/League_Class_System.md` (resolver precedence, fallback modes, effective cohort semantics, UI workflow, boundaries).

| LeagueClass.* global + player metadata | mixed | `LeagueClass.Enabled`, `LeagueClass.Mode`, `LeagueClass.ConfigStatusText`, `LeagueClass.LoadedCount`, `LeagueClass.ValidDriverCount`, `LeagueClass.InvalidRowCount`, `LeagueClass.DuplicateRowCount`, `LeagueClass.Player.Name`, `LeagueClass.Player.ShortName`, `LeagueClass.Player.Rank`, `LeagueClass.Player.ColourHex`, `LeagueClass.Player.Valid`, `LeagueClass.Player.Source`, `LeagueClass.Player.OverrideActive`, `LeagueClass.Player.DriverCount` (player effective-class cohort count; native same-class fallback when League Class disabled/unresolved. When League Class is enabled+resolved but live competing-driver identity rows are temporarily unavailable, falls back to valid CSV cohort count for the player effective class only in CSV-capable modes (`CsvOnly`/`CsvThenName`) and with the same class-definition enabled gate as resolver paths; fallback also preserves player-included cohort semantics when the player is not represented by a valid CSV membership in that class (and only while CSV-capable mode semantics are active)). CSV mapping remains `CustomerId -> CSV ClassName`; effective short/rank/colour/valid come from resolver-applied class-definition layer (`LeagueClassDefinitions`) keyed by CSV class name (`Enabled/ShortName/Rank/ColourHex`). Disabled class definitions force League Class invalid and fall back to native class behavior (`Source=NATIVE`). Resolver-owned only (`CSV/NAME/MANUAL/NATIVE/NONE` source text). | Per tick. | `LeagueClassResolver.cs` + `LalaLaunch.cs` + `AttachCore`. |
| Car.Ahead01P.Gap.Sec / Car.Behind01P.Gap.Sec | double | Direction-safe precision gap for slot 1. Internal checkpoint truth/filtered state remains stored as target crossing time minus player crossing time; after source selection, precision locally negates that stored proximity candidate into TrackSec convention (ahead positive, behind negative). Finite selected-slot `Gap.TrackSec` still chooses the side and lap branch. The direction-normalized candidate is tested at its current value and ± one lap time; the closest same-sign candidate publishes only within `min(6.0 s, max(1.5 s, 8% of |TrackSec|))`. Missing, wrong-direction, or out-of-tolerance checkpoint state falls back directly to TrackSec; invalid slots/TrackSec and blink holds publish `NaN`. | Per tick. | `CarSAEngine.cs` + `AttachCore`. |
| Car.Player.* | mixed | Player slot outputs: CarIdx, TrackPct, PositionInClass, ClassName, ClassColor, ClassColorHex, IRating, Licence, SafetyRating, LicLevel, TeamID, LapsSincePit, Status, StatusE, StatusShort, StatusLong, StatusEReason, LapTimeUpdate (latched PB/SB/LL until next lap), LapTimeUpdateVisibilitySec (short flash countdown). Published `PositionInClass` consumes the Opponents effective/live race-order seam when available, with local/native fallback only when that seam is unavailable. Class presentation fields (`ClassName`, `ClassColor`, `ClassColorHex`) publish effective League Class presentation when enabled+resolved (manual player override included) and retain native CarSA values when disabled/unresolved; `ClassName` uses League `ShortName` when non-blank and falls back to League `Name` when `ShortName` is blank. `TrackPct` is the plugin-owned raw player `CarIdxLapDistPct` normalized to `0..1` (invalid/unavailable => `0`). | Per tick. | `CarSAEngine.cs` + `LalaLaunch.cs` + `AttachCore`【F:CarSAEngine.cs†L248-L709】【F:LalaLaunch.cs†L3475-L3513】【F:LalaLaunch.cs†L10375-L10449】 |
| Car.Player.PaceFlagsRaw / SessionFlagsRaw / TrackSurfaceMaterialRaw | int | Raw telemetry flags for the player CarIdx row (or -1 if missing). Populated only when soft debug + raw-telemetry mode are enabled. | Per tick. | `LalaLaunch.cs` raw telemetry debug + `AttachCore`【F:LalaLaunch.cs†L3557-L3560】【F:LalaLaunch.cs†L4874-L4917】 |
| Car.Player.LapTimeUpdate / Car.Player.LapTimeUpdateVisibilitySec | string / double | Player lap update label mirrors slot behavior: `LapTimeUpdate` latches to `PB`/`SB`/`LL` on lap completion and persists until the next completed lap (or reset). `LapTimeUpdateVisibilitySec` remains the short alert flash timer and counts down to 0. | Per tick; values change on player lap completion and session reset. | `LalaLaunch.cs` telemetry cache/player mapping + `AttachCore` |
| Player.LapTimeUpdate / Player.LapTimeUpdateVisibilitySec | string / double | Backward-compatible aliases of `Car.Player.LapTimeUpdate` and `Car.Player.LapTimeUpdateVisibilitySec` with identical values. | Per tick; alias exports. | `LalaLaunch.cs` `AttachCore` aliases |
| Car.Ahead01..Ahead05.* | mixed | Slot outputs for nearest ahead cars: CarIdx, Name, CarNumber, ClassColor, ClassColorHex, ClassName, CarClassShortName, Initials, AbbrevName, LicLevel, UserID, TeamID, PositionInClass, IRating, Licence, SafetyRating, IsOnTrack, IsOnPitRoad, IsValid, IsTalking (false when not transmitting), TalkRadioIdx (-1 default), TalkFrequencyIdx (-1 default), TalkFrequencyName (empty when unknown), LapDelta, LapsSincePit, BestLapTimeSec, LastLapTimeSec, BestLap, BestLapIsEstimated, LastLap, DeltaBestSec, DeltaBest, EstLapTimeSec, EstLapTime, LapTimeUpdate (latched PB/SB/LL until next lap), LapTimeUpdateVisibilitySec (short flash timer), HotScore, HotVia, Gap.TrackSec (directional track-loop baseline), Gap.RelativeSec (gate-gap v2 filtered proximity; may be NaN), Gap.RelativeSource (0 invalid, 1 filtered, 2 truth, 3 track fallback, 4 sticky hold), InfoVisibility, Info, ClosingRateSecPerSec (player-centric; positive = closing), Status, StatusE, StatusShort, StatusLong, StatusEReason, StatusBgHex, BorderMode, BorderHex, SessionFlagsRaw, TrackSurfaceMaterialRaw, plus passive League Class metadata (`LeagueClassName`, `LeagueClassShortName`, `LeagueClassRank`, `LeagueClassColourHex`, `LeagueClassValid`, `LeagueClassSource`). `PositionInClass` now publishes through the shared effective-position seam (`GetEffectivePositionInClassForPublishedContext`): Opponents effective/live race-context position when available, native slot fallback otherwise. Class presentation fields (`ClassName`, `ClassColor`, `ClassColorHex`) are publish-time League-aware overrides only and do not alter CarSA slot selection/order/filter behavior; for League-resolved class-name presentation, `ClassName` uses `ShortName` first and falls back to `Name` when short name is blank. | Per tick; distance gaps derived from car-centric LapDistPct deltas. | `CarSAEngine.cs` slot update + `AttachCore`【F:CarSAEngine.cs†L248-L709】【F:LalaLaunch.cs†L3419-L3508】 |
| Car.Behind01..Behind05.* | mixed | Slot outputs for nearest behind cars: CarIdx, Name, CarNumber, ClassColor, ClassColorHex, ClassName, CarClassShortName, Initials, AbbrevName, LicLevel, UserID, TeamID, PositionInClass, IRating, Licence, SafetyRating, IsOnTrack, IsOnPitRoad, IsValid, IsTalking (false when not transmitting), TalkRadioIdx (-1 default), TalkFrequencyIdx (-1 default), TalkFrequencyName (empty when unknown), LapDelta, LapsSincePit, BestLapTimeSec, LastLapTimeSec, BestLap, BestLapIsEstimated, LastLap, DeltaBestSec, DeltaBest, EstLapTimeSec, EstLapTime, LapTimeUpdate (latched PB/SB/LL until next lap), LapTimeUpdateVisibilitySec (short flash timer), HotScore, HotVia, Gap.TrackSec (directional track-loop baseline), Gap.RelativeSec (gate-gap v2 filtered proximity; may be NaN), Gap.RelativeSource (0 invalid, 1 filtered, 2 truth, 3 track fallback, 4 sticky hold), InfoVisibility, Info, ClosingRateSecPerSec (player-centric; positive = closing), Status, StatusE, StatusShort, StatusLong, StatusEReason, StatusBgHex, BorderMode, BorderHex, SessionFlagsRaw, TrackSurfaceMaterialRaw, plus passive League Class metadata (`LeagueClassName`, `LeagueClassShortName`, `LeagueClassRank`, `LeagueClassColourHex`, `LeagueClassValid`, `LeagueClassSource`). `PositionInClass` now publishes through the shared effective-position seam (`GetEffectivePositionInClassForPublishedContext`): Opponents effective/live race-context position when available, native slot fallback otherwise. Class presentation fields (`ClassName`, `ClassColor`, `ClassColorHex`) are publish-time League-aware overrides only and do not alter CarSA slot selection/order/filter behavior; for League-resolved class-name presentation, `ClassName` uses `ShortName` first and falls back to `Name` when short name is blank. | Per tick; distance gaps derived from car-centric LapDistPct deltas. | `CarSAEngine.cs` slot update + `AttachCore`【F:CarSAEngine.cs†L248-L709】【F:LalaLaunch.cs†L3526-L3587】 |
| Car.Debug.* | mixed | Debug telemetry for player/slot identity, raw telemetry availability, and slot filtering: `PlayerCarIdx`, `PlayerLapPct`, `PlayerLap`, `SessionTimeSec`, `SourceFastPathUsed`, `HasCarIdxPaceFlags`, `HasCarIdxSessionFlags`, `HasCarIdxTrackSurfaceMaterial`, `RawTelemetryReadMode`, `RawTelemetryFailReason`, `Ahead01.CarIdx`, `Ahead01.ForwardDistPct`, `Behind01.CarIdx`, `Behind01.BackwardDistPct`, `InvalidLapPctCount`, `OnPitRoadCount`, `OnTrackCount`, `TimestampUpdatesThisTick`, `FilteredHalfLapCountAhead`, `FilteredHalfLapCountBehind`, `LapTimeEstimateSec`, `LapTimeUsedSec`, `HysteresisReplacementsThisTick`, `SlotCarIdxChangedThisTick`. | Per tick. | `CarSAEngine.cs` debug fields + `AttachCore`【F:CarSAEngine.cs†L85-L283】【F:LalaLaunch.cs†L3482-L3510】 |


## Radio
| Exported name | Type | Units / meaning | Update cadence | Defined in |
| --- | --- | --- | --- | --- |
| Radio.TransmitShortName | string | Abbreviated name for the transmitting car (`RadioTransmitCarIdx`) from `SessionData.DriverInfo.DriversXX.AbbrevName`, empty when no transmitter or missing. | Per tick. | `LalaLaunch.cs` transmit caching + `AttachCore`【F:LalaLaunch.cs†L3673-L3681】【F:LalaLaunch.cs†L7078-L7424】 |
| Radio.TransmitFullName | string | Full name for the transmitting car (`RadioTransmitCarIdx`), resolved from `UserName`/`DisplayName`/`Name` (fallback to `FirstName`+`LastName`). Empty when no transmitter or missing. | Per tick. | `LalaLaunch.cs` transmit caching + `AttachCore`【F:LalaLaunch.cs†L3673-L3681】【F:LalaLaunch.cs†L7078-L7491】 |
| Radio.TransmitClassPosLabel | string | Class position label for the transmitting car (`RadioTransmitCarIdx`), formatted as `{class} P{pos}` when class short name is available (falls back to `P{pos}` when not). Empty when no transmitter or identity missing. | Per tick. | `LalaLaunch.cs` transmit caching + `AttachCore`【F:LalaLaunch.cs†L3673-L3677】【F:LalaLaunch.cs†L7294-L7332】 |
| Radio.TransmitFrequencyName | string | **Deprecated/unused.** Always an empty string to avoid implying a remote speaker channel. | Per tick. | `LalaLaunch.cs` `AttachCore`【F:LalaLaunch.cs†L3673-L3676】 |
| Radio.TransmitFrequencyMuted | bool | Channel-level mute state for the currently transmitting frequency (from `FrequenciesYY.Muted`), false when no transmitter or mapping missing. This reflects the channel setting only (not per-driver mutes) and updates live if the channel mute toggles mid-transmission. | Per tick. | `RadioFrequencyNameCache.cs` + `LalaLaunch.cs` transmit caching + `AttachCore`【F:RadioFrequencyNameCache.cs†L101-L239】【F:LalaLaunch.cs†L3673-L3677】【F:LalaLaunch.cs†L7078-L7349】 |
| Radio.LocalTxFrequencyNum | int | Player local tuned transmit frequency number from `RadiosXX.TunedToFrequencyNum`; falls back to telemetry `RadioTransmitFrequencyIdx` when unavailable. | Per tick. | `RadioFrequencyNameCache.cs` + `LalaLaunch.cs` local TX lookup + `AttachCore`【F:RadioFrequencyNameCache.cs†L11-L239】【F:LalaLaunch.cs†L3678-L3681】【F:LalaLaunch.cs†L7351-L7392】 |
| Radio.LocalTxFrequencyName | string | Frequency name for the player local tuned transmit channel, matched by `FrequenciesYY.FrequencyNum` on the selected radio. Empty when unresolved. | Per tick. | `RadioFrequencyNameCache.cs` + `LalaLaunch.cs` local TX lookup + `AttachCore`【F:RadioFrequencyNameCache.cs†L101-L239】【F:LalaLaunch.cs†L3678-L3681】【F:LalaLaunch.cs†L7351-L7392】 |
| Radio.LocalTxFrequencyMuted | bool | Channel-level mute state for the player local tuned transmit frequency (from `FrequenciesYY.Muted`); updates live. | Per tick. | `RadioFrequencyNameCache.cs` + `LalaLaunch.cs` local TX lookup + `AttachCore`【F:RadioFrequencyNameCache.cs†L101-L239】【F:LalaLaunch.cs†L3678-L3681】【F:LalaLaunch.cs†L7394-L7409】 |
| Radio.IsPlayerTransmitting | bool | True when the transmitting car index matches the player car index; false otherwise. | Per tick. | `LalaLaunch.cs` transmit caching + `AttachCore`【F:LalaLaunch.cs†L3681-L3682】【F:LalaLaunch.cs†L7078-L7103】 |

**CarSA style helper exports (30 total)**
- `Car.Ahead01.StatusBgHex`, `Car.Ahead01.BorderMode`, `Car.Ahead01.BorderHex`
- `Car.Ahead02.StatusBgHex`, `Car.Ahead02.BorderMode`, `Car.Ahead02.BorderHex`
- `Car.Ahead03.StatusBgHex`, `Car.Ahead03.BorderMode`, `Car.Ahead03.BorderHex`
- `Car.Ahead04.StatusBgHex`, `Car.Ahead04.BorderMode`, `Car.Ahead04.BorderHex`
- `Car.Ahead05.StatusBgHex`, `Car.Ahead05.BorderMode`, `Car.Ahead05.BorderHex`
- `Car.Behind01.StatusBgHex`, `Car.Behind01.BorderMode`, `Car.Behind01.BorderHex`
- `Car.Behind02.StatusBgHex`, `Car.Behind02.BorderMode`, `Car.Behind02.BorderHex`
- `Car.Behind03.StatusBgHex`, `Car.Behind03.BorderMode`, `Car.Behind03.BorderHex`
- `Car.Behind04.StatusBgHex`, `Car.Behind04.BorderMode`, `Car.Behind04.BorderHex`
- `Car.Behind05.StatusBgHex`, `Car.Behind05.BorderMode`, `Car.Behind05.BorderHex`

**Notes**
- Hot/Cool intent updates on a coarse 6-sector cadence (10 mini-sectors each), while FAST!/SLOW! conflict detection still evaluates on the 60 mini-sector updates for responsiveness.【F:CarSAEngine.cs†L1502-L1787】
- CarSA style helpers now publish `StatusBgHex`, `BorderMode`, and `BorderHex` for Ahead/Behind slots. `StatusBgHex` is keyed by `StatusE` from settings (except `FasterClass`/`SlowerClass`, which use the slot `ClassColorHex`), and border resolution uses priority TEAM > LEAD > OCLS > DEF with colors from settings.
- Gate-gap v2 publishes `Gap.RelativeSec` using mini-sector gate timing with prediction and sticky hold, keeping local proximity stable during wraps; it falls back to `Gap.TrackSec` when no gate data exists. Slot-01 precision gaps (`Car.Ahead01P/Behind01P.Gap.Sec`) preserve the selected stored/proximity checkpoint candidate for diagnostics, locally negate it into TrackSec sign convention, then reconcile that direction-normalized value onto the selected slot's TrackSec lap branch and fall back to TrackSec on uncertainty. H2HTrack remains on its direct TrackSec baseline.
- `Gap.RelativeSource` indicates which input fed the relative gap: filtered (1), truth (2), track fallback (3), sticky hold (4), or invalid (0). Slot01 precision gaps mirror the same source priority but bypass sticky-hold unless a valid gate/track source exists.【F:CarSAEngine.cs†L1336-L1490】
- `InfoVisibility`/`Info` now use burst-based slot info: 9 s bursts near S/F (laps-since-pit, last-lap vs best, last-lap vs player) plus 9 s half-lap bursts (live Δ + laps-since-pit), with a debounced gate that only shows baseline info when `StatusE == Unknown`. Bursts are latched per car and reset on slot swaps or invalid data.【F:CarSAEngine.cs†L1488-L1709】

## Pit timing and PitLite
| Exported name | Type | Units / meaning | Update cadence | Defined in |
| --- | --- | --- | --- | --- |
| Pit.LastDirectTravelTime / Pit.LastTotalPitCycleTimeLoss / Pit.LastPaceDeltaNetLoss | double | Latest pit lane direct time, total DTL, and pace-delta net loss from PitEngine. | On pit cycle completion (latched) and surfaced each poll. | `LalaLaunch.cs` — pit finalization + `AttachCore`【F:LalaLaunch.cs†L1408-L1469】【F:LalaLaunch.cs†L2743-L2746】 |
| PitLite.Live.TimeOnPitRoadSec / TimeInBoxSec | double | Live timers during a pit stop (lane and box). | Per tick while pitting. | `LalaLaunch.cs` — PitEngine timers + `AttachCore`【F:LalaLaunch.cs†L1408-L1469】【F:LalaLaunch.cs†L2791-L2795】 |
| PitLite.TotalLossSec / TotalLossPlusBoxSec | double | PitLite preferred loss (DTL/direct) and loss+box composite latched on out-lap. | On pit-lite publication, surfaced each poll. | `PitCycleLite.cs` publish + `AttachCore`【F:PitCycleLite.cs†L170-L208】【F:LalaLaunch.cs†L2796-L2800】 |
| Pit.EntryAssistActive | bool | True while pit entry assist is armed (EnteringPits or limiter ON with overspeed >2 kph, including pit-screen manual arming) and stored pit-entry markers are valid for the current track/session. | Per tick. | `PitEngine.UpdatePitEntryAssist` + `AttachCore`【F:PitEngine.cs†L275-L427】【F:LalaLaunch.cs†L4694-L4702】 |
| Pit.EntryDistanceToLine_m | double | Distance to pit entry guidance line (raw stored-marker distance minus profile buffer). Clamped 0–500 m; assist resets when raw distance ≥500 m or stored marker inputs are unavailable. | Per tick when armed. | `PitEngine.UpdatePitEntryAssist` + `AttachCore`【F:PitEngine.cs†L310-L419】【F:LalaLaunch.cs†L4695-L4700】 |
| Pit.EntryRequiredDistance_m | double | Constant-decel distance needed to reach pit speed at the guidance line using the profiled decel. | Per tick when armed. | `PitEngine.UpdatePitEntryAssist` + `AttachCore`【F:PitEngine.cs†L397-L418】【F:LalaLaunch.cs†L2752-L2754】 |
| Pit.EntryMargin_m | double | Guidance distance remaining minus required distance; positive = early, negative = late. Primary dash signal. | Per tick when armed. | `PitEngine.UpdatePitEntryAssist` + `AttachCore`【F:PitEngine.cs†L405-L417】【F:LalaLaunch.cs†L2752-L2755】 |
| Pit.EntryCue | int | Pit entry cue level (0 OFF, 1 OK, 2 BRAKE SOON, 3 BRAKE NOW, 4 LATE) derived from margin vs. buffer. | Per tick when armed. | `PitEngine.UpdatePitEntryAssist` + `AttachCore`【F:PitEngine.cs†L334-L339】【F:LalaLaunch.cs†L2754-L2756】 |
| Pit.EntryCueText | string | Dash-friendly cue text (“OFF/OK/BRAKE SOON/BRAKE NOW/LATE”). | Per tick when armed. | `PitEngine.PitEntryCueText` + `AttachCore`【F:PitEngine.cs†L19-L38】【F:LalaLaunch.cs†L2755-L2757】 |
| Pit.EntrySpeedDelta_kph | double | Current speed minus pit speed limit (native session pit limit only). | Per tick when armed. | `PitEngine.UpdatePitEntryAssist` + `AttachCore`【F:PitEngine.cs†L251-L276】【F:LalaLaunch.cs†L2756-L2758】 |
| Pit.EntryDecelProfile_mps2 | double | Profile-configured pit entry decel used for the constant-decel model (clamped to 5–25 m/s²). | Per tick when armed. | `PitEngine.UpdatePitEntryAssist` + `AttachCore`【F:PitEngine.cs†L240-L259】【F:LalaLaunch.cs†L2757-L2759】 |
| Pit.EntryBuffer_m | double | Profile-configured buffer distance used for cue thresholds (clamped to 0–50 m). | Per tick when armed. | `PitEngine.UpdatePitEntryAssist` + `AttachCore`【F:PitEngine.cs†L240-L259】【F:LalaLaunch.cs†L2758-L2760】 |
| Pit.EntryLineDebrief | string | Debrief status at the pit entry line (`safe`, `normal`, `bad`) based on line speed delta and buffer window. | Latched on pit entry line. | `PitEngine.UpdatePitEntryAssist` + `AttachCore`【F:PitEngine.cs†L447-L497】【F:LalaLaunch.cs†L3045-L3048】 |
| Pit.EntryLineDebriefText | string | Plain-English debrief message for the pit entry line outcome (includes time loss vs. pit limit when available). | Latched on pit entry line. | `PitEngine.UpdatePitEntryAssist` + `AttachCore`【F:PitEngine.cs†L452-L507】【F:LalaLaunch.cs†L3045-L3049】 |
| Pit.EntryLineTimeLoss_s | double | Time loss in seconds versus pit limit from the first compliant point to the pit entry line (0 if not computed). | Latched on pit entry line. | `PitEngine.UpdatePitEntryAssist` + `AttachCore`【F:PitEngine.cs†L452-L510】【F:LalaLaunch.cs†L3045-L3049】 |
| PitExit.DistanceM | int | Forward distance in metres to the stored pit exit marker (wraps at S/F). Returns 0 if data missing. | Per tick. | `LalaLaunch.cs` — `UpdatePitExitDisplayValues` + `AttachCore`【F:LalaLaunch.cs†L4216-L4248】【F:LalaLaunch.cs†L3068-L3069】 |
| PitExit.TimeS | int | Estimated time in seconds to pit exit using current speed (0 if speed too small or data missing). | Per tick. | `LalaLaunch.cs` — `UpdatePitExitDisplayValues` + `AttachCore`【F:LalaLaunch.cs†L4216-L4248】【F:LalaLaunch.cs†L3068-L3069】 |
| Pit.Box.DistanceM | int | Forward wrapped distance in metres to the player pit box using native player track percent + native `DriverPitTrkPct` + session track length authority. Publishes while in pit lane and also in a pit-owned pre-entry window (`PitPhase.EnteringPits` or fallback player track-percent band `[0.80..1.00] ∪ [0.00..0.20]`). Returns 0 outside that gate or when authority is invalid/missing. | Per tick while pit box visibility gate is active (otherwise `0`). | `PitEngine.cs` (`PlayerPitBoxTrackPct`, `CurrentPitPhase`, `PlayerTrackPercentNormalized`) + `LalaLaunch.cs` (`UpdatePitBoxDisplayValues`) + `AttachCore`. |
| Pit.Box.TimeS | int | Estimated seconds to the player pit box using current speed and `Pit.Box.DistanceM`. Uses conservative timing (`0` when speed is too low/invalid) and follows the same pit-owned pre-entry visibility gate as `Pit.Box.DistanceM`; returns `0` when authority is invalid/missing. | Per tick while pit box visibility gate is active (otherwise `0`). | `LalaLaunch.cs` — `UpdatePitBoxDisplayValues` + `AttachCore`. |
| Pit.Box.BrakeNow | bool | Plugin-owned BoxEntry “BRAKE NOW” visibility helper for dashboards. `true` only when pit-box authority is valid, speed is sane (`>2` kph), distance is positive and within dynamic trigger distance `25.0 * (pitLimitKph / 80.0)`, pit-limit authority is valid, and pit box visibility gate is active (in lane or allowed pre-entry window). Pit-limit authority chain: `DataCorePlugin.GameData.PitLimiterSpeed` then parsed `DataCorePlugin.GameRawData.SessionData.WeekendInfo.TrackPitSpeedLimit`. | Per tick (false when authority/gate conditions are not met). | `LalaLaunch.cs` — `TryResolvePitLimiterSpeedKph`, `UpdatePitBoxDisplayValues`, `AttachCore`. |

## Launch
| Exported name | Type | Units / meaning | Update cadence | Defined in |
| --- | --- | --- | --- | --- |
| ManualTimeoutRemaining | string | Seconds remaining on 30 s manual prime timeout (empty when inactive). | Per tick during launch states. | `LalaLaunch.cs` — `AttachCore` timeout block【F:LalaLaunch.cs†L2845-L2860】 |
| LaunchModeActive | bool | Whether launch UI is visible (primed/in-progress/logging/completed). | Per tick. | `LalaLaunch.cs` — launch state machine + `AttachCore`【F:LalaLaunch.cs†L2470-L2510】【F:LalaLaunch.cs†L2869-L2878】 |
| LaunchStateLabel / LaunchStateCode | string/string | Human-readable and numeric launch state tokens. | Per tick. | `LalaLaunch.cs` — launch state machine + `AttachCore`【F:LalaLaunch.cs†L2470-L2510】【F:LalaLaunch.cs†L2872-L2878】 |
| LaunchRPM / TargetLaunchRPM | double | Live and target RPMs for launch. | Per tick. | `LalaLaunch.cs` — launch metrics + `AttachCore`【F:LalaLaunch.cs†L2845-L2895】 |
| ActualRPMAtClutchRelease / RPMDeviationAtClutchRelease | string/string | RPM at clutch release and deviation vs. target. | Per launch event; surfaced per tick. | `LalaLaunch.cs` — launch metrics + `AttachCore`【F:LalaLaunch.cs†L2845-L2895】 |
| ActualThrottleAtClutchRelease / ThrottleDeviationAtClutchRelease | double/double | Throttle at release and deviation vs. target. | Per launch event; surfaced per tick. | `LalaLaunch.cs` — launch metrics + `AttachCore`【F:LalaLaunch.cs†L2845-L2895】 |
| TargetLaunchThrottle / OptimalThrottleTolerance | string/string | Target throttle % and tolerance from profile. | Per tick. | `LalaLaunch.cs` — profile-backed metrics + `AttachCore`【F:LalaLaunch.cs†L2845-L2895】 |
| OptimalBitePoint / OptimalBitePointTolerance | double/double | Profile bite point target/tolerance. | Per tick. | `LalaLaunch.cs` — profile metrics + `AttachCore`【F:LalaLaunch.cs†L2845-L2895】 |
| AvgSessionLaunchRPM / LastAvgLaunchRPM / LastLaunchRPM / LastMinRPM / MinRPM | string/double/double/double/double | Session launch RPM aggregates and last-run snapshots. | Per tick. | `LalaLaunch.cs` — launch metrics + `AttachCore`【F:LalaLaunch.cs†L2845-L2895】 |
| MaxTractionLoss | double | Max % traction loss during launch. | Per launch run; surfaced per tick. | `LalaLaunch.cs` — launch metrics + `AttachCore`【F:LalaLaunch.cs†L2845-L2895】 |
| ReactionTime / ClutchReleaseTime / ClutchReleaseDelta | double/string/string | Launch reaction/phase timing measurements. | Per launch run; surfaced per tick. | `LalaLaunch.cs` — launch metrics + `AttachCore`【F:LalaLaunch.cs†L2845-L2895】 |
| BitePointInTargetRange / RPMInTargetRange / ThrottleInTargetRange | bool | True when the respective control stayed within tolerance during launch. | Per launch run; surfaced per tick. | `LalaLaunch.cs` — launch metrics + `AttachCore`【F:LalaLaunch.cs†L2845-L2895】 |
| WheelSpinDetected / BoggedDown / BogDownFactorPercent | bool/bool/double | Spin/bog detection flags and profile bog factor. | Per tick. | `LalaLaunch.cs` — launch metrics + `AttachCore`【F:LalaLaunch.cs†L2845-L2895】 |
| AntiStallActive / AntiStallDetectedInLaunch | bool | Anti-stall detection live flag and launch-run latch. | Per tick. | `LalaLaunch.cs` — anti-stall logic + `AttachCore`【F:LalaLaunch.cs†L2845-L2895】 |
| TargetLaunchThrottle | string | Target throttle % from profile. | Per tick. | `LalaLaunch.cs` — profile metrics + `AttachCore`【F:LalaLaunch.cs†L2845-L2895】 |
| ThrottleModulationDelta | double | % modulation measurement during launch. | Per launch run; surfaced per tick. | `LalaLaunch.cs` — launch metrics + `AttachCore`【F:LalaLaunch.cs†L2845-L2895】 |
| ZeroTo100Time / ZeroTo100Delta | double | 0–100 km/h time and delta vs. baseline. | Per launch run. | `LalaLaunch.cs` — launch metrics + `AttachCore`【F:LalaLaunch.cs†L2845-L2895】 |
| FalseStartDetected | bool | Flag set when car moves before green while clutch released. | Per tick. | `LalaLaunch.cs` — false start detection + `AttachCore`【F:LalaLaunch.cs†L4989-L5015】【F:LalaLaunch.cs†L2807-L2811】 |

## Messages & Rejoin
| Exported name | Type | Units / meaning | Update cadence | Defined in |
| --- | --- | --- | --- | --- |
| MSG.OvertakeApproachLine | double | Relative line metric for approaching traffic. | Per tick. | `LalaLaunch.cs` — `_msgSystem` outputs + `AttachCore`【F:LalaLaunch.cs†L2899-L2940】 |
| MSG.OtherClassBehindGap | double | Seconds behind for the selected different-class car (or -1 when none); no `MSGOtherClassBehindGap` alias. | Per tick. | `LalaLaunch.cs` — `_msgSystem` outputs + `AttachCore`【F:LalaLaunch.cs†L2899-L2940】 |
| MSG.OvertakeWarnSeconds | double | Approach buffer seconds to warn (from profile). | Per tick. | `LalaLaunch.cs` — profile read + `AttachCore`【F:LalaLaunch.cs†L2899-L2940】 |
| MSG.MsgCxTimeMessage / MsgCxStateMessage / MsgCxActionMessage | string | Message text for time/state/action lanes. | Per tick. | `LalaLaunch.cs` — `_msgSystem` outputs + `AttachCore`【F:LalaLaunch.cs†L2899-L2940】 |
| MSG.MsgCxTimeVisible / MsgCxStateVisible | bool | Visibility flags for respective lanes. | Per tick. | `LalaLaunch.cs` — `_msgSystem` outputs + `AttachCore`【F:LalaLaunch.cs†L2899-L2940】 |
| MSG.MsgCxTimeSilenceRemaining | double | Remaining silence window for time lane. | Per tick. | `LalaLaunch.cs` — `_msgSystem` outputs + `AttachCore`【F:LalaLaunch.cs†L2899-L2940】 |
| MSG.MsgCxStateToken | string | Token controlling state lane reappearance. | Per tick. | `LalaLaunch.cs` — `_msgSystem` outputs + `AttachCore`【F:LalaLaunch.cs†L2899-L2940】 |
| MSG.MsgCxActionPulse | bool | One-shot trigger when action lane fires. | Per tick. | `LalaLaunch.cs` — `_msgSystem` outputs + `AttachCore`【F:LalaLaunch.cs†L2899-L2940】 |
| MSGV1.ActiveText_Lala / ActivePriority_Lala / ActiveMsgId_Lala / ActiveText_Msg / ActivePriority_Msg / ActiveMsgId_Msg | string | Active message payloads for Lala and Msg lanes. | Per tick while MSGV1 engine active. | `LalaLaunch.cs` — `_msgV1Engine.Outputs` + `AttachCore`【F:LalaLaunch.cs†L2919-L2940】【F:Messaging/MessageEngine.cs†L478-L560】 |
| MSGV1.ActiveTextColor_* / ActiveBgColor_* / ActiveOutlineColor_* / ActiveFontSize_* | string/int | Visual styling for active messages. | Per tick. | `Messaging/MessageEngine.cs` outputs + `AttachCore`【F:Messaging/MessageEngine.cs†L478-L560】【F:LalaLaunch.cs†L2919-L2940】 |
| MSGV1.ActiveCount | int | Count of active queued messages. | Per tick. | `Messaging/MessageEngine.cs` outputs + `AttachCore`【F:Messaging/MessageEngine.cs†L478-L560】【F:LalaLaunch.cs†L2919-L2940】 |
| MSGV1.LastCancelMsgId | string | ID of last cancelled message. | Per tick. | `Messaging/MessageEngine.cs` outputs + `AttachCore`【F:Messaging/MessageEngine.cs†L478-L560】【F:LalaLaunch.cs†L2919-L2940】 |
| MSGV1.ClearAllPulse | bool | One-shot clear-all pulse. | Per tick. | `Messaging/MessageEngine.cs` outputs + `AttachCore`【F:Messaging/MessageEngine.cs†L478-L560】【F:LalaLaunch.cs†L2919-L2940】 |
| MSGV1.StackCsv | string | CSV of active message IDs/priorities for debugging. | Per tick. | `Messaging/MessageEngine.cs` outputs + `AttachCore`【F:Messaging/MessageEngine.cs†L478-L560】【F:LalaLaunch.cs†L2919-L2940】 |
| MSGV1.MissingEvaluatorsCsv | string | CSV of evaluator IDs missing definitions. | On detection; surfaced per tick. | `Messaging/MessageEngine.cs` — `RegisterMissingEvaluators` + `AttachCore`【F:Messaging/MessageEngine.cs†L478-L560】 |
| RejoinAlertReasonCode / RejoinAlertReasonName / RejoinAlertMessage | int/string/string | Active rejoin logic code/name/message. | Per tick. | `LalaLaunch.cs` — `_rejoinEngine` outputs + `AttachCore`【F:LalaLaunch.cs†L2818-L2824】 |
| RejoinIsExitingPits | bool | Whether rejoin assist thinks you are exiting pits. | Per tick. | `RejoinAssistEngine` state + `AttachCore`【F:LalaLaunch.cs†L2819-L2823】【F:RejoinAssistEngine.cs†L90-L160】 |
| RejoinCurrentPitPhase / RejoinCurrentPitPhaseName | int/string | Current pit phase enum and label. | Per tick. | `RejoinAssistEngine` state + `AttachCore`【F:LalaLaunch.cs†L2819-L2825】【F:RejoinAssistEngine.cs†L90-L160】 |
| RejoinThreatLevel / RejoinThreatLevelName / RejoinTimeToThreat | int/string/double | Threat scoring and time-to-threat for rejoin assist. | Per tick. | `RejoinAssistEngine` outputs + `AttachCore`【F:LalaLaunch.cs†L2826-L2831】【F:RejoinAssistEngine.cs†L540-L640】 |
| MsgCxPressed | bool | Latched true for 500 ms after MsgCx action. | Per tick. | `LalaLaunch.cs` — `RegisterMsgCxPress` + `AttachCore`【F:LalaLaunch.cs†L2475-L2479】【F:LalaLaunch.cs†L2815-L2820】 |
| Debug.EventMarkerPressed | bool | Latched true for 500 ms after the Event Marker action. | Per tick. | `LalaLaunch.cs` — `RegisterEventMarkerPress` + `AttachCore`【F:LalaLaunch.cs†L2481-L2492】【F:LalaLaunch.cs†L2816-L2822】 |
| Debug.PropertySnapshotEnabled | int | Reflects Property Snapshot child toggle state (`1`/`0`) for dashboards/troubleshooting; capture still requires master `EnableSoftDebug`. | Per tick. | `LaunchPluginSettings.EnablePropertySnapshot` + `AttachCore`. |
| Debug.PropertySnapshot.RollingStatusText | string | Rolling snapshot status gate text: `OFF` (any gate off), `READY` (gates on + automation stopped), `RECORDING` (automation active). | Per tick. | `LalaLaunch.ResolvePropertySnapshotRollingStatusText()` + `AttachCore`. |
| Debug.PropertySnapshot.RollingModeText | string | Rolling mode text export from current mode setting: `MANUAL` / `FREQUENCY` / `PER LAP`. | Per tick. | `LalaLaunch.ResolvePropertySnapshotRollingModeText()` + `AttachCore`. |
| OfflineDataModule | int (`0`/`1`) | Plugin-owned offline-data module toggle state for dash visibility gating. Flipped by action `OfflineDataModule_Toggle`; not exposed in plugin bindings UI section. | Per tick. | `LaunchPluginSettings.OfflineDataModule` + `LalaLaunch.cs` `AddAction`/`AttachCore`. |

## Shift Assist

Shift Assist runtime/settings notes:
- `ShiftAssistUrgentEnabled` (bool, per-profile): enables/disables urgent secondary beep playback.
- `ShiftAssistShiftLightMode` (int, per-profile): Shift Light routing mode (`0=PrimaryOnly`, `1=UrgentOnly`, `2=Both`, default `2`).
- `ShiftAssistMuteInReplay` (bool, global Debug): when true (default), suppresses Shift Assist sound while replay is active; cue evaluation/lights/learning remain active.
- No separate urgent volume parameter is used.
- Urgent volume is derived at runtime as 50% of the main beep volume slider.

| Exported name | Type | Units / meaning | Update cadence | Defined in |
| --- | --- | --- | --- | --- |
| ShiftAssist.ActiveGearStackId | string | Active gear stack id read from `DataCorePlugin.GameRawData.SessionData.CarSetup.Chassis.GearsDifferential.GearStack` (falls back to `Default`). | Per tick. | `LalaLaunch.cs` — `EvaluateShiftAssist` + `AttachCore`. |
| ShiftAssist.TargetRPM_CurrentGear | int | Current gear target RPM resolved from active car profile + active gear stack; when profile target is `0`, falls back to `DataCorePlugin.GameData.CarSettings_CurrentGearRedLineRPM` if available (else `0`). | Per tick. | `LalaLaunch.cs` — `EvaluateShiftAssist` + `AttachCore`. |
| ShiftAssist.ShiftRPM_G1..ShiftAssist.ShiftRPM_G8 | int | Active-stack target RPM values for each source gear, exported directly for dash display and tuning overlays. | Per tick. | `LalaLaunch.cs` — per-gear target resolver + `AttachCore`. |
| ShiftAssist.EffectiveTargetRPM_CurrentGear | int | Effective target RPM after predictive lead-time adjustment, using the same chosen target source as `ShiftAssist.TargetRPM_CurrentGear` (profile target or redline fallback). | Per tick. | `ShiftAssistEngine.cs` predictive evaluator + `LalaLaunch.cs` export. |
| ShiftAssist.RpmRate | int | RPM/sec estimate used by predictive cueing; `0` when unavailable/guarded. | Per tick. | `ShiftAssistEngine.cs` predictive evaluator + `LalaLaunch.cs` export. |
| ShiftAssist.Beep | bool | Audio issued pulse. True only on ticks where `SoundPlayer` issue succeeds for either primary `TryPlayBeep()` or urgent `TryPlayBeepWithVolumeOverride()`. | Per tick (pulse). | `LalaLaunch.cs` — audio issue path in `EvaluateShiftAssist`/`TriggerShiftAssistTestBeep` + `AttachCore`. |
| ShiftAssist.ShiftLight | bool | Canonical combined Shift Light latch output selected by `ShiftAssistShiftLightMode` (`Primary`, `Urgent`, `Both`). | Per tick. | `LalaLaunch.cs` — Shift Light routing/latch update in `EvaluateShiftAssist` + `AttachCore`. |
| ShiftAssist.ShiftLightPrimary | bool | Canonical primary Shift Light latch output; true during duration window when a primary cue occurs and Shift Light is enabled. | Per tick. | `LalaLaunch.cs` — primary-cue latch in `EvaluateShiftAssist` + `AttachCore`. |
| ShiftAssist.ShiftLightUrgent | bool | Canonical urgent Shift Light latch output; true during duration window when an urgent cue occurs and Shift Light is enabled. | Per tick. | `LalaLaunch.cs` — urgent-cue latch in `EvaluateShiftAssist` + `AttachCore`. |
| ShiftAssist.BeepLight | bool | Legacy alias of `ShiftAssist.ShiftLight` kept for backward compatibility. | Per tick. | `LalaLaunch.cs` — alias export in `AttachCore`. |
| ShiftAssist.BeepPrimary | bool | Legacy alias of `ShiftAssist.ShiftLightPrimary` kept for backward compatibility. | Per tick. | `LalaLaunch.cs` — alias export in `AttachCore`. |
| ShiftAssist.BeepUrgent | bool | Legacy alias of `ShiftAssist.ShiftLightUrgent` kept for backward compatibility. | Per tick. | `LalaLaunch.cs` — alias export in `AttachCore`. |
| ShiftAssist.ShiftLightEnabled | int | `1` when Shift Light visibility/export is enabled in profile settings, else `0`; used by dashboards to gate ShiftAssist flash cues independently of audio. | Per tick. | `LalaLaunch.cs` settings bridge + `AttachCore`. |
| ShiftAssist.Learn.Enabled | int | Learning mode state exported as `1`/`0` for dash overlays and learning workflows. | Per tick. | `LalaLaunch.cs` settings bridge + `AttachCore`. |
| ShiftAssist.Learn.State | string | Learning state machine state (`Off`, `Armed`, `Sampling`, `Complete`, `Rejected`). | Per tick. | `ShiftAssistLearningEngine.cs` tick state + `LalaLaunch.cs` export. |
| ShiftAssist.Learn.ActiveGear | int | Source gear currently being sampled by learning mode (`0` when inactive). | Per tick. | `ShiftAssistLearningEngine.cs` tick output + `LalaLaunch.cs` export. |
| ShiftAssist.Learn.WindowMs | int | Elapsed learning collection window duration for the current passive sampling burst. | Per tick while active learning window exists. | `ShiftAssistLearningEngine.cs` + `LalaLaunch.cs` export. |
| ShiftAssist.Learn.PeakAccelMps2 / ShiftAssist.Learn.PeakRpm | double/int | Peak acceleration and RPM latched from passive learning sampling activity. | Per tick (latched between saves). | `LalaLaunch.cs` learning latch state + `AttachCore`. |
| ShiftAssist.Learn.LastSampleRpm | int | Last RPM sample captured by learning state machine this tick. | Per tick. | `ShiftAssistLearningEngine.cs` + `LalaLaunch.cs` export. |
| ShiftAssist.Learn.SavedPulse | bool | Short pulse when learning writes accepted targets back into the active stack profile row(s). | Per tick (pulse). | `LalaLaunch.cs` save pulse timer + `AttachCore`. |
| ShiftAssist.Learn.Samples_G1..ShiftAssist.Learn.Samples_G8 | int | Count of valid passive curve samples retained per source gear for active stack (sample progress `N`). | Per tick. | `ShiftAssistLearningEngine.cs` sample store + `LalaLaunch.cs` export. |
| ShiftAssist.Learn.LearnedRpm_G1..ShiftAssist.Learn.LearnedRpm_G8 | int | Adjacent-gear speed-domain solved learned shift RPM per source gear (`0` until curve coverage, ratio readiness, and stability are satisfied). | Per tick. | `ShiftAssistLearningEngine.cs` sample aggregation + `LalaLaunch.cs` export. |
| ShiftAssist.Learn.Locked_G1..ShiftAssist.Learn.Locked_G8 | bool | Active-stack per-gear lock state; locked gears are preserved during learning auto-apply/reset workflows. | Per tick. | `LalaLaunch.cs` active profile stack lock state + `AttachCore`. |
| ShiftAssist.State | string | Runtime evaluator state (`Off` / `On` / `NoData` / `Cooldown`). | Per tick. | `ShiftAssistEngine.cs` state machine + `LalaLaunch.cs` export. |
| ShiftAssist.Debug.AudioDelayMs | int | Most recent measured delay between trigger and `SoundPlayer` issue time (ms), clamped and session-local. | Per tick. | `LalaLaunch.cs` — `EvaluateShiftAssist` + `AttachCore`. |
| ShiftAssist.Debug.AudioDelayAgeMs | int | Age (ms) of `AudioDelayMs`; returns `-1` when no measurement exists yet this session. | Per tick. | `LalaLaunch.cs` — `GetShiftAssistAudioDelayAgeMs` + `AttachCore`. |
| ShiftAssist.Debug.AudioIssued | bool | Pulse true when an audio issue timestamp is captured this tick. | Per tick. | `LalaLaunch.cs` — `EvaluateShiftAssist` + `AttachCore`. |
| ShiftAssist.Debug.AudioBackend | string | Current playback backend identifier (`SoundPlayer`). | Per tick. | `LalaLaunch.cs` — `AttachCore`. |
| ShiftAssist.Debug.CsvEnabled | int | Reflects runtime debug CSV toggle state (`1`/`0`) for dashboard visibility and quick troubleshooting. | Per tick. | `LalaLaunch.cs` settings bridge + `AttachCore`. |
| ShiftAssist.DelayAvg_G1..ShiftAssist.DelayAvg_G8 | int | Runtime rolling average beep→upshift delay (ms) per source gear, over last 5 valid samples. | Per tick. | `LalaLaunch.cs` runtime delay tracker + `AttachCore`. |
| ShiftAssist.DelayN_G1..ShiftAssist.DelayN_G8 | int | Runtime sample count currently included in each per-gear rolling average (0..5). | Per tick. | `LalaLaunch.cs` runtime delay tracker + `AttachCore`. |
| ShiftAssist.Delay.Pending | int | `1` while a primary-beep delay capture window is armed awaiting a qualifying upshift sample; otherwise `0`. | Per tick. | `LalaLaunch.cs` pending delay tracker + `AttachCore`. |
| ShiftAssist.Delay.PendingGear | int | Source gear currently armed for pending delay capture (`0` when no capture is armed). | Per tick. | `LalaLaunch.cs` pending delay tracker + `AttachCore`. |
| ShiftAssist.Delay.PendingAgeMs | int | Elapsed milliseconds since pending delay capture was armed (`-1` when inactive). | Per tick. | `LalaLaunch.cs` pending delay timer helper + `AttachCore`. |
| ShiftAssist.Delay.PendingRpmAtCue | int | RPM captured at the moment the pending delay window was armed. | Per tick. | `LalaLaunch.cs` pending delay tracker + `AttachCore`. |
| ShiftAssist.Delay.RpmAtBeep | int | Last latched RPM at beep trigger time (primary beep path). | Per tick. | `LalaLaunch.cs` beep/delay diagnostics latch + `AttachCore`. |
| ShiftAssist.Delay.CaptureState | int | Encoded capture state for delay diagnostics (`0` none, `1` arm, `2` capture, `3` timeout, `4` cancelled/downshift). | Per tick. | `LalaLaunch.cs` delay diagnostics mapper + `AttachCore`. |
| ShiftAssist UI gear rows | n/a | Profile storage remains 8 slots, but UI only shows up-shiftable gears `1..(maxForwardGears-1)` where max forward gears comes from `DataCorePlugin.GameData.CarSettings_MaxGears`, then `DataCorePlugin.GameRawData.SessionData.DriverInfo.DriverCarGearNumForward`, then defaults to `8`. | On profile/UI refresh. | `ProfilesManagerViewModel.cs` — `ShiftAssistMaxTargetGears` + `ShiftGearRows`. |

## Session / Identity
| Exported name | Type | Units / meaning | Update cadence | Defined in |
| --- | --- | --- | --- | --- |
| Reset.LastSession / Reset.ThisSession | string | Last and current session tokens (`SessionID:SubSessionID`). | On session change; surfaced each poll. | `LalaLaunch.cs` — session token handling + `AttachCore`【F:LalaLaunch.cs†L3308-L3365】【F:LalaLaunch.cs†L2737-L2740】 |
| Reset.ThisSessionType | string | Cached session type used for finish timing. | On session change. | `LalaLaunch.cs` — finish timing reset + `AttachCore`【F:LalaLaunch.cs†L4566-L4715】【F:LalaLaunch.cs†L2737-L2740】 |
| LastSessionType | string | Last observed session type token (raw). | Per tick. | `LalaLaunch.cs` — DataUpdate session probe + `AttachCore`【F:LalaLaunch.cs†L3201-L3274】【F:LalaLaunch.cs†L2808-L2811】 |
| Race.OverallLeaderHasFinished / Race.ClassLeaderHasFinished | bool | Leader-finished latches (overall/class). Finish truth prefers resolved per-car `CarIdxSessionFlags` finish-like telemetry with checkered as the primary usable finish-like source in this path (crossed may be present but is not treated as a required line-crossing proof). Class-finish consumes the same class-leader seam as `ClassLeader.*`; overall-finish prefers true overall leader identity (`CarIdxPosition==1 && CarIdxClassPosition==1`) with position-only fallback. SessionState remains overall lifecycle authority; class-finish is not latched from SessionState alone in multiclass. Single-class keeps the existing overall->class mirror. Multiclass lifecycle fallback captures a dynamic finish-reference LapDistPct from overall leader at `SessionState 4->5` and resolves class finish from class-leader own prior->current reference crossing/wrap evidence (with guarded already-crossed transition evidence), not from class-vs-overall lap-count comparison. | Per tick; updated when flags/heuristics trip. | `LalaLaunch.cs` — `UpdateFinishTiming` + per-car flags helpers + `AttachCore`. |
| Race.EndPhase / Race.EndPhaseText / Race.EndPhaseConfidence | int / string / int | Race end-phase authority (`0 Unknown`, `1 Running`, `2 AfterZeroLeaderRunning`, `3 LeaderFinished`, `4 SessionComplete`) resolved from race `SessionState` (+ timed-race zero-remaining branch for phase 2). Confidence scale: `0 none`, `3 high` for authoritative state-resolved phases. | Per tick. | `LalaLaunch.cs` — `UpdateFinishTiming` phase resolver + `AttachCore`. |
| Race.LastLapLikely | bool | Dash behavior gate only (not finish proof): `true` when `SessionState==5` (any race type), or timed race with `SessionState==4` and `SessionTimeRemain<=0`; forced `false` when non-race/unknown/session-complete. | Per tick. | `LalaLaunch.cs` — `UpdateFinishTiming` + `AttachCore`. |
| Race.OverallLeaderHasFinishedValid / Race.ClassLeaderHasFinishedValid | bool | Validity flags for leader-finished values. | Per tick. | `LalaLaunch.cs` — `UpdateFinishTiming` + `AttachCore`【F:LalaLaunch.cs†L4566-L4790】【F:LalaLaunch.cs†L2809-L2816】 |
| RaceFinish.ClassSnapshotActive / RaceFinish.PlayerSnapshotActive / RaceFinish.Active | bool / bool / bool | Split finish snapshot stage flags. Class snapshot captures when class winner finishes; player snapshot captures when player finishes (per-car finish-like flags and robust player-checkered seams from `GameData.Flag_Checkered` / `SessionFlagsDetails.IsCheckered*`, then `SessionState==6` last-resort fallback); `RaceFinish.Active` is true when either stage is active. Both stages reset when leaving post-finish lifecycle. | Per tick. | `LalaLaunch.cs` — split `RaceFinish` capture helpers + `ResetRaceFinishSnapshot` + `AttachCore`. |
| RaceFinish.PlayerOverallPosition / RaceFinish.PlayerClassPosition | int / int | Player overall/class position for finish widgets. Neutral defaults before class snapshot (`0`), live values only while class snapshot is active and player snapshot is pending, then frozen at **player snapshot** time. Class position prefers native telemetry `PlayerCarClassPosition`, then existing effective class-position publish seam (League-aware when resolved, native fallback otherwise). Player snapshot freeze is deferred until class position is positive (except `SessionState==6` safety fallback), preventing `class=0` freeze when valid class position resolves shortly after finish trigger. | Neutral pre-class-snapshot, live-while-pending, then player one-shot capture + held. | `LalaLaunch.cs` — `ResolveRaceFinishLiveOverallPosition`, `ResolveRaceFinishLiveClassPosition`, `TryCaptureRaceFinishPlayerSnapshot`, `GetEffectivePositionInClassForPublishedContext`, `AttachCore`. |
| Race.FieldSize / Race.PlayerClassFieldSize | int / int | Live plugin-owned field-size exports for race and player effective/native class cohorts. Counts use current-session `DriverInfo.Drivers##` roster rows (no `CompetingDrivers` fallback), explicitly exclude pace car rows, and avoid on-track-only drift. `Race.PlayerClassFieldSize` uses canonical denominator resolution with strict League ON subclass counting from `DriverInfo.Drivers##` (resolved via League resolver) before native/session telemetry fallbacks (native fallback class matching prefers class ID with name fallback), and explicitly does **not** use CSV registered class-membership counts as denominator fallback. `GameData.OpponentsCount` is consumed as race field size (no `+1`). | Per tick (live). | `LalaLaunch.cs` — `ResolveLiveOverallFieldSize`, `ResolveLivePlayerClassFieldSize`, `ResolveCanonicalPlayerClassRaceDenominator`, roster helpers, `AttachCore`. |
| Opp.* / PitExit.* identity source | internal contract | Opponents live normal-driver rows are sourced from current-session `DriverInfo.Drivers##.*` plus CarIdx telemetry only. `DriverInfo.CompetingDrivers[*]` is not used as a normal-driver fallback, and non-player `Drivers##` Pace Car rows are excluded via explicit pace-car metadata/identity markers before they can enter `NativeRaceModel.Rows`, `Opp.Ahead1`/`Opp.Behind1`, H2HRace selectors, or PitExit prediction. The filter never excludes solely by `CarIdx`, preserving `PlayerCarIdx == 0`; skipped Pace Car `CarIdx`/identity evidence clears matching blink-continuity holds, and a pace-flagged player row is preserved with a one-time anomaly warning. | Per tick (live). | `Opponents.cs` — `NativeSnapshot.Build`, `BuildRowFromDriver`, `NativeRaceModel.Build`, `PitExitPredictor.Update`. |
| RaceFinish.PlayerOverallFieldSize / RaceFinish.PlayerClassFieldSize | int / int | Player finish denominator exports for dash `Pxx / yy` rendering. Neutral defaults before class snapshot (`0`). Denominators freeze at class snapshot activation and stay held through player snapshot/session end to prevent quitter/dropout denominator drift. Overall/class denominators are sourced from live plugin-owned `Race.FieldSize` / `Race.PlayerClassFieldSize` canonical denominator seams at class snapshot time; with League ON the class denominator freeze reflects strict current-session `DriverInfo.Drivers##` subclass cohort when available (never CSV membership), and class denominator may refresh while class snapshot is active and player snapshot is pending only when initially invalid (`0`) and later becomes valid, without changing snapshot trigger semantics. | Neutral pre-class-snapshot, class one-shot capture + bounded pending refresh + held. | `LalaLaunch.cs` — `ResolveLiveOverallFieldSize`, `ResolveLivePlayerClassFieldSize`, `ResolveCanonicalPlayerClassRaceDenominator`, `TryCaptureRaceFinishClassSnapshot`, pending refresh helper, `AttachCore`. |
| RaceFinish.PlayerFuelLeft | double | Player fuel for finish widgets. Neutral default before class snapshot (`0`), live while player snapshot is pending, then frozen at player snapshot capture tick. | Neutral pre-class-snapshot, live-while-pending, then player one-shot capture + held. | `LalaLaunch.cs` — `ResolveRaceFinishLiveFuelLeft`, `TryCaptureRaceFinishPlayerSnapshot` + `AttachCore`. |
| RaceFinish.PlayerBestLap / RaceFinish.PlayerBestLapSec | string / double | Player best lap for finish widgets. Neutral defaults before class snapshot (`"-"` / `0`), live from existing runtime player best-lap seam (`_lastSeenBestLap` with native cache fallback) while player snapshot pending, then frozen at player snapshot time. | Neutral pre-class-snapshot, live-while-pending, then player one-shot capture + held. | `LalaLaunch.cs` — `ResolveBestLapLiveSeconds`, `FormatBestLapLiveOrDash`, `TryCaptureRaceFinishPlayerSnapshot` + `AttachCore`. |
| RaceFinish.ClassWinnerName / RaceFinish.ClassWinnerAbbrevName | string / string | Frozen class winner identity captured at class snapshot activation. Class snapshot activation requires a valid class-leader identity payload (live or held-last-valid) in normal flow; if identity is still unavailable, capture retries each tick and only `SessionState==6` fallback may capture without identity as last resort. | Class one-shot capture + held. | `LalaLaunch.cs` — `TryCaptureRaceFinishClassSnapshot` + class-leader seams + `AttachCore`. |
| RaceFinish.PlayerFinishGapSec / RaceFinish.ClassWinnerGapSec | double / double | Gap timer from class winner finish to player finish. Before class snapshot: `0`. After class snapshot and before player snapshot: live elapsed seconds since class winner finish. At player snapshot: frozen final elapsed value. `ClassWinnerGapSec` mirrors `PlayerFinishGapSec` for compatibility. | Live-while-pending then frozen. | `LalaLaunch.cs` — split `RaceFinish` capture/update logic + `AttachCore`. |
| RaceFinish.ClassBestLap / RaceFinish.ClassBestLapSec | string / double | Frozen class session-best lap captured at **player snapshot** time so final player lap can be included. Defaults to `"-"` / `0` when unresolved. | Player one-shot capture + held. | `LalaLaunch.cs` — `TryCaptureRaceFinishPlayerSnapshot` + `ClassBestBestLapTime*` seam + `AttachCore`. |
| ClassLeader.Valid / ClassLeader.CarIdx | bool / int | Plugin-owned current player-class leader state for live opponent sessions (`Practice`, `Open Qualify`, `Lone Qualify`, `Qualifying`, `Race`). Shared leader seam: when League Class is enabled and the player effective class resolves valid (including manual override), leader selection uses the resolver-backed effective-class cohort and the lowest positive `CarIdxPosition`; the player car self-matches so a player leading the effective class resolves as leader. When League Class is disabled or the player effective class is unresolved, native fallback is unchanged (`HasMultipleClassOpponents=false` overall leader, native multiclass player-class matching otherwise). Includes bounded transient-miss tolerance (short hold) to avoid one-frame clear/flicker when resolver inputs churn; sustained unresolved state still clears to invalid defaults. | Per tick. | `LalaLaunch.cs` — `UpdateClassLeaderExports`, `FindResolvedClassLeaderCarIdx`, transient hold logic, `AttachCore`. |
| ClassLeader.Name / ClassLeader.AbbrevName / ClassLeader.CarNumber | string | Identity fields for the resolved class leader car index from native/session-info seams (`DriverInfo.Drivers##` identity helpers). Empty when unresolved. | Per tick. | `LalaLaunch.cs` — `TryGetCarIdentityFromSessionInfo`, `TryGetCarDriverInfo`, `UpdateClassLeaderExports`, `AttachCore`. |
| ClassLeader.BestLapTimeSec / ClassLeader.BestLapTime | double / string | Best lap of the resolved class leader car as seconds and formatted lap text. Fails safe to `0` and `"-"` when unresolved/invalid. | Per tick. | `LalaLaunch.cs` — `_carSaBestLapTimeSecByIdx` cache + `UpdateClassLeaderExports` + `AttachCore`. |
| ClassLeader.GapToPlayerSec | double | Gap magnitude from player to class leader using existing preferred live-gap semantics: checkpoint-time CarSA seam (`TryGetCheckpointGapSec`) when sane (`abs <= 30s`), otherwise native progress/pace fallback (`|Δlaps| * paceRef`). Fails safe to `0` when unresolved. | Per tick. | `LalaLaunch.cs` — `ResolveClassGapToPlayerSec` + `UpdateClassLeaderExports` + `AttachCore`. |
| ClassBest.Valid / ClassBest.CarIdx | bool / int | Plugin-owned current player-class **session-best lap holder** state for live opponent sessions (`Practice`, `Open Qualify`, `Lone Qualify`, `Qualifying`, `Race`). Shared class-best seam is the same one used by `H2H*.ClassSessionBestLapSec` and session-best-in-class coloring: `HasMultipleClassOpponents=false` uses whole-field best lap holder; `HasMultipleClassOpponents=true` uses race-context class cohort matching (League effective class when enabled+valid via resolver seam; native player-class matching fallback otherwise). `Valid=false` with `CarIdx=-1` when unresolved. | Per tick. | `LalaLaunch.cs` — `TryResolveClassSessionBestLap`, `FindResolvedClassBestCarIdx`, `UpdateClassBestExports`, `AttachCore`. |
| ClassBest.Name / ClassBest.AbbrevName / ClassBest.CarNumber | string | Identity fields for the resolved class-best holder car index from existing identity helpers (`DriverInfo.Drivers##` + `TryGetCarDriverInfo` abbrev seam). Empty when unresolved. | Per tick. | `LalaLaunch.cs` — `TryGetCarIdentityFromSessionInfo`, `TryGetCarDriverInfo`, `UpdateClassBestExports`, `AttachCore`. |
| ClassBest.BestLapTimeSec / ClassBest.BestLapTime | double / string | Best valid lap of the resolved class-best holder as seconds and formatted lap text. Fails safe to `0` and `"-"` when unresolved/invalid. | Per tick. | `LalaLaunch.cs` — `_carSaBestLapTimeSecByIdx` cache + `UpdateClassBestExports` + `AttachCore`. |
| ClassBest.GapToPlayerSec | double | Gap magnitude from player to class-best holder using the same preferred live-gap semantics as `ClassLeader.GapToPlayerSec`: checkpoint-time CarSA seam (`TryGetCheckpointGapSec`) when sane (`abs <= 30s`), otherwise native progress/pace fallback (`|Δlaps| * paceRef`). Fails safe to `0` when unresolved. | Per tick. | `LalaLaunch.cs` — `ResolveClassGapToPlayerSec` + `UpdateClassBestExports` + `AttachCore`. |

## Dash control & visibility
| Exported name | Type | Units / meaning | Update cadence | Defined in |
| --- | --- | --- | --- | --- |
| CurrentDashPage | string | Current dash page set by `ScreenManager`. | Per tick. | `LalaLaunch.cs` — `Screens` state + `AttachCore`【F:LalaLaunch.cs†L2805-L2810】【F:LalaLaunch.cs†L3688-L3730】 |
| DashControlMode | string | Dash control mode (“manual”/“auto”). | Per tick. | `LalaLaunch.cs` — `Screens.Mode` + `AttachCore`【F:LalaLaunch.cs†L2805-L2810】【F:LalaLaunch.cs†L3688-L3730】 |
| DeclutterMode | int | Dash declutter mode (0/1/2) cycled by the Declutter/Secondary dash action; used for visibility bindings. | Per tick. | `LalaLaunch.cs` — action handler + `AttachCore`【F:LalaLaunch.cs†L60-L108】【F:LalaLaunch.cs†L3278-L3449】 |
| LalaLaunch.Dash.DarkMode.Mode | int | Dark Mode selector (`0` Off, `1` Manual, `2` Auto). Note: internal `AttachCore` key is `Dash.DarkMode.Mode`; SimHub-visible parameter is `LalaLaunch.Dash.DarkMode.Mode`. | Per tick. | `LalaLaunch.cs` dark mode evaluation + `AttachCore`. |
| LalaLaunch.Dash.DarkMode.Active | bool | Final Dark Mode active state after Lovely/manual/auto rules. When `Use Lovely True Dark` is enabled and Lovely is available, Lovely state drives `Active` even if Mode is `Off`. Note: internal `AttachCore` key is `Dash.DarkMode.Active`; SimHub-visible parameter is `LalaLaunch.Dash.DarkMode.Active`. | Per tick. | `LalaLaunch.cs` `EvaluateDarkMode` + `AttachCore`. |
| LalaLaunch.Dash.DarkMode.BrightnessPct | int | Brightness percent (`0..100`, 100 brightest). When Lovely drives Dark Mode, always exports the user slider value (no auto scaling). Without Lovely driving, exports effective auto-scaled brightness when active, otherwise the user slider value. Note: internal `AttachCore` key is `Dash.DarkMode.BrightnessPct`; SimHub-visible parameter is `LalaLaunch.Dash.DarkMode.BrightnessPct`. | Per tick. | `LalaLaunch.cs` `EvaluateDarkMode` + `AttachCore`. |
| LalaLaunch.Dash.DarkMode.LovelyAvailable | bool | True when Lovely is detected/usable (same runtime signal that enables the “Use Lovely True Dark” setting). Note: internal `AttachCore` key is `Dash.DarkMode.LovelyAvailable`; SimHub-visible parameter is `LalaLaunch.Dash.DarkMode.LovelyAvailable`. | Per tick. | `LalaLaunch.cs` `EvaluateDarkMode` + `AttachCore`. |
| LalaLaunch.Dash.DarkMode.OpacityPct | int | Final dim-overlay opacity percent (`0..100`) after brightness is finalized; forced to `0` when dark mode is inactive, otherwise `clamp(100 - BrightnessPct, 0, 100)`. Note: internal `AttachCore` key is `Dash.DarkMode.OpacityPct`; SimHub-visible parameter is `LalaLaunch.Dash.DarkMode.OpacityPct`. | Per tick. | `LalaLaunch.cs` `EvaluateDarkMode` + `AttachCore`. |
| LalaLaunch.Dash.DarkMode.ModeText | string | Dark Mode label derived from `Mode` (`"Off"`, `"Manual"`, `"Auto"`). Purely reflects configured mode and does not change when Lovely drives `Active`. Note: internal `AttachCore` key is `Dash.DarkMode.ModeText`; SimHub-visible parameter is `LalaLaunch.Dash.DarkMode.ModeText`. | Per tick. | `LalaLaunch.cs` `EvaluateDarkMode` + `AttachCore`. |
| PitScreenActive | bool | Whether pit screen is currently shown. | Per tick. | `LalaLaunch.cs` — pit screen state + `AttachCore`【F:LalaLaunch.cs†L3732-L3878】【F:LalaLaunch.cs†L3158-L3162】 |
| PitScreenMode | string | Pit screen mode (`auto` or `manual`). | Per tick. | `LalaLaunch.cs` — pit screen state + `AttachCore`【F:LalaLaunch.cs†L3837-L3878】【F:LalaLaunch.cs†L3158-L3162】 |
| Pit.Command.DisplayText | string | Short-lived user-facing pit/custom-message feedback text (for example `Fuel +1`, `Fuel MAX`, `Tyres OFF`, a custom-message label, or specific warning text). Failure warning texts are `PIT CMD WINDOW FAIL`, `PIT CMD CHAT FAIL`, `PIT CMD SEND FAIL`, `PIT CMD CONFIRM FAIL`, and reserved `PIT CMD TIMEOUT FAIL`; current timeout expiry paths do not emit the reserved timeout text unless a true timeout failure publish path is added. | Per tick while active message window. | `PitCommandEngine.cs` feedback publisher + `LalaLaunch.cs` `AttachCore`. |
| Pit.Command.Active | bool | `true` while `Pit.Command.DisplayText` is active; otherwise `false`. Every feedback publish restarts the active window (restartable pulse), including repeated identical text; current hold is 3000 ms from publish. While active, lower-severity incoming feedback is suppressed and does not reset hold. | Per tick. | `PitCommandEngine.cs` message timer + `LalaLaunch.cs` `AttachCore`. |
| Pit.Command.Severity | int | Pit feedback severity level (`0=None`, `1=Info`, `2=Advisory`, `3=Caution`, `4=Warning`) published with the same feedback lifecycle as `DisplayText/Active`. Priority rule: while active, equal/higher severity replaces + restarts hold; lower severity is suppressed. Dash styling contract: `0` no display; `1` black/white (no blink); `2` black/cyan (no blink); `3` yellow/red (no blink); `4` red/yellow (blink 1s at 750ms). | Per tick. | `PitCommandEngine.cs` severity owner + `LalaLaunch.cs` `AttachCore`. |
| Pit.Command.SeverityText | string | Text label mirror of `Pit.Command.Severity` (`None`, `Info`, `Advisory`, `Caution`, `Warning`). | Per tick. | `PitCommandEngine.cs` severity owner + `LalaLaunch.cs` `AttachCore`. |
| Pit.Command.LastAction | string | Last fired pit command action id for troubleshooting. | On action fire; surfaced each tick. | `PitCommandEngine.cs` action audit state + `LalaLaunch.cs` `AttachCore`. |
| Pit.Command.LastRaw | string | Last raw command/message text submitted by the caller before final transport normalization (for example `#fuel +500$` on raw pit-command path, `#...$` on built-in actions, or custom plain text). | On action fire; surfaced each tick. | `PitCommandEngine.cs` command mapping/normalization + `LalaLaunch.cs` `AttachCore`. |
| Pit.Command.FuelSetMaxToggleState | bool | Plugin-owned `Pit.FuelSetMax` behavioral toggle phase. Flips on every `Pit.FuelSetMax` press (even if transport later fails). `true` means the last press selected **MAX** send (`#fuel +150`); `false` means the last press selected **ZERO** send (`#fuel 0.01`). Tank-full short-circuit applies to MAX phase only; ZERO phase is always allowed through transport. This export therefore also indicates what the next press is armed to send (`false -> next MAX`, `true -> next ZERO`). | On `Pit.FuelSetMax` action fire; surfaced each tick. | `PitCommandEngine.cs` toggle state + `LalaLaunch.cs` `AttachCore`. |
| Pit.FuelControl.Data | int | **Active DATA authority state** (`0=LIVE`, `1=PEND`, `2=SAVED`, `3=SIMH`, `4=DFALT`, `5=FAIL`). This export reports the actual active runtime authority basis in use, not just the selected control mode. | Per tick. | `LalaLaunch.cs` active authority resolver + `AttachCore`. |
| Pit.FuelControl.DataText | string | Text mirror of active DATA authority (`LIVE`/`PEND`/`SAVED`/`SIMH`/`DFALT`/`FAIL`). | Per tick. | `LalaLaunch.cs` active authority resolver + `AttachCore`. |
| Pit.FuelControl.DataColor | string | Active DATA authority colour (`LIVE=#FF00FF`, `PEND=#FFFF00`, `SAVED=#00FFFF`, `SIMH=#FFA500`, `DFALT=#FFA500`, `FAIL=#FF0000`). | Per tick. | `LalaLaunch.cs` active authority resolver + `AttachCore`. |
| Pit.FuelControl.Source | int | Plugin-owned pit fuel source selector (`0` PUSH, `1` NORM, `2` SAVE, `4` STBY). `SOURCE=PLAN` is retired and is no longer emitted. Reset/cancel/suppressed states force `Source=STBY` for truthful ownership display. OFF hard guard: while effective mode is `OFF`, source actions (`SourceCycle`, `SetPush/SetNorm/SetSave`) send nothing and hold `OFF STBY`. Source-cycle contract is `STBY -> NORM -> PUSH -> SAVE -> STBY`; landing on `STBY` sends no fuel command. | Per tick. | `PitFuelControlEngine.cs` state machine + `LalaLaunch.cs` `AttachCore`. |
| Pit.FuelControl.SourceText | string | Short source label (`PUSH`/`NORM`/`SAVE`/`STBY`). `PLAN` is no longer a source label. | Per tick. | `PitFuelControlEngine.cs` source text mapping + `LalaLaunch.cs` `AttachCore`. |
| Pit.FuelControl.Mode | int | Effective pit fuel mode (`0` OFF, `1` MAN, `2` AUTO). Only `AUTO` is plugin-owned; outside AUTO, mode is MFD truth (`Telemetry.dpFuelFill`: `false => OFF`, `true => MAN`). `ModeCycle`: `OFF -> MAN` explicitly sends `#fuel$` and then mirrors `MAN STBY`; `MAN -> AUTO` sends only for `PUSH/NORM/SAVE` (`STBY` becomes `AUTO STBY` without send); `AUTO -> OFF` always attempts `#-fuel$`, with send failure leaving AUTO unchanged and success mirroring OFF truth. External MFD/fuel changes are monitor+mirror only (no plugin send): AUTO publishes `AUTO REFUEL CANCELLED BY MFD`; MAN/OFF publish `REFUEL SET OFF BY MFD`, `REFUEL SET ON BY MFD`, or `FUEL CHANGED BY MFD`, with mirrored OFF/MAN STBY state. MAN-owned Fuel Control sends are consumed as owned mirror echoes so delayed telemetry is not treated as external takeover. iRacing AutoFuel ownership and lifecycle suppress/reset seams keep disarmed STBY behavior. | Per tick. | `PitFuelControlEngine.cs` effective-mode resolver + `LalaLaunch.cs` snapshot/exports via `AttachCore`. |
| Pit.FuelControl.ModeText | string | Short mode label (`OFF`/`MAN`/`AUTO`). | Per tick. | `PitFuelControlEngine.cs` mode text mapping + `LalaLaunch.cs` `AttachCore`. |
| Pit.FuelControl.TargetLitres | double | Current plugin-computed target liters for the selected source before command send; send path rounds up to whole liters. Source contract: `NORM` uses runtime/live requirement-minus-current (`-Fuel.Delta.LitresCurrent`) regardless of DATA; `PUSH/SAVE` use live tactical deltas when DATA is `LIVE`, or planner/profile memory push/save assumptions when DATA is `SAVED` and available (fallback keeps existing live behavior). All targets apply non-negative clamp (`max(0, shortfall)`) and are capped to valid runtime max tank authority when available. Because tactical deltas are already contingency-protected on the required-to-finish side, pit fuel control targets must not add contingency again. | Per tick. | `PitFuelControlEngine.cs` target resolver + `LalaLaunch.cs` snapshot builder + `AttachCore`. |
| Pit.FuelControl.OverrideActive | bool | `true` when MAX override is armed/active for active fuel sources (hysteresis: arm `>1.10` stops required, disarm `<1.05`). Never active while `SOURCE=STBY`. | Per tick. | `PitFuelControlEngine.cs` override hysteresis + `LalaLaunch.cs` `AttachCore`. |
| Pit.FuelControl.TargetText | string | Dash-facing rounded target text companion for `TargetLitres` with explicit max-cap feedback (`"0L"`, `"41L"`, `"87L MAX"`). Text-only export; numeric contract remains in `TargetLitres`. | Per tick. | `PitFuelControlEngine.cs` + `LalaLaunch.cs` + `AttachCore`. |
| Pit.FuelControl.Fault | int | Diagnostic selector fault state for dash-color use only (`0=None`, `1=Mode fault`, `2=Source/request fault`, `3=Mode + Source/request fault`). Fault evaluation is suppressed during Fuel Control post-send settle/suppression windows and in suppressed/invalid snapshot contexts. No command/retry/correction is triggered from this export. | Per tick. | `PitFuelControlEngine.cs` fault composition + `LalaLaunch.cs` `AttachCore`. |
| Pit.TyreControl.Mode | int | Plugin-owned pit tyre control mode (`0` OFF, `1` DRY, `2` WET, `3` AUTO). Command model is one-shot raw-command ownership with combined tyre service+compound targets (`OFF => #cleartires$`, `DRY => #t tc 0$`, `WET => #t tc 2$`; transport normalizes trailing `$`). A plugin-issued tyre command starts a 1.0s settle hold. Plugin-driven actions use CHANGE wording (`TYRE CHANGE OFF/DRY/WET/AUTO`). Outside AUTO, once settle ends, mode mirrors known MFD truth only (`service OFF -> OFF`, `service ON + requested compound 0 -> DRY`, `service ON + requested compound 1 -> WET`) and never sends corrective commands for manual pit-menu edits; passive mirror wording is state-only (`TYRE OFF` / `TYRE DRY` / `TYRE WET`) and only on actual mirrored mode changes. Requested-compound telemetry mapping is intentionally independent from outgoing command values. Unknown/ambiguous truth is held fail-safe (no remap/send). AUTO sends at most one correction command when known truth mismatches declared-wet target (`false => DRY`, `true => WET`) with `TYRE AUTO CHANGE DRY/WET` feedback. In AUTO steady state, manual takeover (truth change away from AUTO desired target after settle) cancels AUTO and remaps to MFD truth with `TYRE AUTO CANCELLED`; no fight-back command is sent. Raw-send failure feedback is owned by the raw command engine, preserving specific `PIT CMD WINDOW FAIL`, `PIT CMD CHAT FAIL`, or `PIT CMD SEND FAIL` text (no timeout retry/fail loop); the broader pit feedback contract also reserves `PIT CMD CONFIRM FAIL` and `PIT CMD TIMEOUT FAIL` for distinguishable command-engine failures. Mode still resets to OFF on `Telemetry.IsOnTrackCar` edge transitions and other existing pit-control reset paths. | Per tick. | `PitTyreControlEngine.cs` + `LalaLaunch.cs` on-track reset seam + `AttachCore`. |
| Pit.TyreControl.ModeText | string | Short mode label (`OFF`/`DRY`/`WET`/`AUTO`). | Per tick. | `PitTyreControlEngine.cs` mode text mapping + `LalaLaunch.cs` `AttachCore`. |
| Pit.TyreControl.Fault | int | Diagnostic selector fault state for dash-color use only (`0=None`, `1=Mode fault`, `2=Source/request fault`, `3=Mode + Source/request fault`). Evaluation is disabled during tyre post-send settle and while tyre truth is unknown/unavailable (including AUTO initial pending-unknown state). No command/retry/correction is triggered from this export. | Per tick. | `PitTyreControlEngine.cs` fault composition + `LalaLaunch.cs` `AttachCore`. |
| LalaDashShowLaunchScreen / LalaDashShowPitLimiter / LalaDashShowPitScreen / LalaDashShowRejoinAssist / LalaDashShowVerboseMessaging / LalaDashShowRaceFlags / LalaDashShowRadioMessages / LalaDashShowTraffic | bool | User visibility toggles for Lala dash. | Per tick. | `LaunchPluginSettings` persisted values + `AttachCore`【F:LalaLaunch.cs†L3177-L3185】 |
| MsgDashShowLaunchScreen / MsgDashShowPitLimiter / MsgDashShowPitScreen / MsgDashShowRejoinAssist / MsgDashShowVerboseMessaging / MsgDashShowRaceFlags / MsgDashShowRadioMessages / MsgDashShowTraffic | bool | User visibility toggles for messaging dash. | Per tick. | `LaunchPluginSettings` persisted values + `AttachCore`【F:LalaLaunch.cs†L3187-L3195】 |
| OverlayDashShowLaunchScreen / OverlayDashShowPitLimiter / OverlayDashShowPitScreen / OverlayDashShowRejoinAssist / OverlayDashShowVerboseMessaging / OverlayDashShowRaceFlags / OverlayDashShowRadioMessages / OverlayDashShowTraffic | bool | User visibility toggles for overlay dash. | Per tick. | `LaunchPluginSettings` persisted values + `AttachCore`【F:LalaLaunch.cs†L3197-L3205】 |

Strategy Dash binding note: `StrategyDash.ModeToggle` is a plugin-owned action for Controls & Events / Dash Studio. It toggles the persisted `StrategyDash.AdvancedMode` status only; dashboards use that status for Advanced/Simple presentation and must not move strategy/fuel calculations into dash-side scripts.
Fuel burn analysis binding note: `BurnDisplayToggle`, `BurnAnalysisResetAverages`, `BurnAnalysisResetCurrentStint`, `BurnAnalysisResetSessionAverage`, `BurnAnalysisResetMaxObserved`, and `BurnAnalysisResetMinObserved` are plugin-owned actions for Controls & Events / Dash Studio. They drive only `Fuel.Burn.DisplayAnalysis` presentation state or the explicitly scoped `Fuel.Burn.Analysis.*` backing group; max/min observed resets are independent, and none alter accepted-lap gating, stable fuel, predictor, Strategy planner, or pit/refuel math.

Dark Mode binding warning: When `Use Lovely True Dark` is enabled and Lovely is available, Lovely controls `DarkMode.Active` even if Mode is `Off`, and `BrightnessPct` stays on the user slider value (no auto scaling while Lovely drives). `OpacityPct` remains the final dim value published to dashes. `ModeText` remains tied to `Mode` only (`Off`/`Manual`/`Auto`). `LalaLaunch.ToggleDarkMode` is ignored to prevent conflicting toggles. Do not bind both Lovely and LalaLaunch dark-mode toggles to the same physical input.

Settings persistence note (not runtime SimHub exports): `MonitorSystemEnabled` gates dashboard-facing MonitorSystem publication and `EnableMonitorEventCsv` gates the debug-only `MonitorSystem_Events.csv` writer; neither field is a SimHub export. `PitCommandTransportMode` (`0=Auto`, `1=LegacyForegroundSendInput`, `2=DirectMessageOnly`) is retained only as ignored legacy persisted data so old settings load safely; pit/custom dispatch is fixed to direct window-message transport and Settings no longer shows a transport selector. `PitCommandsAutoFocusPreview` (legacy persisted field; no longer shown in Settings UI) and `CustomMessages[1..10].Name/MessageText` are persisted UI/settings fields in `LaunchPluginSettings` used by Settings-tab editors and custom-message dispatch. Custom-message slot collection changes still save immediately, while slot `Name`/`MessageText` typing is debounced (500 ms settle window) before writing settings; pending debounced saves are flushed during normal tick/shutdown so latest settled text persists across restarts. These settings fields are intentionally not attached via `AttachCore/AttachVerbose`.

Pit action surface note (Controls & Events): plugin-owned pit actions include `Pit.ClearAll`, `Pit.ClearTires`, `Pit.ToggleFuel`, `Pit.FuelSetZero`, `Pit.FuelAdd1`, `Pit.FuelRemove1`, `Pit.FuelAdd10`, `Pit.FuelRemove10`, `Pit.FuelSetMax`, `Pit.ToggleTiresAll`, `Pit.ToggleFastRepair`, `Pit.ToggleAutoFuel`, `Pit.Windshield`, `Pit.FuelControl.SourceCycle`, `Pit.FuelControl.ModeCycle`, `Pit.FuelControl.SetPush`, `Pit.FuelControl.SetNorm`, `Pit.FuelControl.SetSave`, `Pit.FuelControl.SetDataLive`, `Pit.FuelControl.SetDataPlan`, `Pit.FuelControl.CycleData`, legacy `Pit.FuelControl.SetPlan`, `Pit.TyreControl.ModeCycle`, `Pit.TyreControl.SetOff`, `Pit.TyreControl.SetDry`, `Pit.TyreControl.SetWet`, and `Pit.TyreControl.SetAuto`. League Class control surface now also includes plugin action `LeagueClass.ToggleEnabled` (toggles `Settings.LeagueClassEnabled`, applies existing enable-mode guard, reloads resolver state, and on enable performs a quiet CSV self-check reload in CSV-capable modes when CSV file exists but valid rows are not loaded; missing/empty path does not block enable). In plugin Settings → Pit Commands, tyre-control binding UI now intentionally exposes only `Pit.TyreControl.ModeCycle`; direct `SetOff/SetDry/SetWet/SetAuto` remain available in SimHub Controls & Events / Dash Studio. Pit Fuel Control binding UI exposes `Pit.FuelControl.CycleData` as `Fuel Data Cycle`, and canonical DATA actions (`SetDataLive`, `SetDataPlan`, `CycleData`). Custom-message actions are also plugin-owned (`CustomMessage01..CustomMessage10`), with text/labels authored in Settings → Custom Messages (not as dash-side raw chat literals).

## Friends
| Exported name | Type | Units / meaning | Update cadence | Defined in |
| --- | --- | --- | --- | --- |
| LalaLaunch.Friends.Count | int | Count of active friends entries used by the friends list UI (after cleanup of empty rows). | Per tick. | `LalaLaunch.cs` — friends cache + unqualified `Friends.Count` `AttachCore` registration【F:LalaLaunch.cs†L3280-L3334】 |

## Debug (verbose-only)
Verbose-only Pit/PitLite SimHub exports were removed in Debug Cleanup Phase A. Active pit/PitLite dashboard-facing exports remain in the Pit timing and PitLite section above.

## CSV exports (debug)
* `CarTrackingProbe_<Track>_<Timestamp>.csv` is a master-debug-gated Debug UI controlled diagnostic file, not a SimHub export/property. It writes player + Probe A/B raw CarIdx telemetry (`CarIdxLap`, `CarIdxLapCompleted`, `CarIdxLapDistPct`, track/pit/class/position/lap-time/flag arrays), `DriverInfo.Drivers##` identity/class fields only, player-vs-probe derived deltas, and read-only CarSA/Opp/H2H correlation fields. Slot-01 precision diagnostics include TrackSec authority, raw checkpoint candidate, best reconciled ±lap candidate, tolerance, candidate/chosen source, and reject reason. Bounded checkpoint-truth groups for Ahead01P, Behind01P, Probe A, and Probe B additionally expose sampled checkpoint progression/skips, truth source/gate/times/laps/raw-normalized values, TrackSec correlation, overwrite provenance, RelativeSec mismatch-clearing evidence, and precision difference/error ratios where applicable. Event evidence is collected only while Car Tracking Probe capture is active and latched until a successful CSV-row snapshot consumes it; latest-event fields keep the most recent event while `*SinceSnapshot` counters aggregate multiple truth/progression/overwrite/mismatch events in that capture window. Race-start reset clears diagnostic timing/progression, Soft Debug off invalidates pending provenance, and `TruthSource` remains forward/reverse event provenance while precision `CandidateSource` reports truth versus filtered consumption. It is gated by `EnableCarTrackingProbeCsv`, runtime START/STOP, and `CarTrackingProbeCsvFrequencyHz` (1–20 Hz). Snapshot consumption clears capture-window events/counters while preserving an `existing/unknown` baseline when runtime checkpoint truth remains active, so later prior-truth diagnostics do not reference an impossible chain. No SimHub export/property, Property Snapshot group, dashboard contract, or runtime gap behavior is added or changed.
* `CarSA_Debug_YYYY-MM-DD_HH-mm-ss_<TrackName>.csv` includes an `EventFired` column immediately after `SessionTimeSec`, populated with `1`/`0` based on the event marker pulse state. Each `Ahead01..05` / `Behind01..05` slot also records diagnostics-only assignment evidence: `AssignmentMode`, candidate presence and one-based bounded candidate rank, hysteresis/blink flags, the existing pre-assignment LapDistPct-only retention eligibility and reason, final `IsOnTrack`, and current `LapDistPctValid`, alongside the existing distance, TrackSec, pit, and raw-surface fields. These CSV columns are not SimHub exports/properties and do not affect Property Snapshot grouping.
* `PropertySnapshot_<UTC>_Sess<SessionTimeSec>.csv` is written when master `EnableSoftDebug` and `Enable Property Snapshot` are on and Event Marker is pressed; columns: `SimHubProperty,InternalSource,Value,GroupType` with optional `ChangedVsPrevious` column.
* `MonitorSystem_Events.csv` is a debug-only MonitorSystem event file controlled by `Enable Monitor Event CSV` and `Enable Monitor System`. It writes only changed non-normal MonitorSystem publications (`WATCH`, `CAUTION`, `WARNING`, `FAULT`, `RECOVERED`) with columns `Utc,SessionTimeSec,SessionType,SessionState,MonitorEnum,MonitorSeverityText,MonitorText,MonitorState,Category,Reason,CurrentFuelLitres,OnPitRoad,InPitStall,PlayerCarIdx,ExtraDetail`. It intentionally skips `MONITOR OFF`, `MONITOR READY`, `FUEL HEALTH OK`, unchanged repeat publications, and per-tick OK rows.
* `PropertySnapshot_<UTC>_Sess<SessionTimeSec>.csv` is written when `Enable Property Snapshot` is on and Event Marker is pressed; columns: `SimHubProperty,InternalSource,Value,GroupType` with optional `ChangedVsPrevious` column.
  - Pit/PitExit group include coverage explicitly includes: `Pit.FuelControl.Data`, `Pit.FuelControl.DataText`, `Pit.FuelControl.DataColor`, `Pit.FuelControl.Source`, `Pit.FuelControl.SourceText`, `Pit.FuelControl.TargetLitres`, `Pit.FuelControl.TargetText`.
  - `Pit.FuelControl.Fault` remains `0=None`, `1=Mode fault`, `2=Source/request fault`, `3=Mode + Source/request fault`; stale owned requested-fuel expectations can now expire as manual MFD takeover after the bounded confirmation window, clearing a stale request fault without adding or renaming exports.
* Optional rolling append file `PropertySnapshot_Rolling.csv` writes in wide format (`SimHubProperty` rows + one column per capture timestamp) and includes plugin-export rows plus direct external Fuel/Strategy capture rows when that group is enabled: DataCore reads (`DataCorePlugin.Computed.Fuel_LitersPerLap`, `Fuel_LastLapConsumption`, `Fuel_CurrentLapConsumption`, optional `Fuel_CurrentLapValidForTracking`, `Fuel_RemainingLaps`) and pit-prediction seams (`LalaLaunch.Fuel.Live.TireChangeCount`, `LalaLaunch.Fuel.Live.TireChangeTime_S`, `LalaLaunch.Fuel.Live.TotalStopLoss`, `LalaLaunch.Pit.Box.TargetSec`, `LalaLaunch.Pit.Box.RemainingSec`, `LalaLaunch.Pit.Box.ElapsedSec`).

- 2026-04-30 Phase 1 League Race Class infrastructure: no new public runtime exports yet; existing export semantics unchanged in this phase.


## 2026-06-08 debug inventory confirmation
- No SimHub exports/properties were added, removed, or renamed by the Debug Options UI tidy/master gate change.
- `Car.Debug.OffTrack.ProbeCarIdx` and the old OffTrack Debug CSV workflow remain absent.
- Active debug file writers remain diagnostics-only files rather than SimHub exports: Property Snapshot, CarSA Debug CSV, CarSA Events CSV when enabled in event-change cadence diagnostics, Car Tracking Probe CSV, and Shift Assist Debug CSV.
