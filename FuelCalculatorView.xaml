<UserControl x:Class="LaunchPlugin.FuelCalculatorView"
             xmlns:global="clr-namespace:System.Globalization;assembly=mscorlib"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:styles="clr-namespace:SimHub.Plugins.Styles;assembly=SimHub.Plugins"
             xmlns:ui="clr-namespace:SimHub.Plugins.UI;assembly=SimHub.Plugins"
             xmlns:local="clr-namespace:LaunchPlugin"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:InvertBooleanConverter x:Key="InvertBooleanConverter"/>
        <Style x:Key="FuelChoiceButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="MinWidth" Value="70"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2A2D34"/>
            <Setter Property="BorderBrush" Value="#4C5261"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="True">
                    <Setter Property="Background" Value="#3B7BC6"/>
                    <Setter Property="BorderBrush" Value="#7AB0FF"/>
                </DataTrigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- Unit label style: small, muted -->
        <Style x:Key="UnitHint" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Opacity" Value="0.75"/>
            <Setter Property="Margin" Value="6,2,0,0"/>
        </Style>

        <Style x:Key="SnapshotLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#C9CFDC"/>
            <Setter Property="Margin" Value="0,4,8,0"/>
        </Style>

        <Style x:Key="SnapshotValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <!-- Warning label style when a guard-rail condition is active -->
        <Style x:Key="WarnText" TargetType="TextBlock" BasedOn="{StaticResource UnitHint}">
            <Setter Property="Foreground" Value="#FFCC7A"/>
            <!-- warm amber -->
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="10">
            
            <styles:SHSection Title="STRATEGY PLANNER" ShowSeparator="True" Margin="0,5,0,0">
                <StackPanel>

                    <Grid Margin="5,0,5,5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Grid Grid.Column="0" Margin="0,0,15,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <!-- 0: Section header -->
                                <RowDefinition Height="Auto"/>
                                <!-- 1: Live Session telemetry -->
                                <RowDefinition Height="Auto"/>
                                <!-- 2: Car + Track -->
                                <RowDefinition Height="Auto"/>
                                <!-- 3: Track Condition + Wet factor -->
                                <RowDefinition Height="Auto"/>
                                <!-- 4: Est Avg Lap Time -->
                                <RowDefinition Height="Auto"/>
                                <!-- 5: Pace vs Leader / Race Pace vs PB -->
                                <RowDefinition Height="Auto"/>
                                <!-- 6: Max Fuel Override -->
                                <RowDefinition Height="Auto"/>
                                <!-- 7: Fuel per Lap -->
                            </Grid.RowDefinitions>

                            <!-- Row 0: Car/Track header + planning source toggle -->
                            <Grid Grid.Row="0" Margin="0,5,0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                   Text="Car and Track Parameters"
                                   FontWeight="Bold"
                                   ToolTip="Inputs used to calculate the race strategy plan."/>

                                <!-- Planning source selector (Profile / Live) -->
                                <StackPanel Grid.Column="1"
                                    Orientation="Horizontal"
                                    Margin="12,0,0,0"
                                    VerticalAlignment="Center">
                                    <RadioButton Content="Profile"
                                         IsChecked="{Binding IsPlanningSourceProfile, Mode=TwoWay}"
                                         GroupName="PlanningSourceMode"
                                         Margin="0,0,8,0"
                                         ToolTip="Use saved profile/track data for planning."/>
                                    <RadioButton Content="Live snapshot"
                                         IsChecked="{Binding IsPlanningSourceLiveSnapshot, Mode=TwoWay}"
                                         GroupName="PlanningSourceMode"
                                         IsEnabled="{Binding CanSelectLiveSnapshot}"
                                         ToolTip="Use live session data once confidence builds (may take a few laps)."/>
                                </StackPanel>
                            </Grid>
                            <!-- Live session telemetry, styled like LAUNCH SUMMARY on Post-Launch tab -->
                            <styles:SHExpander Grid.Row="1"
                               Header="{Binding LiveSessionHeader}"
                               IsExpanded="{Binding IsLiveSessionSnapshotExpanded}"
                               Margin="0,0,0,10"
                               ToolTip="Live session snapshot used for planning when available. Confidence improves with more laps.">

                                <Grid Margin="5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="125" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0"
                                       Text="Max Tank Limit"
                                       Style="{StaticResource SnapshotLabelStyle}" />
                                    <TextBlock Grid.Row="0" Grid.Column="1"
                                       Text="{Binding LiveFuelTankSizeDisplay}"
                                       Style="{StaticResource SnapshotValueStyle}" />
                                    <TextBlock Grid.Row="1" Grid.Column="0"
                                       Text="Track State"
                                       Style="{StaticResource SnapshotLabelStyle}" />
                                    <TextBlock Grid.Row="1" Grid.Column="1"
                                       Text="{Binding LiveSurfaceModeDisplay}"
                                       Style="{StaticResource SnapshotValueStyle}" />
                                    <TextBlock Grid.Row="2" Grid.Column="0"
                                       Text="Dry Lap Times"
                                       Style="{StaticResource SnapshotLabelStyle}" />
                                    <TextBlock Grid.Row="2" Grid.Column="1"
                                       Text="{Binding DryLapTimeSummary}"
                                       Style="{StaticResource SnapshotValueStyle}" />
                                    <TextBlock Grid.Row="3" Grid.Column="0"
                                       Text="Dry Pace Deltas"
                                       Style="{StaticResource SnapshotLabelStyle}" />
                                    <TextBlock Grid.Row="3" Grid.Column="1"
                                       Text="{Binding DryPaceDeltaSummary}"
                                       Style="{StaticResource SnapshotValueStyle}" />
                                    <TextBlock Grid.Row="4" Grid.Column="0"
                                       Text="Dry Fuel Burn"
                                       Style="{StaticResource SnapshotLabelStyle}" />
                                    <TextBlock Grid.Row="4" Grid.Column="1"
                                       Text="{Binding DryFuelBurnSummary}"
                                       Style="{StaticResource SnapshotValueStyle}" />
                                    <TextBlock Grid.Row="5" Grid.Column="0"
                                       Text="Data Confidence"
                                       Style="{StaticResource SnapshotLabelStyle}"
                                       ToolTip="Confidence in the live fuel/pace data. Low confidence may use profile estimates." />
                                    <TextBlock Grid.Row="5" Grid.Column="1"
                                       Text="{Binding LiveConfidenceSummary}"
                                       Style="{StaticResource SnapshotValueStyle}"
                                       TextWrapping="Wrap"
                                       ToolTip="Confidence builds with more clean laps; some fields stay locked until confident." />
                                    <TextBlock Grid.Row="6" Grid.Column="0"
                                       Text="Wet Lap Times"
                                       Style="{StaticResource SnapshotLabelStyle}"
                                       Visibility="{Binding ShowWetSnapshotRows, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    <TextBlock Grid.Row="6" Grid.Column="1"
                                       Text="{Binding WetLapTimeSummary}"
                                       Style="{StaticResource SnapshotValueStyle}"
                                       Visibility="{Binding ShowWetSnapshotRows, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    <TextBlock Grid.Row="7" Grid.Column="0"
                                       Text="Wet Pace Delta"
                                       Style="{StaticResource SnapshotLabelStyle}"
                                       Visibility="{Binding ShowWetSnapshotRows, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    <TextBlock Grid.Row="7" Grid.Column="1"
                                       Text="{Binding WetPaceDeltaSummary}"
                                       Style="{StaticResource SnapshotValueStyle}"
                                       Visibility="{Binding ShowWetSnapshotRows, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    <TextBlock Grid.Row="8" Grid.Column="0"
                                       Text="Wet Fuel Burn"
                                       Style="{StaticResource SnapshotLabelStyle}"
                                       Visibility="{Binding ShowWetSnapshotRows, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    <TextBlock Grid.Row="8" Grid.Column="1"
                                       Text="{Binding WetFuelBurnSummary}"
                                       Style="{StaticResource SnapshotValueStyle}"
                                       Visibility="{Binding ShowWetSnapshotRows, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </Grid>
                            </styles:SHExpander>

                            <!-- NEW: Car + Track selectors inside the section -->
                            <Grid Grid.Row="2" Margin="0,5,0,10" Visibility="{Binding IsPlanningSourceProfile,Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="250"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="250"/>
                                </Grid.ColumnDefinitions>

                            <!-- Car & Track, vertically aligned in a 2x2 Grid -->
                                <Grid Grid.Row="1" Margin="0,5,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <!-- label column -->
                                        <ColumnDefinition Width="300"/>
                                        <!-- combo column -->
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Car Profile:"
                                        VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="Bold"
                                        ToolTip="Choose which car profile to use for planning."/>
                                    <ComboBox  Grid.Row="0" Grid.Column="1" Width="300" Margin="0,0,0,6"
                                         ItemsSource="{Binding AvailableCarProfiles}"
                                         SelectedItem="{Binding SelectedCarProfile, Mode=TwoWay}"
                                         DisplayMemberPath="ProfileName"
                                         ToolTip="Select a car profile to plan a strategy for."/>
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Track:"
                                        VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="Bold"
                                        ToolTip="Choose the track and layout for planning."/>
                                    <ComboBox  Grid.Row="1" Grid.Column="1" Width="Auto"
                                         ItemsSource="{Binding AvailableTrackStats}"
                                         DisplayMemberPath="DisplayName"
                                         SelectedItem="{Binding SelectedTrackStats, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         ToolTip="Select the track and layout."/>
                                </Grid>
                            </Grid>

                            <!-- Track condition (Profile planning only) -->
                            <StackPanel Grid.Row="3" Margin="0,5,0,5">
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                    <TextBlock Text="Track Condition:" VerticalAlignment="Center" Margin="0,0,10,0"
                                               ToolTip="Select dry or wet planning mode for this track."/>
                                    <RadioButton Content="Auto" IsChecked="{Binding IsTrackConditionAuto, Mode=TwoWay}" GroupName="TrackCondition"
                                                 ToolTip="Use automatic dry/wet detection."/>
                                    <RadioButton Content="Dry" IsChecked="{Binding IsTrackConditionManualDry, Mode=TwoWay}" GroupName="TrackCondition" Margin="10,0,0,0"
                                                 ToolTip="Plan using dry track data."/>
                                    <RadioButton Content="Wet" IsChecked="{Binding IsTrackConditionManualWet, Mode=TwoWay}" GroupName="TrackCondition" Margin="10,0,0,0"
                                                 ToolTip="Plan using wet track data and the wet factor."/>
                                </StackPanel>
                                <TextBlock Text="{Binding TrackConditionModeLabel}" Style="{StaticResource UnitHint}" Margin="2,2,0,0" />
                                <ui:TitledSlider Title="Wet Factor (%):" Minimum="70" Maximum="130" Value="{Binding WetFactorPercent, Mode=TwoWay}" Visibility="{Binding IsEffectiveWetCondition, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}" Margin="0,5,0,0"
                                                 ToolTip="Scale fuel burn for wet running (%). Higher = higher burn."/>
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0" Visibility="{Binding IsEffectiveWetCondition, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Text="{Binding SourceWetFactorDisplay}" Style="{StaticResource UnitHint}" VerticalAlignment="Center" Visibility="{Binding HasSourceWetFactor, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                    <Button Content="Use source" Command="{Binding ApplySourceWetFactorCommand}" Margin="10,0,0,0" Visibility="{Binding HasSourceWetFactor, Converter={StaticResource BooleanToVisibilityConverter}}"
                                            ToolTip="Apply the suggested wet factor from saved or live data."/>
                                </StackPanel>
                            </StackPanel>

                            <!-- Avg Lap Time -->
                            <Grid Grid.Row="4" Margin="0,15,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="Avg Lap Time:" VerticalAlignment="Center" Margin="0,0,10,0"
                                           ToolTip="Average lap time used for strategy (m:ss.fff)." />

                                <TextBox Grid.Column="1" VerticalAlignment="Center" Width="60" HorizontalAlignment="Left"
                                         ToolTip="Edit average lap time in m:ss.fff (used for strategy calculations).">
                                    <TextBox.Text>
                                        <Binding Path="EstimatedLapTime" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                                            <Binding.ValidationRules>
                                                <local:LapTimeValidationRule />
                                            </Binding.ValidationRules>
                                        </Binding>
                                    </TextBox.Text>
                                </TextBox>

                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0"
                                            ToolTip="Data source hint for the lap time estimate.">
                                    <TextBlock Text="{Binding LapTimeSourceInfo}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding LiveLapPaceInfo, StringFormat=Live avg: {0}}"
                                       Foreground="Gray"
                                       FontStyle="Italic"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"
                                       ToolTip="Live rolling average lap time."
                                       Visibility="{Binding ShowLiveLapHelper, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    <TextBlock Text="{Binding ProfileAvgLapTimeDisplay, StringFormat=Profile avg: {0}}"
                                       Foreground="Gray"
                                       FontStyle="Italic"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"
                                       ToolTip="Saved average lap time from profile (condition aware)."
                                       Visibility="{Binding ShowProfileLapHelper, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </StackPanel>

                            </Grid>

                        <StackPanel Grid.Row="5" Margin="0,10,0,10" Grid.IsSharedSizeScope="True">

                            <!-- Leader delta -->
                            <Grid HorizontalAlignment="Stretch">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,5,0,5">
                                    <TextBlock Text="Your Pace vs Leader (s):" ToolTip="How many seconds slower your average lap time is compared to the race leader."/>
                                    <TextBlock Text=" Live Avg Delta to Ldr: " Foreground="Gray" FontStyle="Italic" Margin="10,1,0,5"/>
                                        <TextBlock Text="{Binding AvgDeltaToLdrValue}" Foreground="Gray" FontStyle="Italic" Margin="10,1,0,5"/>
                                </StackPanel>

                                <Grid Grid.Row="1" HorizontalAlignment="Stretch">
                                    <Grid.ColumnDefinitions>
                                        <!-- Slider column: use * (NOT shared) -->
                                        <ColumnDefinition Width="*"/>
                                        <!-- Value column: Auto and SHARED across both rows -->
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="DeltaValueCol"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Slider Grid.Column="0"
              VerticalAlignment="Center"
              Value="{Binding LeaderDeltaSeconds, Mode=TwoWay}"
              Margin="0,2,0,0"
              Minimum="0" Maximum="5"
              SmallChange="0.1" TickFrequency="0.1"
              IsSnapToTickEnabled="True"
              IsEnabled="{Binding IsLeaderDeltaEditable}"
              ToolTip="Adjust the pace delta to the leader (sec) used in the plan." />

                                    <TextBlock Grid.Column="1"
                Text="{Binding LeaderDeltaSeconds, StringFormat=N1}"
                VerticalAlignment="Center"
                Margin="10,0,0,0"
                MinWidth="35"
                TextAlignment="Right"/>
                                </Grid>
                            </Grid>

                        </StackPanel>

                        <StackPanel Grid.Row="6" Margin="0,15,0,0">
                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="Max Fuel Override (litres):" VerticalAlignment="Center"
                                           ToolTip="Profile mode: limit the usable tank size for restricted fuel races (L). Live Snapshot: locked to the detected live session cap." />
                            </Grid>

                            <ui:TitledSlider Title="" Minimum="0" Maximum="{Binding MaxFuelOverrideMaximum}" Value="{Binding MaxFuelOverrideDisplayValue, Mode=TwoWay}" ToolTip="Profile mode: edit the planner tank limit. Live Snapshot: this control is locked to the detected live session cap." IsEnabled="{Binding IsPlanningSourceProfile}">
                                <ui:TitledSlider.Resources>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsMaxFuelOverrideTooHigh}" Value="True">
                                                <Setter Property="Foreground" Value="#FFC43C"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ui:TitledSlider.Resources>
                            </ui:TitledSlider>
                            <TextBlock Text="{Binding MaxFuelOverridePercentDisplay}" HorizontalAlignment="Right" Margin="0,4,0,0"
                                       Visibility="{Binding IsPlanningSourceProfile, Converter={StaticResource BooleanToVisibilityConverter}}"
                                       FontStyle="Italic" Opacity="0.7"/>
                        </StackPanel>

                        <Grid Grid.Row="7" Margin="0,15,0,0" Grid.IsSharedSizeScope="True">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Fuel per Lap:" VerticalAlignment="Center" Margin="0,0,10,0"
                                       ToolTip="Fuel burn per lap used for strategy (L/lap)." />

                            <TextBox Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" Width="50" HorizontalAlignment="Left" Text="{Binding FuelPerLapText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     ToolTip="Fuel burn per lap (L/lap). Edit to override the suggested value." />

                            <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Margin="2,4,0,0" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="{Binding FuelPerLapSourceInfo}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsPlanningSourceProfile}" Value="True">
                                                <Setter Property="Text" Value="profile"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsPlanningSourceLiveSnapshot}" Value="True">
                                                <Setter Property="Text" Value="Live"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <Grid Grid.Row="0" Grid.Column="3" Grid.RowSpan="2" Margin="10,5,0,0" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="FuelChoice1"/>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="FuelChoice2"/>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="FuelChoice3"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Grid Grid.ColumnSpan="3" Visibility="{Binding IsPlanningSourceProfile, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="FuelChoice1"/>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="FuelChoice2"/>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="FuelChoice3"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0" Content="AVG" Style="{StaticResource FuelChoiceButtonStyle}" Tag="{Binding IsProfileAverageFuelChoiceActive}" Command="{Binding UseProfileFuelPerLapCommand}" IsEnabled="{Binding HasProfileFuelPerLap}" ToolTip="Use the average fuel per lap saved in the selected profile."/>
                                    <Button Grid.Column="1" Content="ECO" Style="{StaticResource FuelChoiceButtonStyle}" Tag="{Binding IsProfileEcoFuelChoiceActive}" Command="{Binding UseProfileFuelSaveCommand}" IsEnabled="{Binding IsProfileFuelSaveAvailable}" ToolTip="Use the most fuel-efficient lap stored in the selected profile."/>
                                    <Button Grid.Column="2" Content="MAX" Style="{StaticResource FuelChoiceButtonStyle}" Tag="{Binding IsProfileMaxFuelChoiceActive}" Margin="0" Command="{Binding UseProfileFuelMaxCommand}" IsEnabled="{Binding IsProfileFuelMaxAvailable}" ToolTip="Use the highest recorded fuel per lap stored in the selected profile."/>
                                </Grid>

                                <Grid Grid.ColumnSpan="3" Grid.Row="1" Visibility="{Binding IsPlanningSourceProfile, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,4,0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="FuelChoice1"/>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="FuelChoice2"/>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="FuelChoice3"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding ProfileAvgFuelDisplay}" Foreground="Gray" FontStyle="Italic" Margin="0,0,8,0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding ProfileFuelSaveDisplay}" Foreground="Gray" FontStyle="Italic" Margin="0,0,8,0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="{Binding ProfileFuelMaxDisplay}" Foreground="Gray" FontStyle="Italic" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Grid>

                                <Grid Grid.ColumnSpan="3" Visibility="{Binding IsPlanningSourceLiveSnapshot, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="FuelChoice1"/>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="FuelChoice2"/>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="FuelChoice3"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0" Content="AVG" Style="{StaticResource FuelChoiceButtonStyle}" Tag="{Binding IsLiveAverageFuelChoiceActive}" Command="{Binding UseLiveFuelPerLapCommand}" IsEnabled="{Binding IsLiveFuelPerLapAvailable}" ToolTip="Use the live rolling average fuel per lap."/>
                                    <Button Grid.Column="1" Content="ECO" Style="{StaticResource FuelChoiceButtonStyle}" Tag="{Binding IsLiveSaveFuelChoiceActive}" Command="{Binding UseLiveFuelSaveCommand}" IsEnabled="{Binding IsLiveFuelSaveAvailable}" ToolTip="Use the most fuel-efficient lap observed in the current live session."/>
                                    <Button Grid.Column="2" Content="MAX" Style="{StaticResource FuelChoiceButtonStyle}" Tag="{Binding IsLiveMaxFuelChoiceActive}" Margin="0" Command="{Binding UseMaxFuelPerLapCommand}" IsEnabled="{Binding IsMaxFuelAvailable}" ToolTip="Use the highest valid fuel per lap observed in the current live session."/>
                                </Grid>

                                <Grid Grid.ColumnSpan="3" Grid.Row="1" Visibility="{Binding IsPlanningSourceLiveSnapshot, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,4,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="FuelChoice1"/>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="FuelChoice2"/>
                                        <ColumnDefinition Width="Auto" SharedSizeGroup="FuelChoice3"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding LiveFuelPerLapDisplay}" Foreground="Gray" FontStyle="Italic" Margin="0,0,8,0" HorizontalAlignment="Center" VerticalAlignment="Center" ToolTip="Live rolling average fuel per lap."/>
                                    <TextBlock Grid.Column="1" Text="{Binding LiveFuelSaveDisplay}" Foreground="Gray" FontStyle="Italic" Margin="0,0,8,0" HorizontalAlignment="Center" VerticalAlignment="Center" ToolTip="Most efficient lap recorded in this session."/>
                                    <TextBlock Grid.Column="2" Text="{Binding LiveFuelMaxDisplay}" Foreground="Gray" FontStyle="Italic" HorizontalAlignment="Center" VerticalAlignment="Center" ToolTip="Highest valid fuel per lap recorded in this session."/>
                                </Grid>
                            </Grid>
                        </Grid>
                    </Grid>

                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="15,0,0,0">
                        <TextBlock Text="Race Strategy" FontWeight="Bold" Margin="0,5,0,5"/>

                            <!-- Race Preset selector (inline, auto-apply) -->
                            <Grid Margin="0,10,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                   Text="Race Preset:"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"
                                   FontWeight="Bold"/>

                                <ComboBox Grid.Column="1"
                                  Width="140"
                                  MinWidth="140"
                                  ItemsSource="{Binding AvailablePresets}" 
                                  SelectedItem="{Binding SelectedPreset, Mode=TwoWay}"
                                  DisplayMemberPath="Name"
                                  ToolTip="Select a preset to apply."/>

                                <TextBlock Grid.Column="2"
                                   Text="modified"
                                   Foreground="Yellow"
                                   Margin="10,0,0,0"
                                   VerticalAlignment="Center"
                                   FontStyle="Italic"
                                   Opacity="0.8">
                                    <TextBlock.Visibility>
                                        <Binding Path="IsPresetModifiedFlag"
                                            Converter="{StaticResource BooleanToVisibilityConverter}"/>
                                    </TextBlock.Visibility>
                                </TextBlock>

                                <styles:SHButtonPrimary Grid.Column="3"
                            Content="Presets..."
                            Height="28"
                            Margin="12,0,0,0"
                            VerticalAlignment="Center"
                            Click="OnOpenPresetManager"
                            ToolTip="Open the Preset Manager to create, rename, edit, or delete strategy presets."/>
                            </Grid>

                        <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                            <TextBlock Text="Race Type:" VerticalAlignment="Center" Margin="0,0,10,0"
                                       ToolTip="Choose whether the race is limited by laps or time."/>
                            <RadioButton Content="Lap-Limited" IsChecked="{Binding IsLapLimitedRace, Mode=TwoWay}" GroupName="RaceType"
                                         ToolTip="Plan for a fixed number of laps."/>
                            <RadioButton Content="Time-Limited" IsChecked="{Binding IsTimeLimitedRace, Mode=TwoWay}" GroupName="RaceType" Margin="10,0,0,0"
                                         ToolTip="Plan for a fixed race duration (minutes)."/>
                            <RadioButton Content="Live Detect" IsChecked="{Binding IsLiveDetectRace, Mode=TwoWay}" GroupName="RaceType" Margin="10,0,0,0"
                                         ToolTip="Use declared race session metadata as race type/length authority."/>
                        </StackPanel>
                        <TextBlock Text="{Binding LiveDetectHelperText}"
                                   Margin="0,-6,0,10"
                                   FontSize="12"
                                   Opacity="0.8"
                                   Visibility="{Binding IsLiveDetectRace, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                        <ui:TitledSlider Title="Race Laps:" Minimum="1" Maximum="500" Value="{Binding RaceLaps, Mode=TwoWay}" Visibility="{Binding ShowEffectiveLapLimitedRace, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}" IsEnabled="{Binding IsRaceLengthEditable}"
                                         ToolTip="Total race distance in laps."/>
                        <ui:TitledSlider Title="Race Minutes:" Minimum="10" Maximum="1440" Value="{Binding RaceMinutes, Mode=TwoWay}" Visibility="{Binding ShowEffectiveTimeLimitedRace, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}" IsEnabled="{Binding IsRaceLengthEditable}"
                                         ToolTip="Total race duration in minutes."/>

                        <ui:TitledSlider Title="Formation Lap Fuel (liters):" Minimum="0" Maximum="10" Value="{Binding FormationLapFuelLiters, Mode=TwoWay}" Margin="0,10,0,0"
                                         ToolTip="Fuel to reserve for formation or pace laps (L)." />

                        <StackPanel Margin="0,10,0,0">
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                                <TextBlock Text="Contingency Type:" VerticalAlignment="Center" Margin="0,0,10,0"
                                           ToolTip="Choose whether contingency is in laps or litres."/>
                                <RadioButton Content="Extra Laps" IsChecked="{Binding IsContingencyInLaps, Mode=TwoWay}" GroupName="ContingencyType"
                                             ToolTip="Add extra laps of fuel as a safety margin."/>
                                <RadioButton Content="Extra Litres" IsChecked="{Binding IsContingencyLitres, Mode=TwoWay}" GroupName="ContingencyType" Margin="10,0,0,0"
                                             ToolTip="Add extra litres as a safety margin."/>
                            </StackPanel>
                            <ui:TitledSlider Title="Contingency Laps:" Minimum="0" Maximum="5" Value="{Binding ContingencyValue, Mode=TwoWay}" Visibility="{Binding IsContingencyInLaps, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}"
                                             ToolTip="Number of extra laps of fuel to add."/>
                            <ui:TitledSlider Title="Contingency Litres:" Minimum="0" Maximum="20" Value="{Binding ContingencyValue, Mode=TwoWay}" Visibility="{Binding IsContingencyLitres, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}"
                                             ToolTip="Extra fuel to add as a safety margin (L)." />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,10,0,6">
                            <TextBlock Text="PreRace Mode" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,16,0"
                                       ToolTip="Driver-selected mode for the PreRace on-grid info layer only. Auto mirrors planner-derived assumptions; manual modes only affect PreRace outputs and never alter planner/live fuel behavior."/>
                            <ComboBox Width="170"
                                      VerticalAlignment="Center"
                                      SelectedValuePath="Tag"
                                      SelectedValue="{Binding SelectedPreRaceMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      ToolTip="Auto mirrors planner-derived assumptions for PreRace outputs. No Stop uses current fuel only; Single Stop uses current + pit-menu add intent; Multi Stop remains informational and favors stint threshold context.">
                                    <ComboBoxItem Content="Auto">
                                        <ComboBoxItem.Tag>
                                            <sys:Int32>3</sys:Int32>
                                        </ComboBoxItem.Tag>
                                    </ComboBoxItem>
                                    <ComboBoxItem Content="No Stop">
                                        <ComboBoxItem.Tag>
                                            <sys:Int32>0</sys:Int32>
                                        </ComboBoxItem.Tag>
                                    </ComboBoxItem>
                                    <ComboBoxItem Content="Single Stop">
                                        <ComboBoxItem.Tag>
                                            <sys:Int32>1</sys:Int32>
                                        </ComboBoxItem.Tag>
                                    </ComboBoxItem>
                                    <ComboBoxItem Content="Multi Stop">
                                        <ComboBoxItem.Tag>
                                            <sys:Int32>2</sys:Int32>
                                        </ComboBoxItem.Tag>
                                    </ComboBoxItem>
                                </ComboBox>
                        </StackPanel>

                        <!-- Full-width sliders (restored to original single-column layout) -->
                        <ui:TitledSlider
                            Title="Pit Drive-Through Loss (s):"
                            Minimum="0" Maximum="60"
                            Value="{Binding PitLaneTimeLoss, Mode=TwoWay}"
                            Margin="0,0,0,6">
                            <ui:TitledSlider.ToolTip>
                                <ToolTip>
                                    <TextBlock Text="Estimated pit lane loss per stop. Auto selection uses: drive-through loss, then direct pit entry-to-exit time, then the default."
                                        TextWrapping="Wrap" Width="260"/>
                                </ToolTip>
                            </ui:TitledSlider.ToolTip>
                        </ui:TitledSlider>

                        <ui:TitledSlider
                            Title="Tyre Change Time (s): [Default is 22s]"
                            Minimum="0" Maximum="60"
                            Value="{Binding TireChangeTime, Mode=TwoWay}">
                            <ui:TitledSlider.ToolTip>
                                <ToolTip>
                                    <TextBlock Text="Additional stationary time if tyres are changed. Set to 0s for a drive-through-only stop."
                                        TextWrapping="Wrap" Width="260"/>
                                </ToolTip>
                            </ui:TitledSlider.ToolTip>
                        </ui:TitledSlider>

                    </StackPanel>
                    </Grid>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <styles:SHButtonPrimary
                            Content="Refresh Calcs"
                            Command="{Binding RefreshPlannerViewCommand}"
                            Padding="8,2"
                            HorizontalAlignment="Left"
                            ToolTip="Reload the planner from the active profile and live session without clearing live samples."/>
                        <styles:SHButtonPrimary
                            Content="Save All to Profile"
                            Command="{Binding SavePlannerDataToProfileCommand}"
                            Padding="8,2"
                            HorizontalAlignment="Left"
                            ToolTip="Save the current Race and Strategy parameters to the selected car/track profile."/>
                        <TextBlock Text="(modified)"
                                   Foreground="Yellow"
                                   Margin="10,0,0,0"
                                   VerticalAlignment="Center"
                                   FontStyle="Italic" Opacity="0.8">
                            <TextBlock.Visibility>
                               <Binding Path="IsPlannerDirty"
                                 Converter="{StaticResource BooleanToVisibilityConverter}"/>
                            </TextBlock.Visibility>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </styles:SHSection>

            <styles:SHSection Title="CALCULATED STRATEGY" ShowSeparator="True" Margin="0,10,0,0">
                <StackPanel Margin="5,10,5,5">
                    <TextBlock Text="{Binding ValidationMessage}" Foreground="#FFC43C" FontWeight="Bold" Margin="0,0,0,10" TextWrapping="Wrap" Visibility="{Binding IsValidationMessageVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Total Fuel Needed" FontWeight="Bold"/>
                            <TextBlock FontSize="21" Margin="0,0,0,5">
                                <Run Text="{Binding TotalFuelNeeded, StringFormat='{}{0:F1} litres', Mode=OneWay}" FontWeight="Bold"/>
                            </TextBlock>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="First Stint Fuel" FontWeight="Bold"/>
                            <Border BorderBrush="Gray" BorderThickness="1" Padding="4,2" CornerRadius="3" Background="#222" HorizontalAlignment="Left">
                                <TextBlock FontSize="21">
                                    <Run Text="{Binding FirstStintFuel, StringFormat='{}{0:F1} litres', Mode=OneWay}" FontWeight="Bold"/>
                                </TextBlock>
                            </Border>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Visibility="{Binding IsPitstopRequired, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="Est. Time Per Stop" FontWeight="Bold"/>
                            <TextBlock FontSize="21" Margin="0,0,0,5">
                                <Run Text="{Binding FirstStopTimeLoss, StringFormat='{}{0:F1}s', Mode=OneWay}" FontWeight="Bold"/>
                            </TextBlock>
                        </StackPanel>

                        <StackPanel Grid.Column="3" HorizontalAlignment="Right">
                            <TextBlock Text="Required Pit Stops" FontWeight="Bold" TextAlignment="Right"/>
                            <TextBlock FontSize="21" TextAlignment="Right">
                                <Run Text="{Binding RequiredPitStops, StringFormat='{}{0}', Mode=OneWay}" FontWeight="Bold"/>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                    <TextBlock FontSize="14" Margin="0,10,0,10" Visibility="{Binding IsTimeLimitedRace, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Run Text="Est. Drive Time After Timer Zero: "/>
                        <Run Text="{Binding ExtraTimeAfterLeader, Mode=OneWay}" FontWeight="Bold"/>
                    </TextBlock>
                    <TextBlock Text="Stint Breakdown:" FontWeight="Bold" Margin="0,5,0,5"/>
                    <Border BorderBrush="Gray" BorderThickness="1" Padding="5">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="400">
                            <TextBlock FontFamily="Consolas" FontSize="18" Text="{Binding StintBreakdown, Mode=OneWay}" TextWrapping="Wrap"/>
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </styles:SHSection>

            <styles:SHSection Title="FUEL SAVE vs. TIME LOSS SIMULATOR" ShowSeparator="True" Margin="0,15,0,0">
                <StackPanel Margin="5,10,5,5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,15,0">
                            <Grid Margin="0,5,0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Fuel Save Target (L/Lap):" VerticalAlignment="Center" Margin="0,0,10,0" Grid.Column="0"/>
                                <Slider Grid.Column="1" VerticalAlignment="Center" Value="{Binding FuelSaveTarget, Mode=TwoWay}" Minimum="0" Maximum="1.0" SmallChange="0.05" TickFrequency="0.05" IsSnapToTickEnabled="True"
                                        ToolTip="Fuel to save per lap (L/lap) for the simulator." />
                                <TextBlock Grid.Column="2" Text="{Binding FuelSaveTarget, StringFormat=N2}" VerticalAlignment="Center" Margin="10,0,0,0" MinWidth="35" TextAlignment="Right"/>
                            </Grid>
                            <TextBlock Text="Est. Time Loss per Lap (m:ss.fff):" Margin="0,10,0,2"/>
                            <TextBox Text="{Binding TimeLossPerLapOfFuelSave, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     ToolTip="Estimated time lost per lap when saving fuel (m:ss.fff)." />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                            <TextBlock FontSize="14" Margin="0,0,0,5"><Run Text="Pit Stops Saved: "/><Run Text="{Binding StopsSaved, Mode=OneWay}" FontWeight="Bold"/></TextBlock>
                            <TextBlock FontSize="14"><Run Text="Total Race Time Difference: "/><Run Text="{Binding TotalTimeDifference, Mode=OneWay}" FontWeight="Bold"/></TextBlock>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </styles:SHSection>

        </StackPanel>
    </ScrollViewer>
</UserControl>
