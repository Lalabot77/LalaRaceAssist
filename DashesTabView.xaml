<UserControl x:Class="LaunchPlugin.DashesTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:LaunchPlugin"
             xmlns:styles="clr-namespace:SimHub.Plugins.Styles;assembly=SimHub.Plugins"
             xmlns:ui="clr-namespace:SimHub.Plugins.UI;assembly=SimHub.Plugins"
             mc:Ignorable="d" d:DesignWidth="900" d:DesignHeight="1200" x:Name="DashSettingsPage">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="10" Width="820" HorizontalAlignment="Left">
            <Expander Header="General / Dash Controls" Margin="0,0,0,12" IsExpanded="True">
                <StackPanel Margin="12,10,12,12">
                    <ui:ControlsEditor ActionName="LalaLaunch.MsgCx" FriendlyName="Acknowledge" ToolTip="Acknowledge or dismiss plugin messages, prompts and alerts. This is the primary user response button used throughout Lala Race Assist." />
                    <ui:ControlsEditor ActionName="LalaLaunch.TogglePitScreen" FriendlyName="Toggle Pit Screen Popup" Margin="0,8,0,0" ToolTip="Assign a button to show or hide the pit screen popup." />
                    <ui:ControlsEditor ActionName="LalaLaunch.PrimaryDashMode" FriendlyName="Plugin Quick Reset" Margin="0,8,0,0" ToolTip="Bind to allow rapid plugin reset if any plugin or dash data issues." />
                    <ui:ControlsEditor ActionName="LalaLaunch.StrategyDash.ModeToggle" FriendlyName="Toggle Strategy Dash Mode" Margin="0,8,0,0" ToolTip="Assign a button to toggle the Strategy Dash between Advanced and Simple mode. The current state is exported as LalaLaunch.StrategyDash.AdvancedMode (true = Advanced, false = Simple)." />
                    <ui:ControlsEditor ActionName="LalaLaunch.DeclutterMode" FriendlyName="Cycle Declutter Mode" Margin="0,8,0,0" ToolTip="Assign a button to cycle declutter mode (0/1/2) for dash visibility bindings." />
                    <ui:ControlsEditor ActionName="LalaLaunch.ToggleDarkMode" FriendlyName="Toggle Dark Mode" Margin="0,8,0,0" ToolTip="Assign a button to cycle LalaLaunch Dark Mode mode (Off → Manual → Auto). Warning: don’t bind this to the same button as Lovely’s True Dark toggle. If ‘Use Lovely True Dark’ is enabled and Lovely is available, LalaLaunch toggle is ignored." />
                    <ui:ControlsEditor ActionName="LalaLaunch.DarkModeBrightnessDecrease" FriendlyName="Dark Mode Brightness -" Margin="0,8,0,0" ToolTip="Assign a button to decrease the existing Dark Mode brightness slider value by 5 percentage points, clamped to 0–100." />
                    <ui:ControlsEditor ActionName="LalaLaunch.DarkModeBrightnessIncrease" FriendlyName="Dark Mode Brightness +" Margin="0,8,0,0" ToolTip="Assign a button to increase the existing Dark Mode brightness slider value by 5 percentage points, clamped to 0–100." />
                    <ui:ControlsEditor ActionName="LalaLaunch.ShiftAssistToggle" FriendlyName="Shift Assist Toggle" Margin="0,8,0,0" ToolTip="Assign a button to toggle the Shift Assist system enabled setting ON/OFF. This does not open learning/review popups or reset learned data." />
                    <ui:ControlsEditor ActionName="LalaLaunch.StrategyContingencyAdd1L" FriendlyName="Contingency +1L" Margin="0,8,0,0" ToolTip="Assign a button to switch Strategy contingency to Extra Litres if needed, then add 1.0 litre." />
                    <ui:ControlsEditor ActionName="LalaLaunch.StrategyContingencyRemove1L" FriendlyName="Contingency -1L" Margin="0,8,0,0" ToolTip="Assign a button to switch Strategy contingency to Extra Litres if needed, then remove 1.0 litre, clamped at zero." />
                    <ui:ControlsEditor ActionName="LalaLaunch.LeagueClass.ToggleEnabled" FriendlyName="League Class Toggle" Margin="0,8,0,0" ToolTip="Assign a button to toggle League Class mode ON/OFF using the existing League Class enable guard and resolver reload behavior." />
                </StackPanel>
            </Expander>

            <Expander Header="Launch" Margin="0,0,0,12" IsExpanded="False">
                <StackPanel Margin="12,10,12,12">
                    <ui:ControlsEditor ActionName="LalaLaunch.LaunchMode" FriendlyName="Toggle Launch Mode" ToolTip="Manual prime/abort launch mode (useful for testing and non-standing-start sessions)." />
                </StackPanel>
            </Expander>

            <Expander Header="Pit Commands" Margin="0,0,0,12" IsExpanded="False">
                <StackPanel Margin="12,10,12,12">
                    <TextBlock Foreground="LightGray" TextWrapping="Wrap" Text="Built-in pit command actions let you bind your pit service controls directly in SimHub Controls &amp; Events and trigger them from hardware, keyboard, or dashboard buttons." />
                    <StackPanel Margin="0,10,0,0">
                        <ui:ControlsEditor ActionName="LalaLaunch.Pit.ClearAll" FriendlyName="Pit Clear All"/>
                        <ui:ControlsEditor ActionName="LalaLaunch.Pit.ClearTires" FriendlyName="Clear Tyres"/>
                        <ui:ControlsEditor ActionName="LalaLaunch.Pit.ToggleFuel" FriendlyName="Toggle Fuel"/>
                        <ui:ControlsEditor ActionName="LalaLaunch.Pit.FuelSetZero" FriendlyName="Fuel 0"/>
                        <ui:ControlsEditor ActionName="LalaLaunch.Pit.FuelAdd1" FriendlyName="Fuel +1"/>
                        <ui:ControlsEditor ActionName="LalaLaunch.Pit.FuelRemove1" FriendlyName="Fuel -1"/>
                        <ui:ControlsEditor ActionName="LalaLaunch.Pit.FuelAdd10" FriendlyName="Fuel +10"/>
                        <ui:ControlsEditor ActionName="LalaLaunch.Pit.FuelRemove10" FriendlyName="Fuel -10"/>
                        <ui:ControlsEditor ActionName="LalaLaunch.Pit.FuelSetMax" FriendlyName="Fuel MAX"/>
                        <ui:ControlsEditor ActionName="LalaLaunch.Pit.ToggleTiresAll" FriendlyName="Toggle Tyres"/>
                        <ui:ControlsEditor ActionName="LalaLaunch.Pit.ToggleFastRepair" FriendlyName="Toggle Fast Repair"/>
                        <ui:ControlsEditor ActionName="LalaLaunch.Pit.ToggleAutoFuel" FriendlyName="Toggle Auto Fuel"/>
                        <ui:ControlsEditor ActionName="LalaLaunch.Pit.Windshield" FriendlyName="Toggle Tear-off"/>
                        <TextBlock Margin="0,12,0,4" FontWeight="SemiBold" Text="Tyre Control"/>
                        <TextBlock Foreground="LightGray" TextWrapping="Wrap" Text="Plugin-owned tyre mode control (OFF / DRY / WET / AUTO)."/>
                        <ui:ControlsEditor ActionName="LalaLaunch.Pit.TyreControl.ModeCycle" FriendlyName="Tyre Mode Cycle"/>
                        <TextBlock Margin="0,12,0,4" FontWeight="SemiBold" Text="Pit Fuel Control"/>
                        <TextBlock Foreground="LightGray" TextWrapping="Wrap" Text="Plugin-owned pit fuel control surface (data/source/mode logic remains plugin-owned)."/>
                        <ui:ControlsEditor ActionName="LalaLaunch.Pit.FuelControl.CycleData" FriendlyName="Fuel Data Cycle"/>
                    </StackPanel>
                </StackPanel>
            </Expander>

            <Expander Header="Custom Messages" Margin="0,0,0,12" IsExpanded="False">
                <StackPanel Margin="12,10,12,12">
                    <TextBlock Foreground="LightGray" TextWrapping="Wrap" Text="Configure up to 10 custom message buttons. Give each slot a friendly name, enter your message text, then bind it like any other plugin action." />
                    <ItemsControl ItemsSource="{Binding Settings.CustomMessages}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,8,0,0" Padding="8" BorderBrush="#2C2C2C" BorderThickness="1" CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock FontWeight="SemiBold" Margin="0,0,0,6" Text="{Binding FriendlyActionName}" />
                                        <Grid Margin="0,0,0,6">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,0" Text="Label"/>
                                            <TextBox Grid.Column="1" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ToolTip="Friendly name shown in this settings list and used for feedback text when the message is sent."/>
                                        </Grid>
                                        <Grid Margin="0,0,0,6">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,0" Text="Message"/>
                                            <TextBox Grid.Column="1" Text="{Binding MessageText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ToolTip="Chat message content to send when this custom message action is triggered."/>
                                        </Grid>
                                        <ui:ControlsEditor ActionName="{Binding ActionName}" FriendlyName="{Binding FriendlyActionName}"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Expander>
        </StackPanel>
    </ScrollViewer>
</UserControl>
